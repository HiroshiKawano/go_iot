# テストガイダンス集（Gin + templ + HTMX + sqlc/pgx 版）

本書は、別プロジェクト(Laravel 12 + Blade + HTMX + MariaDB + PHPUnit)の cc-sdd / TDD 開発で実際に遭遇したテストエラー知見集を、
本プロジェクト(Go 1.26 + Gin + templ + HTMX + Alpine.js + sqlc/pgx + PostgreSQL 16)の流儀へ全面的に書き換えたものである(2026-06-07)。
原本(Laravel 版)は別プロジェクト由来のため本リポジトリには残していない。

各節は次のいずれかで扱う:
- **PORT(直接移植)**: 教訓がそのまま通用。Go の実例に置換。
- **ADAPT(機構読替)**: 概念は通用するが仕組みが違う(Eloquent→sqlc/pgx、Blade→templ、PHPUnit→go test、MariaDB→PostgreSQL 等)。
- **DROP(Go 非該当)**: Go/本構成に対応物が無い純 Laravel 固有。`> 【Go では該当なし】` で理由のみ残す。

節番号は traceability のため原本に準拠する(本文は Go 版)。下記は Go テーマ別の早見索引。

## Go テーマ別索引
- **テスト環境・ツールチェーン**: §1, §3.4, §5.8, §37
- **DB テスト(sqlc / pgx / PostgreSQL)**: §2, §5.1–5.7, §8, §21, §30, §31, §32, §38, §39, §40, §42, §43, §44, §46
- **HTTP ステータス・レスポンス**: §3, §9, §11, §22
- **templ テンプレートのテスト**: §4, §12, §15, §16, §20, §36, §47
- **HTMX リクエストのテスト**: §7, §34
- **認証・認可・CSRF**: §6, §28
- **バリデーションのテスト**: §10, §24, §25, §41
- **クライアントサイド(Alpine / Tom Select / JSON 埋め込み / E2E)**: §5.6, §13, §18, §19, §29
- **CRUD・一括保存・CSV 入出力**: §23, §26, §33, §35, §45
- **データ整合性(楽観ロック等)**: §14
- **チェックリスト**: §27

---

## 1. テスト環境の基本構成

> **分類: ADAPT** — DB 統一の教訓は通用するが、機構は PostgreSQL + goose + Querier モックへ読み替える。

| 項目 | 本番/開発 | テスト (`go test`) |
|------|-----------|-------------------|
| DB ポート | sqlc 生成 `repository.Querier`(pgx) | **原則 DB 非依存** — `Querier` の手書きモック |
| 実 DB が要る検証 | PostgreSQL 16 (Docker) / 開発DB | PostgreSQL 16 / **テスト専用DB**(接続文字列で分離) |
| 設定 | `.env` / `DATABASE_URL` | `TEST_DATABASE_URL`(or 環境変数で上書き) |

**Querier モック統一**: 本プロジェクトのテストは「本番と同じDBエンジンを使う」より一歩進めて、**そもそもDBを使わない**を既定にする。DB ポートが sqlc 生成の単一 interface `repository.Querier`(`emit_interface=true`) に集約されているため、ハンドラ/サービスのテストは Querier の手書きモック(`internal/handler/auth_test.go` の `fakeAuthRepo`、`sensor_api_test.go` の `fakeSensorRepo` 参照)で完結する。これにより SQL 互換性問題どころか「テストにDBが要らない」状態を作れる。

> Laravel では「テストDBを SQLite→MariaDB へ統一して SQL 互換問題を解消」した。→ Go では「DB ポートを interface 化し、ハンドラ層テストからDBを排除」することで同じ問題をそもそも発生させない。実 SQL の検証は実DBテスト(後述)に局所化する。

**実 DB が要るテストの分離(必須)**: sqlc が生成した実 SQL(`db/queries/*.sql`)の挙動・CHECK 制約・`ON CONFLICT`・トランザクションを検証する場合のみ、goose マイグレーション済みの PostgreSQL に接続する。**テスト用DBは本番/開発DBと接続文字列で必ず分離する**(誤って開発DBを TRUNCATE する事故を防ぐ)。テストの各ケースは「対象テーブルを TRUNCATE」または「tx を張って末尾で Rollback」して隔離する。

**初回セットアップ(実DBテスト)**:
```bash
# テスト専用DBを作成し goose で最新スキーマへ
createdb go_iot_test
go tool goose -dir db/migrations postgres "$TEST_DATABASE_URL" up
```
実DBテストは `TEST_DATABASE_URL` 等の専用環境変数が設定されたときだけ走らせ、未設定なら `t.Skip()` でスキップする(CI と手元で同じテストコードを共有しつつ、DB 無しの開発機でも `go test ./...` が緑になるようにする)。

> **指針**: 8 割超のテストは Querier モック(DB不要・高速)。実 SQL 由来の不変条件(CHECK 制約違反・`ON CONFLICT`・GROUP BY)だけ実DBテストに集める。比率を逆にしない。

---

## 2. SQL知見（PostgreSQL / sqlc / pgx）

> **分類: 主に ADAPT、一部 DROP** — 「集約と GROUP BY の整合」「ソフトデリート条件を JOIN に含める」という本質は PostgreSQL でも通用する。ただし Eloquent Builder / `paginate()` / トレイト固有の API は Go に対応物が無い。本プロジェクトは生 SQL を `db/queries/*.sql` に書き、sqlc がメソッドを生成する方式なので、**問題は「ランタイムのクエリ組み立てミス」ではなく「`sqlc generate` 時に検出される SQL の静的不備」に前倒しされる**点が最大の違い。

> **注記:** PostgreSQL は MariaDB の strict mode より一貫して厳格で、`ONLY_FULL_GROUP_BY` 相当の挙動が常時 ON(オフにできない)。さらに sqlc は `sqlc generate` の時点で SQL を解析して列名・型を確定するため、GROUP BY 不整合や存在しない列は**コード生成時にエラーになり、実行前に潰れる**。以下は sqlc/pgx でクエリを書く際の知見として読み替える。

### 2.1 JOIN + 集約クエリは「件数取得用クエリ」を別に持つ

Laravel の `paginate()` は内部で自動的に `COUNT(*)` クエリを派生させるが、本プロジェクトに自動ページネーションは無い。**一覧本体クエリと件数クエリを別の名前付きクエリとして明示的に書く**(`db/queries/sensor_readings.sql` の `ListSensorReadingsPaginated` と `CountSensorReadingsInRange` のペアが実例)。

```sql
-- name: ListSensorReadingsPaginated :many
SELECT * FROM sensor_readings
 WHERE device_id = $1 AND recorded_at BETWEEN $2 AND $3 AND deleted_at IS NULL
 ORDER BY recorded_at DESC
 LIMIT $4 OFFSET $5;

-- name: CountSensorReadingsInRange :one
SELECT COUNT(*)::BIGINT AS total
  FROM sensor_readings
 WHERE device_id = $1 AND recorded_at BETWEEN $2 AND $3 AND deleted_at IS NULL;
```

> Laravel では「`paginate()` の内部 COUNT が GROUP BY と衝突」する罠があった。→ Go(sqlc) では COUNT を自分で書くため衝突しないが、代わりに **本体クエリと件数クエリで WHERE 条件をズレなく同期させる責任**が開発者に移る。テストで「本体の件数」と「Count の戻り値」が一致することを実DBで担保する。

### 2.2 ソフトデリートは JOIN 条件・WHERE に `deleted_at IS NULL` を明示する

Eloquent の SoftDeletes グローバルスコープのような暗黙適用は sqlc には無い。**JOIN する全テーブルについて `deleted_at IS NULL` を手で書く**。論理削除済みの親(devices)・中間(alert_rules)を辿った行が混入すると、件数や一覧が狂う。

```sql
-- db/queries/alert_histories.sql の実例: 3テーブルすべてに deleted_at 条件
  FROM alert_histories AS ah
  JOIN alert_rules     AS ar ON ar.id = ah.alert_rule_id
  JOIN devices         AS d  ON d.id  = ar.device_id
 WHERE d.user_id      = $1
   AND ah.deleted_at  IS NULL
   AND ar.deleted_at  IS NULL   -- 中間テーブルの論理削除も除外
   AND d.deleted_at   IS NULL   -- 親テーブルの論理削除も除外
```

> Laravel では「LEFT JOIN 先の SoftDeletes が JOIN 条件に自動適用されない」罠だった。→ Go では**そもそも自動適用が一切無い**ため、毎クエリで明示するのが既定。索引も部分索引(`WHERE (deleted_at IS NULL)`、スナップショット参照)で張られているので、条件を書けば索引も効く。テストは「論理削除した行が一覧/件数に出ない」を実DBで検証する。

### 2.3 JOIN 時のカラム曖昧回避は「テーブル別名で完全修飾」

複数テーブルを JOIN すると `user_id` / `device_id` など同名カラムが曖昧になり、PostgreSQL は `column reference "..." is ambiguous` エラーを返す(sqlc generate 時に検出される)。**常にテーブル別名で完全修飾する**。

```sql
-- NG: どの user_id か曖昧
WHERE user_id = $1
-- OK: 別名で完全修飾 (alert_histories.sql の実例は d.user_id)
WHERE d.user_id = $1
```

> Laravel では `applyBranchFilter()` の `branchColumn` パラメータでテーブルを明示する話だった。→ Go では生 SQL を書くので、**最初から `d.user_id` のように別名修飾で書く**だけ。曖昧なまま書くと `sqlc generate` が失敗するため、実行前に必ず気づける(ランタイムまで潜伏しない)のが利点。

> 【Go では該当なし】`FiltersListData::getStatusCounts()` のような汎用フィルタトレイトを別テーブル起点で再利用したときの `addSelect` 罠(原本 §2.4)。**転用できる教訓**: 「`SELECT *` を上書きする `addSelect` 系 API」が無く、sqlc は SELECT 列を明示するため、`COUNT(filtered_items.id)` が拾えない種の事故は構造上起きない。集計には COUNT 用クエリ(§2.1)を独立に書く。

### 2.4 集約クエリは非集約 SELECT 列をすべて GROUP BY に列挙する

`SUM`/`AVG`/`COUNT` 等を使う集計クエリでは、SELECT に並べた非集約列をすべて GROUP BY 句にも列挙する。PostgreSQL は MariaDB strict mode より厳格で、**この規則を常時強制**する(設定でゆるめられない)。違反は `column "..." must appear in the GROUP BY clause` で、しかも sqlc generate 時に検出される。

```sql
-- db/queries/sensor_readings.sql の実例: GROUP BY DATE(recorded_at) と
-- SELECT の非集約列が一致している (avg/max/min/count はすべて集約関数)
SELECT
    DATE(recorded_at)               AS reading_date,
    AVG(temperature)::NUMERIC(5,2)  AS avg_temperature,
    MAX(temperature)                AS max_temperature,
    COUNT(*)::BIGINT                AS sample_count
  FROM sensor_readings
 WHERE device_id = $1 AND recorded_at >= $2 AND deleted_at IS NULL
 GROUP BY DATE(recorded_at)         -- 非集約 SELECT は DATE(recorded_at) のみ
 ORDER BY DATE(recorded_at) ASC;
```

**テストでの確認**:
GROUP BY 不整合は `sqlc generate`(コード生成)時点でエラーになるため、**`make sqlc` が通ること自体が最初のテスト**。さらに集計値の正しさ(平均・最大・最小)は実DBに既知データを INSERT して検証する。`AVG(...)::NUMERIC(5,2)` のように型を明示キャストしておくと、pgx 側で `pgtype.Numeric` として安定して受け取れる(`internal/infra/pgconv` で `domain` 型へ変換)。

**§2.1 との使い分け**:
- **集計不要の一覧** → `SELECT *` + ページング(§2.1 の `ListSensorReadingsPaginated`)
- **集計が必要**(`SUM`/`AVG`/`COUNT`) → 本節の GROUP BY 明示列挙(`ListDailySensorAggregates`)

**適用場面**: ダッシュボードの日別集計(7日/30日グラフ)、履歴画面のサマリボックス(`GetSensorReadingsSummary`)など。

#### 2.4.1 GROUP BY クエリの件数は「グループ行数を数えるクエリ」で取る

GROUP BY を含むクエリの「ヒット件数(=グループ数)」を取りたい場合、本体クエリに `COUNT(*)` を足すと**各グループの行数**が返ってしまい、グループ総数にならない。グループ数を取るには**サブクエリで包んで数える**専用クエリを書く。

```sql
-- name: CountDailyAggregateGroups :one
-- 日別集計の「日数 (= グループ数)」を取得
SELECT COUNT(*)::BIGINT AS total FROM (
    SELECT DATE(recorded_at)
      FROM sensor_readings
     WHERE device_id = $1 AND recorded_at >= $2 AND deleted_at IS NULL
     GROUP BY DATE(recorded_at)
) AS g;
```

> Laravel では「GROUP BY クエリに `->count()` が衝突するので `Paginator::total()`(内部サブクエリ)を使う」話だった。→ Go(sqlc) には自動ページネーションが無いので、**上記のようにサブクエリで包んだ Count クエリを自分で書く**。明細モード(GROUP BY 無し)の件数は §2.1 の素朴な `COUNT(*)` で足り、両モードを別クエリにしておくと意図が明快。テストは「集計行数」と「Count クエリの戻り値」が一致することを実DBで担保する。

---

## 3. Web ハンドラのレスポンスステータスコード

> **分類: 主に ADAPT、一部 DROP** — 「Web(HTML)と API(JSON)でステータスコードの扱いが違う」「バリデーション失敗時の返し方を意図的に設計する」という本質は完全に通用する。ただし Laravel 固有の「`response()->json()` が 200 に化ける」現象は Gin には存在しない。

### 3.1 Gin の `c.JSON(code, ...)` はステータスがそのまま返る（化けない）

Laravel の Web ルートで `response()->json([], 409)` が 200 に化ける現象は、**Gin には無い**。`c.JSON(http.StatusConflict, ...)` は素直に 409 を返す。

```go
// Gin: 指定したステータスがそのまま返る
c.JSON(http.StatusConflict, gin.H{"error": "conflict"})
// → w.Code == 409 (化けない)
```

ただし本質的な教訓は残る: **API(JSON)とWeb(HTML)でハンドラを分離する**。本プロジェクトは `/api/...` を JSON 専用(`c.JSON`)、Web ルートを templ 描画(HTML)として明確に分けている(`cmd/server/main_test.go` の `TestNewHTTPHandler_デバイスAPIは認証なしで401かつCSRF対象外` が API/Web 分離の証左)。

```go
// API ハンドラ (JSON 限定。internal/handler/sensor_api_test.go の検証対象)
func TestSensorAPI_Create_他ユーザーのデバイスは403かつ保存しない(t *testing.T) {
    repo := &fakeSensorRepo{device: repository.Device{ID: 10, UserID: 999}}
    w := postSensorData(t, newRouterWithUser(&SensorAPI{Repo: repo}, 7))
    if got, want := w.Code, http.StatusForbidden; got != want {
        t.Errorf("status: got %d, want %d", got, want)
    }
}
```

> Laravel では「`JsonResponse` を Web ルートで返すとミドルウェアが文字列化して 200 にラップする → `abort()` で例外に逃がす」必要があった。→ Go(Gin) では `c.JSON(code,...)` / `c.AbortWithStatusJSON(code,...)` がそのままステータスを返すため、この回避策は不要。代わりに「**API とWeb のハンドラを物理的に分ける**」という設計判断が重要。

### 3.2 ステータスコードの設計方針（構文エラー / バリデーション失敗 / 認証 / 認可）

API(JSON)のステータスは本プロジェクトで以下に標準化されている(`sensor_api_test.go` / `device_auth_test.go` の実テストが固定している)。

| 状況 | ステータス | 根拠テスト |
|------|-----------|-----------|
| JSON 構文エラー / 型ミスマッチ | **400** | `TestSensorAPI_Create_InvalidJSONSyntax`, `..._TypeMismatchReturns400` |
| binding バリデーション違反(範囲外・必須欠落) | **422** | `TestSensorAPI_Create_ValidationFailureReturns422` |
| 未認証(Bearer 無し/不正) | **401** | `TestDeviceAuth_MissingAuthorizationHeader`, `..._不正トークンは401` |
| 他人の資源(所有者不一致 = BOLA) | **403** | `TestSensorAPI_Create_他ユーザーのデバイスは403かつ保存しない` |
| 参照先が存在しない | **422 or 404** | `TestSensorAPI_Create_存在しないデバイスは422` |
| サーバ内部エラー(DB 障害等) | **500** | `..._デバイス参照のDBエラーは500`, `..._保存失敗は500` |

```go
// binding タグでバリデーション → 違反は 422 (sensor_api_test.go の table-driven 実例)
func TestSensorAPI_Create_ValidationFailureReturns422(t *testing.T) {
    cases := []struct {
        name string
        body string
    }{
        {"temperature below lower bound", `{"device_id":1,"temperature":-100,"humidity":50,"recorded_at":"2026-04-21T00:00:00Z"}`},
        {"humidity above 100",           `{"device_id":1,"temperature":20,"humidity":150,"recorded_at":"2026-04-21T00:00:00Z"}`},
        {"device_id missing",            `{"temperature":20,"humidity":50,"recorded_at":"2026-04-21T00:00:00Z"}`},
    }
    for _, tc := range cases {
        t.Run(tc.name, func(t *testing.T) {
            req := httptest.NewRequest(http.MethodPost, "/api/sensor-data", bytes.NewReader([]byte(tc.body)))
            req.Header.Set("Content-Type", "application/json")
            w := httptest.NewRecorder()
            r.ServeHTTP(w, req)
            if w.Code != http.StatusUnprocessableEntity {
                t.Errorf("status: got %d, want 422, body=%s", w.Code, w.Body.String())
            }
        })
    }
}
```

Web(HTML)のバリデーション失敗は **200 で HTML を再描画し、フィールドエラーを表示する**設計(Laravel の 302 redirect-back とは異なる)。エラーは「共有マジック」ではなく view struct のフィールドで運ぶ(`internal/handler/auth_validation.go` の `translateValidationErrors` が field→日本語 map を作り、それを view struct に詰めて templ へ渡す)。

```go
// Web ログイン失敗は 200 + 共通エラー文言 (auth_test.go の実例)
func TestLoginPost_パスワード不一致は200で共通エラー(t *testing.T) {
    w := postForm(app, "/login", url.Values{"email": {"user@example.com"}, "password": {"wrongpass"}})
    if w.Code != http.StatusOK {
        t.Fatalf("status = %d, want 200", w.Code)
    }
    if !strings.Contains(w.Body.String(), "メールアドレスまたはパスワードが間違っています") {
        t.Error("共通エラーメッセージが表示されていない")
    }
}
```

> Laravel では「手動 `Validator` + `response()->json(422)` が Web ルートで化ける → `$request->validate()`(JSON は 422 / HTML は 302)」が定石だった。→ Go では **API ハンドラ = `ShouldBindJSON` 失敗で 422 JSON、Web ハンドラ = `ShouldBind` 失敗で 200 + templ 再描画** とハンドラごとに明示実装する。フレームワークが「Accept ヘッダで自動分岐」してくれないぶん、ハンドラの分離で意図を表す。

#### 3.2.1 GET 検索画面で「HTML 200 + インラインエラー + 空一覧」を返すパターン

検索画面では「条件未指定/不正ならリダイレクトせず画面上にエラーを出し、一覧は空で返す」UX が要る。Go ではフレームワークの自動分岐に頼らず、**ハンドラ内で `HX-Request` / `Accept` を見て分岐**し、エラーを view struct のフィールドに詰めて templ を描画する。

```go
// internal/handler 想定: GET 検索画面 (年月必須)
type SummaryView struct {
    Errors   map[string]string // フィールド→日本語メッセージ (templ がこれを描画)
    Readings []ReadingRow      // 空一覧なら nil
    Filters  SummaryFilters
}

func (h *SummaryHandler) Index(c *gin.Context) {
    // 初期表示 (クエリパラメータなし) はバリデーションをスキップ (§3.2.3)
    if len(c.Request.URL.Query()) == 0 {
        render(c, http.StatusOK, page.Summary(SummaryView{}))
        return
    }

    var q SummaryQuery // `form:"report_year_month" binding:"required,..."`
    if err := c.ShouldBindQuery(&q); err != nil {
        errs := translateValidationErrors(err) // auth_validation.go と同方式

        // JSON 期待 (API/明示 Accept) は 422 JSON
        if wantsJSON(c) {
            c.JSON(http.StatusUnprocessableEntity, gin.H{"errors": errs})
            return
        }

        // HTML / HTMX: 302 にせず 200 + エラー入り view struct + 空一覧
        view := SummaryView{Errors: errs, Readings: nil, Filters: q.ToFilters()}
        if c.GetHeader("HX-Request") == "true" {
            // 部分テンプレ (一覧フラグメント) だけを返す
            render(c, http.StatusOK, page.SummaryList(view))
            return
        }
        render(c, http.StatusOK, page.Summary(view)) // フルページ
        return
    }
    // 通常フロー (検索・集計の実行)
}
```

**ポイント**:
- `c.ShouldBindQuery(&q)` を使う(GET 前提。POST ボディ用の `ShouldBind` ではなくクエリ専用)
- JSON/HTML の分岐は `Accept: application/json`(または明示の API ルート)で行う。**`HX-Request` 単独では HTML 側ブランチに入れる**(HTMX も HTML を受け取る)
- **セッションフラッシュは使わない** — エラーは `SummaryView.Errors` map に詰めて直接 templ へ渡す。templ には Laravel の `ViewErrorBag` のような共有機構が無いので、struct のフィールドが唯一の伝達経路
- HTMX のときは**フルページではなく部分テンプレ**(一覧フラグメント)を Render する。フラグメント側 templ にもエラー表示ブロックを含めておく(§3.2.2)

**選び方の目安**:

| シナリオ | 返却方式 | 参照 |
|----------|---------|------|
| CRUD フォーム(POST/PUT/DELETE、通常フォーム) | HTML: 200 + templ 再描画 / API: 422 JSON | §3.2 |
| HTMX モーダル・インラインフォーム | HTMX: 422 + 部分テンプレ / 非HTMX: 200 + フルページ | `HTMX実装ガイド(動的).md` §7 |
| **GET 検索画面(一覧系)** | **HTML: 200 + インラインエラー + 空一覧 / API: 422 JSON** | **本小節** |

**テストサイド**: HTML(フルページ)、HTMX フラグメント、初期表示の 3 ケースを別々に検証する(§3.2.2, §3.2.3)。

```go
// HTML (フルページ): 200 + 固定文言
req := httptest.NewRequest(http.MethodGet, "/summary?device_id=1", nil) // 年月欠落
// ... cookie 等を付与 ...
w := httptest.NewRecorder()
app.ServeHTTP(w, req)
if w.Code != http.StatusOK || !strings.Contains(w.Body.String(), "年月を指定してください") {
    t.Errorf("status=%d body に文言なし", w.Code)
}
```

#### 3.2.2 HTMX フラグメント経由のエラー表示は「部分テンプレにもエラーブロックを含める」— テストで担保する

> Laravel では `ShareErrorsFromSession` ミドルウェアと Blade anonymous component の shared 変数解決が絡み、`view()->share('errors', $bag)` を呼ばないとフラグメント内 component にエラーが届かない、という固有の罠があった。

**Go での読み替え**: templ にはセッション共有エラー(`ViewErrorBag`)の機構が無く、**部分テンプレに渡す view struct の `Errors` フィールドがすべて**。したがって罠は「shared 変数の解決失敗」ではなく、**「フルページ templ にはエラー表示ブロックがあるのに、HTMX 時に返す部分テンプレにエラー表示ブロックを入れ忘れる」** という構造的見落としに変わる。

```templ
// internal/view/page/Summary.templ
// フルページにもフラグメントにも同じエラーブロックを置く
templ errorBlock(errs map[string]string) {
    if len(errs) > 0 {
        <div class="error-messages" role="alert">
            for _, msg := range errs {
                <p>{ msg }</p>
            }
        </div>
    }
}

templ SummaryList(v SummaryView) { // ← HTMX が返す部分テンプレ
    @errorBlock(v.Errors)          // フラグメントにもエラーブロックを含める (必須)
    // ... 一覧 ...
}

templ Summary(v SummaryView) {    // ← フルページ
    @layout.App() {
        @SummaryList(v)            // フルページは部分テンプレを内包
    }
}
```

**テスト**: `HX-Request: true` を付けてフラグメント応答を取り、エラー文言が乗ることを検証する。

```go
func TestSummary_HTMXフラグメントで必須欠落時にエラー表示(t *testing.T) {
    req := httptest.NewRequest(http.MethodGet, "/summary?device_id=&report_year_month=", nil)
    req.Header.Set("HX-Request", "true")
    // ... 認証 cookie を付与 ...
    w := httptest.NewRecorder()
    app.ServeHTTP(w, req)

    if w.Code != http.StatusOK {
        t.Fatalf("status = %d, want 200", w.Code)
    }
    if !strings.Contains(w.Body.String(), "年月を指定してください") {
        t.Error("フラグメント内にエラー文言が乗っていない")
    }
}
```

**ポイント**:
- **`HX-Request: true` は必須**。非 HTMX(フルページ)だけだとフルページ側のエラーブロックで表示されてパスし、**部分テンプレ側のブロック入れ忘れに気づけない**
- 期待値はアプリ側で定義したカスタム日本語文言(`validationMessage()` の戻り値)を使う。validator のデフォルト英語メッセージ(`Key: 'Field' Error:Field validation for ...`)に依存しない
- フラグメントとフルページが同じ `errorBlock` コンポーネントを共有していれば、入れ忘れ自体を構造的に防げる(モック写経・独自クラス禁止の方針: `HTMX実装ガイド(動的).md` §31)

#### 3.2.3 初期表示（クエリパラメータなし）でエラーが出ないことをテストする

検索条件に `required` を課す画面では、**「クエリパラメータなしでアクセスしたときエラーが出ない」** ことも明示テストする。これが無いと「初期表示で required 違反が出る」UX 退行に気づけない。本プロジェクトでは `len(c.Request.URL.Query()) == 0` で初期表示を判定し、バリデーションをスキップする(§3.2.1 のハンドラ実装)。

```go
func TestSummary_初期表示はバリデーションエラーを出さない(t *testing.T) {
    req := httptest.NewRequest(http.MethodGet, "/summary", nil) // クエリなし
    // ... 認証 cookie を付与 ...
    w := httptest.NewRecorder()
    app.ServeHTTP(w, req)

    if w.Code != http.StatusOK {
        t.Fatalf("status = %d, want 200", w.Code)
    }
    if strings.Contains(w.Body.String(), "年月を指定してください") {
        t.Error("初期表示でバリデーションエラーが表示されている")
    }
}
```

**組み合わせて書くべきテストケース3種**(検索画面テンプレート):

| ケース | 期待 | テストメソッド例 |
|---|---|---|
| クエリパラメータなし(初期表示) | エラー非表示、空一覧 | `TestSummary_初期表示はバリデーションエラーを出さない` |
| クエリあり・必須欠落 | HTML 200 + インラインエラー | `TestSummary_必須欠落で200エラー表示` |
| HTMX フラグメント + 必須欠落 | フラグメント内にエラー表示 | `TestSummary_HTMXフラグメントで必須欠落時にエラー表示` |

### 3.3 テストでの JSON / HTML / HTMX リクエストの使い分け

Go では「JSON か HTML か」をテスト側でヘッダ操作して明示する。`getJson()` のような専用ヘルパは無いので、`req.Header.Set` で組み立てる(`auth_test.go` の `postForm`、`sensor_api_test.go` の `postSensorData` が雛形)。

```go
// HTML フォーム POST (application/x-www-form-urlencoded)
req := httptest.NewRequest(http.MethodPost, "/login", strings.NewReader(vals.Encode()))
req.Header.Set("Content-Type", "application/x-www-form-urlencoded")

// JSON API POST → 422 を期待
req := httptest.NewRequest(http.MethodPost, "/api/sensor-data", strings.NewReader(body))
req.Header.Set("Content-Type", "application/json")

// HTMX フラグメント期待
req.Header.Set("HX-Request", "true")

// JSON エラーを GET で期待するなら Accept を明示
req.Header.Set("Accept", "application/json")
```

**HTMX 経由の PUT/DELETE は `_method` フィールドで上書きする**(本プロジェクトは `MethodOverride` ミドルウェアを採用。`internal/middleware/method_override_test.go` 参照)。HTML フォームは PUT/DELETE を直接送れないため、`POST` + `_method=DELETE` を送って上書きする方式。

```go
// MethodOverride の実テスト: POST + _method=DELETE → r.Method == DELETE
{"DELETE に上書き", "_method=DELETE", http.MethodDelete},
{"GET への上書きは許可せず POST のまま", "_method=GET", http.MethodPost}, // GET 昇格は禁止
```

> Laravel では「`putJson()` で `Accept: application/json` が自動付与され 422 が返る / `HX-Request` だけでは 302」だった。→ Go では **(1) API は `/api/...` ルートを叩けば常に JSON で 422、(2) Web の HTMX PUT/DELETE は `_method` 上書き + `HX-Request` ヘッダ** で表現する。GET で API エラーを得たいなら `Accept: application/json` を手で付ける(Gin は Accept で自動 302 化しないので、むしろハンドラ側の分岐ロジックを `wantsJSON(c)` で明示する)。

#### 3.3.1 GET でバリデーションエラーの分岐をテストする

GET 検索画面のバリデーション分岐(§3.2.1)は、`Accept: application/json` の有無でテストを 2 本書く。Gin はフレームワーク既定で 302 へ化けさせないので、**分岐ロジックはハンドラの `wantsJSON(c)` 実装が責任を持つ**。そのためテストは「ハンドラの分岐が正しいか」を直接突く。

```go
// API 期待 (Accept: application/json) → 422 JSON
func TestSummary_GET必須欠落でJSON期待なら422(t *testing.T) {
    req := httptest.NewRequest(http.MethodGet, "/summary?device_id=abc", nil)
    req.Header.Set("Accept", "application/json")
    // ... 認証 cookie ...
    w := httptest.NewRecorder()
    app.ServeHTTP(w, req)
    if w.Code != http.StatusUnprocessableEntity {
        t.Fatalf("status = %d, want 422", w.Code)
    }
    if !strings.Contains(w.Body.String(), `"errors"`) {
        t.Error("JSON エラーペイロードが無い")
    }
}
```

**ポイント**:
- GET の `ShouldBindQuery` も、ハンドラの `wantsJSON(c)` 分岐で 422 JSON / 200 HTML を切り替える(自動ではない)
- エラー対象フィールド名(`device_id` 等)を JSON に含め、テストでフィールド名をアサートすると、将来 binding ルール追加時の意図しない回帰を検出できる
- HTMX も `HX-Request: true` だけでは HTML 側(200)に入る。JSON 422 が要るなら `Accept: application/json` を併記する

### 3.4 テスト実行コマンド

> Laravel では Sail 停止中に `docker exec app_kdcs php artisan test` を使う、という Docker 固有の話だった。

**Go での読み替え**: 本プロジェクトのハンドラ層テストは DB を要さない(Querier モック)ため、**ホストで `go test ./...` を直接走らせるのが基本**。Docker(PostgreSQL)は実DBテストのときだけ起動すればよい。

```bash
# 全テスト (DB 不要のものはこれだけで緑になる)
go test ./...

# 特定パッケージ・特定テストに絞る
go test ./internal/handler/ -run TestSensorAPI_Create -v

# カバレッジ (目標 80%+)
go test ./... -cover
go test ./... -coverprofile=cover.out && go tool cover -func=cover.out

# 実DBテストを含める場合のみ Docker を起動し環境変数を渡す
make up && go tool goose -dir db/migrations postgres "$TEST_DATABASE_URL" up
TEST_DATABASE_URL="postgres://...test" go test ./internal/repository/... -v
```

> **指針**: `go test` はビルドキャッシュが効くので Laravel の `--filter` ほど絞り込みに神経質にならなくてよい。DB を要すテストだけ環境変数ガードで分離し、未設定なら `t.Skip()`。

### 3.5 ストリーミング応答（CSV 等）のテスト

> Laravel では `StreamedResponse` を `$response->streamedContent()` で受ける専用メソッドが要った。

**Go での読み替え**: Gin の `c.Data` / `c.Stream` / `io.Copy(c.Writer, ...)` で書いた出力は、テストでは **`httptest.ResponseRecorder` の `w.Body` にそのまま蓄積される**(専用メソッド不要)。CSV は `encoding/csv` で生成する。検証はヘッダ(`Content-Type` / `Content-Disposition`)と本文(BOM・列・行)を見る。

```go
func TestExportCSV_ヘッダと本文を検証(t *testing.T) {
    // ... 認証 cookie 等を付与した GET /readings/export ...
    w := httptest.NewRecorder()
    app.ServeHTTP(w, req)

    if w.Code != http.StatusOK {
        t.Fatalf("status = %d, want 200", w.Code)
    }
    if ct := w.Header().Get("Content-Type"); !strings.Contains(ct, "text/csv") {
        t.Errorf("Content-Type = %q, want text/csv を含む", ct)
    }
    if cd := w.Header().Get("Content-Disposition"); !strings.Contains(cd, "attachment") {
        t.Errorf("Content-Disposition = %q, want attachment", cd)
    }

    body := w.Body.String()
    // Excel 文字化け対策の UTF-8 BOM
    if !strings.HasPrefix(body, "\xEF\xBB\xBF") {
        t.Error("先頭に UTF-8 BOM が無い")
    }
    // ヘッダ行 (列名)
    if !strings.Contains(body, "計測日時,温度,湿度") {
        t.Error("CSV ヘッダ行が出力されていない")
    }
}
```

> `streamedContent()` のような分岐は不要。ただし大きなストリームを `c.Stream` で逐次書き出すハンドラでも、テストでは `ResponseRecorder` が全量バッファするので、本文アサートは同じ書き方で通る。XLSX を出すなら `xuri/excelize` で生成し、テストでは固定の `testdata/*.xlsx` と突き合わせるか、生成バイト列を excelize で開き直してセル値を検証する。

### 3.6 ストリーミング応答エンドポイントの認証テスト

> Laravel では「`StreamedResponse` を返すと未認証時に 302 ではなく 401 が返る場合があり、両方許容が必要」という戻り値型ヒント由来の罠があった。

**Go での読み替え**: 本プロジェクトは**未認証時の挙動が文脈ごとに決まっている**(`Accept` や戻り値型に依存して揺れない)ので、エンドポイント種別ごとに 1 つのステータスを固定アサートできる。

| エンドポイント種別 | 未認証時 | 根拠 |
|---|---|---|
| Web(HTML/CSV ダウンロード、`RequireAuth` 配下) | **302 → /login** | `require_auth_test.go` の `TestRequireAuth_未認証は302でloginへ` |
| API(`/api/...`、DeviceAuth 配下) | **401** | `device_auth_test.go` の `TestDeviceAuth_MissingAuthorizationHeader` |

```go
// Web の CSV エクスポートは RequireAuth 配下 → 未認証は 302 (302 のみを固定アサート可)
func TestExportCSV_未認証は302でloginへ(t *testing.T) {
    app := newAuthApp(&fakeAuthRepo{}) // RequireAuth でガードされた構成
    w := getWithCookies(app, "/readings/export", nil)
    if w.Code != http.StatusFound || w.Header().Get("Location") != "/login" {
        t.Errorf("未認証 export = %d Location=%q, want 302 /login", w.Code, w.Header().Get("Location"))
    }
}
```

> Laravel では「302 と 401 の両方を許容」する曖昧さを受け入れる必要があったが、Go では **Web ルート(`RequireAuth`)=必ず 302、API ルート(`DeviceAuth`)=必ず 401** と分かれているため、テストは片方を `t.Fatalf` で固定できる。「両方許容」で逃げないこと自体が、ルーティング設計が正しく分離している証拠になる。

### 3.7 リポジトリ外ファイルの検証と「環境依存のスキップ」

> Laravel では「`backend/` 外(`local_mock_html/`)が Docker コンテナにマウントされず、コンテナ内テストからアクセスできない → `markTestSkipped`」という Docker 固有の罠だった。

**Go での読み替え**: 本プロジェクトの CSS は **`go:embed` で `internal/view/public/css/` をバイナリに同梱**し、`MountStatic` が `/static` で配信する(`internal/view/static_test.go` 参照)。CSS の正本は `mocks/html/style.css` で、`make sync-css` が **一方向**コピーする(本番側は生成物・手編集禁止・gitignore: CSS 単一ソース運用)。したがって「コンテナ内からモックが見えない」種の問題は構造的に起きにくいが、**「正本(mocks)と同梱物(public)の同期」を不変条件としてテストしたい**場合は、`go test` がリポジトリルートで走る前提で両ファイルを直接比較できる。

```go
// CSS 単一ソースの同期検証 (リポジトリルートで go test する前提)
func TestCSS単一ソースが同期している(t *testing.T) {
    const (
        srcPath = "../../../mocks/html/style.css"            // 正本
        dstPath = "../public/css/style.css"                  // make sync-css の生成物
    )
    src, err := os.ReadFile(srcPath)
    if err != nil {
        // モックが存在しない実行環境 (配布バイナリのテスト等) では検証不能なのでスキップ。
        // 同期は make sync-css / CI で担保する前提。
        t.Skipf("正本 %s が見つからないためスキップ (CI/make sync-css で同期担保): %v", srcPath, err)
    }
    dst, err := os.ReadFile(dstPath)
    if err != nil {
        t.Fatalf("同梱 CSS %s が読めない: %v", dstPath, err)
    }
    if !bytes.Equal(src, dst) {
        t.Errorf("mocks/html/style.css と internal/view/public/css/style.css が不一致 (make sync-css を実行)")
    }
}
```

**指針**:
- `os.ReadFile` の失敗(正本が無い環境)では `t.Fatal` ではなく **`t.Skipf` でスキップ**し、理由に「どこで同期を担保するか」(`make sync-css`、CI)を明記する
- 比較は `bytes.Equal`(bytewise)。CSS は一方向同期なので「双方向に編集された差分」ではなく「`make sync-css` 忘れ」を検出する位置づけ
- `go:embed` 同梱物自体の配信は別テスト(`static_test.go` の `TestMountStaticServesCSS`)で 200/Content-Type を担保済み。本テストは「同梱物の中身が正本と一致するか」に責務を絞る

**対象になりやすい検証**:
- モック HTML(`mocks/html/`)と templ 実装の HTML 構造比較(templ をモックの写経とする方針: `HTMX実装ガイド(動的).md` §31)
- CSS の bytewise 同期(`mocks/html/style.css` ↔ `internal/view/public/css/style.css`)
- モック JS と実装 JS(Alpine.js 初期化)の整合性

---

## 4. templ テンプレートのテスト

templ は `Component.Render(ctx, w)` で `io.Writer` に直接 HTML を書き出す。テストの基本形は本プロジェクト共通の 2 ヘルパーに集約する（`internal/view/*/_test.go` に実在）。

```go
func render(t *testing.T, c templ.Component) string {
	t.Helper()
	var buf bytes.Buffer
	if err := c.Render(context.Background(), &buf); err != nil {
		t.Fatalf("render: %v", err)
	}
	return buf.String()
}

func assertContains(t *testing.T, html, substr string) {
	t.Helper()
	if !strings.Contains(html, substr) {
		t.Errorf("出力に %q が含まれていない:\n%s", substr, html)
	}
}
```

Handler 経由（HTMX 分岐・ステータスを含む）の検証は `httptest.NewRecorder()` + `gin.New()` でレスポンス本文 (`w.Body.String()`) を `strings.Contains` する。Laravel の `assertSee`/`assertDontSee` は `strings.Contains`/`!strings.Contains` に一対一で対応する。

### 4.1 部分コンポーネント返却時の検証は「HTML 文字列アサーション」に統一する

> 【ADAPT】Laravel: `fragmentIf()` を使うと「レスポンスが View でなくなり」`assertViewIs`/`assertViewHas`/`viewData()` が `The response is not a view.` で全滅した → Go: そもそも View 変数を直接 assert する仕組み（`assertViewHas`）が存在しない。

Go の templ は Handler が `Component.Render(c.Request.Context(), c.Writer)` で `http.ResponseWriter` に直接 HTML を書き込む。「サーバー側で組み立てた変数」をフレームワーク経由で取り出す Laravel の `assertViewHas()` / `viewData()` に相当する仕組みは無い。したがって**フルページ返却か部分コンポーネント返却かに関わらず、検証は常にレスポンス HTML への文字列アサーションで行う**（`fragmentIf` のような「使うと検証手段が変わる」罠自体が存在しない）。

本プロジェクトは「フルページ templ がサブコンポーネントを呼び出し、HTMX 時は Handler がサブコンポーネントを直接 Render する」設計（HTMX実装ガイド §1）。Handler は末尾で `c.GetHeader("HX-Request")` を判定し、HTMX なら部分コンポーネント、そうでなければフルページを `Render` する。テストはその両分岐を本文で見分ける。

```go
// 非 HTMX: フルページ（レイアウト込み） → <html> を含む
req := httptest.NewRequest(http.MethodGet, "/devices/1/readings", nil)
w := httptest.NewRecorder()
router.ServeHTTP(w, req)
body := w.Body.String()
assertContains(t, body, "<html")  // フルページ
assertContains(t, body, "計測履歴")  // ページ見出し

// HTMX: 部分コンポーネントのみ（レイアウト無し） → <html> を含まない
req = httptest.NewRequest(http.MethodGet, "/devices/1/readings", nil)
req.Header.Set("HX-Request", "true")
w = httptest.NewRecorder()
router.ServeHTTP(w, req)
body = w.Body.String()
if strings.Contains(body, "<html") {
	t.Error("HTMX 時にレイアウト（<html>）が混入している")
}
assertContains(t, body, `id="device-readings-list"`)  // 部分コンポーネントのコンテナ
```

**指針**: `perPage` / `totalCount` / 対象デバイス名など「サーバー側で計算・組み立てた値」を検証したいときは、レンダリング結果 HTML に対する `assertContains` で**間接的に**確認する。templ 単体テスト（`render(t, Comp(...))`）で View 構造体から直接描画して構造だけ確認できる画面は、それで足りる。

#### 4.1.1 `assertViewHas('perPage', 20)` 相当の検証を行う代替パターン

> 【ADAPT】Laravel: `assertViewHas('perPage', 20)` でスカラー変数を直接検証していた → Go: View 変数の直接 assert は無いため、UI に表れる副作用か View 構造体の直接アサートで代替する。

スカラー値（表示件数・総件数・ソートパラメータ・フィルタ復元値）が正しく反映されているかは、次の 2 系統で検証する。

**系統 A: View 構造体を直接アサート（templ 単体テスト・最も直接的）**

Handler が組み立てる View 構造体（`page.LoginView` や一覧の `ReadingsView` 等）をテストで直接組み立て、フィールドをそのまま比較できる。これは Laravel の `assertViewHas` に最も近い。

```go
// Handler が組み立てた View 構造体をそのままアサート（Handler を直接呼ぶ単体テスト）
got := h.buildReadingsView(filters)  // Handler 内のビュー組み立てを切り出した関数
if got.PerPage != 20 {
	t.Errorf("PerPage = %d, want 20", got.PerPage)
}
if got.TotalCount != 25 {
	t.Errorf("TotalCount = %d, want 25", got.TotalCount)
}
```

**系統 B: UI に表れる副作用で検証（Handler 経由・E2E に近い）**

ビュー組み立てを切り出していない／レンダリング後の HTML 反映まで確認したい場合は、可視的な副作用でアサートする。

```go
// パターン 1: ページネーションリンクの有無で件数系を検証
// per_page=20 で 25 件 → 2 ページ目リンクが出現する
seedReadings(t, db, deviceID, 25)
w := getReadings(t, router, "/devices/1/readings?per_page=20")
assertContains(t, w.Body.String(), "page=2")

// パターン 2: <select> の selected 属性で表示件数を検証
w = getReadings(t, router, "/devices/1/readings?per_page=50")
assertContains(t, w.Body.String(), `<option value="50" selected`)

// パターン 3: ページネーション URL に検索条件が保持されることを検証
w = getReadings(t, router, "/devices/1/readings?period=7d&per_page=1")
assertContains(t, w.Body.String(), "period=7d")
```

**選び方の目安**:

- **件数系（`PerPage` / `TotalCount`）**: View 構造体直接アサート（系統 A）が最も明快。HTML 反映まで見るならページネーション・件数表示の可視的結果（系統 B）
- **ソート状態（`SortParam` / `OrderParam`）**: ソートヘッダーのリンク URL（`?sort=recorded_at&order=desc`）や `aria-sort` 属性の切り替えで検証
- **フィルタ復元（`Filters`）**: `<option ... selected>` / `<input value="...">` / checkbox の `checked` 属性で検証

### 4.2 表示テキスト・単位記号の検索

> 【ADAPT】Laravel: Blade の `&yen;` 等 HTML エンティティが `assertSee` でそのまま検索できず数値部分のみで検索した → Go: templ は文字列をそのまま UTF-8 で出力するためエンティティ問題は基本起きないが、フォーマット後の文字列で検索する原則は同じ。

templ は `{ fmt.Sprintf("%.1f℃", r.Temperature) }` のように Go の文字列をそのまま UTF-8 で出力する（`℃` `¥` `%` などはエンティティ化されない）。したがって**レンダリング後に画面へ実際に出る文字列**で検索する。テキスト式中の `<` `>` `&` のみ templ が自動エスケープする点に注意し、記号付きの値はフォーマット結果に合わせる。

```go
// templ: <td>{ fmt.Sprintf("%.1f℃", r.Temperature) }</td>  → "23.5℃"
assertContains(t, html, "23.5℃")

// 金額のように桁区切りを伴う値は数値部分だけで検索しても安全側
assertContains(t, html, "1,234,567")

// "<" を含む比較演算子（domain.ComparisonOperator）は templ が &lt; にエスケープする
// → 表示文言で検索する場合はエスケープ後の形に合わせる
assertContains(t, html, "温度 &gt; 30℃")  // templ が ">" を "&gt;" に変換して出力
```

> **補足（演算子表示の落とし穴）**: アラートルールの比較演算子（`>` `<` `>=` `<=`、`internal/domain/comparison_operator.go`）を templ のテキスト式に直接埋めると `&gt;` `&lt;` にエスケープされる。テストはエスケープ後の文字列で検索するか、記号を使わない表示ラベル（例: `domain.ComparisonOperator.Label()` が返す「より大きい」等）で検証する。

### 4.3 ソート順のテスト

> 【PORT】文字列の出現位置で順序を検証する手法はそのまま通用。`strpos` → `strings.Index`。

HTML 内での文字列出現位置を比較して並び順を検証する。

```go
func TestReadingsList_新しい順に並ぶ(t *testing.T) {
	rows := []page.ReadingRow{
		{RecordedAt: "2026-04-21 10:00", Label: "計測_UNIQUE_A"},
		{RecordedAt: "2026-04-20 10:00", Label: "計測_UNIQUE_B"},
	}
	html := render(t, page.DeviceReadingsList(rows, page.ReadingFilters{}))

	posA := strings.Index(html, "計測_UNIQUE_A")
	posB := strings.Index(html, "計測_UNIQUE_B")
	if posA == -1 || posB == -1 {
		t.Fatalf("検証対象が出力に無い: posA=%d posB=%d", posA, posB)
	}
	if posA >= posB {
		t.Errorf("A が B より先に表示されること: posA=%d, posB=%d", posA, posB)
	}
}
```

**注意**: 比較対象の文字列は HTML 内で一意であること。同じ文字列が別の場所（検索フォームの `value` 属性、`data-*` 属性等）にも出ると `strings.Index` が意図しない位置（最初の一致）を返す。テストデータには `"計測_UNIQUE_A"` のように衝突しない値を使う。Laravel の `assertSeeInOrder([...])` に相当する共通ヘルパーが欲しければ、複数 substr の出現位置が単調増加することを検証する `assertContainsInOrder(t, html, ...)` を `_test.go` 内に切り出すとよい。

### 4.4 templ コンポーネントの条件分岐に期待値を合わせる

> 【PORT】コンポーネントが条件で出力を変える場合、期待値を実装の条件に合わせる教訓はそのまま通用。`@if` → templ の `if`。

templ コンポーネントが `if` で出力を切り替える場合、テストの期待値をコンポーネントの分岐条件に正確に合わせる。下は件数表示コンポーネントの例。

```templ
// internal/view/component/hit_count.templ
templ HitCount(total, filtered int) {
	if filtered != total {
		<span class="hit-count">{ fmt.Sprintf("%d件中%d件ヒット", total, filtered) }</span>
	} else {
		<span class="hit-count">{ fmt.Sprintf("%d件", total) }</span>
	}
}
```

```go
// NG: フィルタなし（total == filtered）では "25件中20件ヒット" は描画されない
html := render(t, component.HitCount(25, 25))
assertContains(t, html, "25件中20件ヒット")  // 失敗（実際は "25件" のみ）

// OK: 分岐条件に合わせた期待値
html = render(t, component.HitCount(25, 25))
assertContains(t, html, "25件")            // フィルタなし
html = render(t, component.HitCount(25, 20))
assertContains(t, html, "25件中20件ヒット")  // フィルタあり
```

> **テスト容易性の利点**: templ は部分コンポーネントを `render(t, Comp(...))` で**単独描画**できるため、Blade の `$this->blade('<x-... />')` 同様、条件分岐をコンポーネント単位で網羅できる。`total`/`filtered` を table-driven で回すと境界（`filtered==0`、`filtered==total`）まで一括検証できる。

### 4.5 部分コンポーネント返却テスト（HTMX vs フルページ）

> 【ADAPT】Laravel: 同一エンドポイントが HTMX 時 Fragment／通常時フルページを返すのを `<html` タグの有無で判別し、`assertSee('<html', false)`（第2引数 false でエスケープ無効化）が必要だった → Go: `strings.Contains` はそもそもエスケープしないので第2引数は不要。判別ロジックは同一。

Handler が `c.GetHeader("HX-Request")` で返却を切り替える設計（HTMX実装ガイド §1）では、レスポンス本文に `<html` が含まれるかで「フルページ／部分コンポーネント」を確実に判別できる。

```go
func TestReadings_HTMX時は部分_通常時はフルページ(t *testing.T) {
	router := newReadingsRouter(t)

	// HTMX: 部分コンポーネントのみ（<html> なし）
	req := httptest.NewRequest(http.MethodGet, "/devices/1/readings", nil)
	req.Header.Set("HX-Request", "true")
	hw := httptest.NewRecorder()
	router.ServeHTTP(hw, req)
	if strings.Contains(hw.Body.String(), "<html") {
		t.Error("HTMX 時にフルページ（<html>）が返っている")
	}
	assertContains(t, hw.Body.String(), `id="device-readings-list"`)

	// 通常: フルページ（<html> あり）
	fw := httptest.NewRecorder()
	router.ServeHTTP(fw, httptest.NewRequest(http.MethodGet, "/devices/1/readings", nil))
	assertContains(t, fw.Body.String(), "<html")
	assertContains(t, fw.Body.String(), "計測履歴")
}
```

> **注意**: `strings.Contains(body, "<html")` で含む／含まないの両方をペアで検証すると、`HX-Request` 分岐ミス（HTMX なのにフルページを返す／その逆）を確実に捕捉できる。HTMX実装ガイド §1（共通コンポーネント使用時の `target` 必須ルール）の入れ子表示バグはテキスト存在だけでは検出できないため、`!strings.Contains(body, "<html")` を**HTMX 分岐の固定アサート**として必ず置く。

※ `hx-target` の指定ミスで返却範囲がずれるバグについては HTMX実装ガイド §1「hx-target とコンポーネント返却範囲の整合性」参照。テキスト存在だけ見る `assertContains` では検出できず、二重ヘッダー（`strings.Count(body, "site-header") >= 2`）の検出が必要。

### 4.6 複数コンポーネント同時返却テスト（OOB Swap）

> 【ADAPT】Laravel: `respondWithFragments()` で複数 Fragment を結合返却し、各 Fragment 内容を `assertSee` で検証 → Go: 同一 `c.Writer` に複数コンポーネントを順に `Render` する（メイン + `hx-swap-oob="true"` 付き OOB コンポーネント）。検証観点は同一。

templ は同一 `Writer` へ複数コンポーネントを順番に書き込めるため、HTMX 時にメインコンポーネントの直後へ OOB コンポーネントを `Render` するだけで複数領域の同時更新が成立する（HTMX実装ガイド §1, §5）。テストは「両方の中身が本文に含まれ、かつフルページではない」ことを検証する。

```go
// Handler: HTMX 時に 一覧 + サマリー(OOB) を同時返却
func (h *ReadingHandler) Index(c *gin.Context) {
	device, rows, filters, summary := h.load(c)
	if c.GetHeader("HX-Request") != "" {
		_ = page.DeviceReadingsList(rows, filters).Render(c.Request.Context(), c.Writer)
		_ = component.ReadingSummaryOOB(summary).Render(c.Request.Context(), c.Writer) // hx-swap-oob="true"
		return
	}
	_ = page.DeviceReadingsPage(device, rows, filters, summary).Render(c.Request.Context(), c.Writer)
}
```

```go
func TestReadings_HTMX時は一覧とサマリーを同時返却(t *testing.T) {
	router := newReadingsRouter(t)
	req := httptest.NewRequest(http.MethodGet, "/devices/1/readings", nil)
	req.Header.Set("HX-Request", "true")
	w := httptest.NewRecorder()
	router.ServeHTTP(w, req)
	if w.Code != http.StatusOK {
		t.Fatalf("status: got %d, want 200", w.Code)
	}
	body := w.Body.String()
	assertContains(t, body, `id="device-readings-list"`) // メインコンポーネント
	assertContains(t, body, `hx-swap-oob="true"`)        // OOB 属性
	assertContains(t, body, `id="reading-summary"`)      // OOB コンポーネントのコンテナ
	if strings.Contains(body, "<html") {
		t.Error("HTMX 時にフルページが返っている")
	}
}
```

**注意**:
- OOB コンポーネントはルート要素に `id` と `hx-swap-oob="true"` を持つ（HTMX実装ガイド §1「配置パターン 2」）。**この `hx-swap-oob="true"` の存在自体**を OOB 返却の固定アサートにする
- 各コンポーネントの中身（コンテナ id・代表的な見出し）が本文に含まれることを個別に検証する
- 通常リクエスト時はフルページが返るため、`strings.Contains(body, "<html")` で判別する

### 4.7 文字列アサーションの堅牢化（属性順序・記号衝突）

> 【ADAPT】Laravel: `assertMatchesRegularExpression()` で Blade 出力を正規表現検証し、(1) 区切り文字 `#` が `href="#"` と衝突、(2) Alpine 追加で `<td>` の属性順序が変わり固定位置 regex が壊れた → Go: 検証の第一選択は `strings.Contains`（正規表現不要）。regex を使う場合のみ `regexp.MustCompile` で同じ堅牢化が要る。

本プロジェクトのテストは原則 `strings.Contains`（`assertContains` ヘルパー）で検証するため、Laravel で問題になった「正規表現の区切り文字衝突」は通常発生しない。ただし `<td>` の属性順序のような構造を厳密に確認したい場合に regex を使うなら、以下を守る。

#### 4.7.1 区切り文字の概念は不要（Go の regexp はバッククォート raw string）

> 【DROP】Laravel: PHP の `preg_*` はパターンを区切り文字（`/.../`, `~...~`, `#...#`）で囲む仕様のため `#` が `href="#"` と衝突した。
> 【Go では該当なし】Go の `regexp` はパターンを区切り文字で囲まない（`regexp.MustCompile(\`...\`)`）。`#` 衝突問題は構造的に起きない。転用できる教訓: HTML を含むパターンは raw string リテラル（バッククォート）で書き、`"` のエスケープ地獄を避ける。

```go
// Go: 区切り文字なし。raw string で " をそのまま書ける
re := regexp.MustCompile(`<a[^>]+href="#"[^>]+aria-disabled="true"`)
if !re.MatchString(body) {
	t.Error("無効化されたアンカーが見つからない")
}
```

#### 4.7.2 属性順序に依存する regex は書かない

> 【PORT】属性順序非依存で書く教訓はそのまま通用。templ も `x-show`/`hx-*`/`x-model` を後付けすると属性順序が変わりうる。

既存の `<td class="reading-value">` に Alpine の `x-show` を後から追加すると、templ の出力で属性順序が変わることがある（`<td x-show="..." class="reading-value">`）。固定位置でマッチさせた regex は壊れる。

```go
// NG: class が <td> 直後にある前提
re := regexp.MustCompile(`<td class="reading-value"[^>]*>[^<]*23\.5`)

// OK: 属性順序非依存（属性群のどこに class があってもよい）
re := regexp.MustCompile(`<td[^>]*class="[^"]*reading-value[^"]*"[^>]*>[^<]*23\.5`)
if !re.MatchString(body) {
	t.Error("温度セルの値が見つからない")
}
```

**ガイドライン**:
- そもそも順序非依存な部分文字列（`assertContains(t, html, "reading-value")` と `assertContains(t, html, "23.5℃")` を別個に）で済むなら regex を使わない
- regex が必要なら、タグ開始 `<td` の直後に必ず `[^>]*` を挟み任意の属性を許容する
- 特定クラス一致は `class="[^"]*{target-class}[^"]*"` のサブストリング形で書く
- `x-show` / `x-model` / `hx-*`（Alpine/HTMX 属性）は後から追加されやすいため、属性順序テストは最初から柔軟に書く

### 4.8 計算値検証のための `data-*` 属性活用

> 【ADAPT】Laravel: `fragmentIf()` 下では `viewData()` が使えず、行単位の計算結果を `data-*` 属性 + `assertSee('data-X="Y"', false)` で exact-match 検証した → Go: View 構造体の直接アサート（§4.1.1 系統 A）が第一選択。HTML 反映まで確認したい行単位の計算値は同じく `data-*` + `assertContains` が有効。

集計・計算結果（比率、残高、損益など）を**行単位で複数値まとめて**検証したい場合、UI の可視的副作用だけでは不足する。テーブル行に `data-*` 属性を仕込み、`assertContains` で属性ごと exact-match 検証する。`data-*` は HTML 仕様上「任意のカスタム属性」で UI に影響せず、ブラウザデバッグ（MCP chrome-devtools）や将来の E2E（chromedp）でも活用できる。

```templ
// internal/view/page/readings_list.templ
// 行ごとに検証可能な data-* を付与（DB 実カラム: temperature / humidity / recorded_at）
templ DeviceReadingsList(rows []ReadingRow, filters ReadingFilters) {
	<table class="data-table">
		<tbody>
			for _, r := range rows {
				<tr
					data-recorded-at={ r.RecordedAt }
					data-temperature={ fmt.Sprintf("%.2f", r.Temperature) }
					data-humidity={ fmt.Sprintf("%.2f", r.Humidity) }
				>
					<td>{ r.RecordedAt }</td>
					<td class="reading-value">{ fmt.Sprintf("%.1f℃", r.Temperature) }</td>
					<td class="reading-value">{ fmt.Sprintf("%.1f%%", r.Humidity) }</td>
				</tr>
			}
		</tbody>
	</table>
}
```

```go
// テスト: 計算値（numeric(5,2) のフォーマット結果）を data 属性から検証
func TestReadingsList_計算値がdata属性に反映(t *testing.T) {
	rows := []page.ReadingRow{
		{RecordedAt: "2026-04-21 10:00", Temperature: 23.5, Humidity: 60.0},
	}
	html := render(t, page.DeviceReadingsList(rows, page.ReadingFilters{}))

	assertContains(t, html, `data-temperature="23.50"`) // numeric(5,2) フォーマット
	assertContains(t, html, `data-humidity="60.00"`)
	assertContains(t, html, `data-recorded-at="2026-04-21 10:00"`)
}
```

**ポイント**:

- `strings.Contains` はエスケープしないため、`data-temperature="23.50"` を引用符ごとそのまま検索できる（Laravel の `assertSee(..., false)` の第2引数に相当する配慮は不要）
- **属性名 + `=` + 引用符で囲まれた値**を丸ごと検索するため、別セル内の `23.50` 等との部分一致衝突を回避できる
- DB の `numeric(5,2)` 型は `pgtype.Numeric`。`internal/infra/pgconv` で `float64`／`string` へ変換した結果が `"23.50"` のように小数 2 桁になるか、フォーマット側の桁と一致させること（スキーマ: `sensor_readings.temperature numeric(5,2)`）

**適用場面**:

- 集計・計算ロジック（比率、平均、残高、損益）の正確性検証
- NULL / 0 / 境界値（ゼロ除算回避、`pgtype` の `Valid=false` を空文字で出す等）の取り扱い検証
- 複数クエリ結果を service 層でマージした結果が正しいことの検証

**他手段との使い分け**:

| 検証したい値 | 推奨手段 | 参照 |
|------------|---------|------|
| `PerPage` / `SortParam` 等スカラー | View 構造体の直接アサート、または UI 副作用（`<option selected>`、pagination URL） | §4.1.1 |
| 集計・計算結果（複数値を行単位） | **`data-*` 属性 + `assertContains(t, html, \`data-X="Y"\`)`** | **本小節** |
| ソート順 | 文字列位置比較（`strings.Index`）or `assertContainsInOrder` | §4.3 |

### 4.9 件数アサートはメッセージ付きヘルパーを自作する（標準ライブラリには message 引数が無い）

> 【ADAPT】Laravel: `assertDatabaseCount($table, $count, $connection)` の第3引数を「メッセージ」と誤用し `Database connection [...] not configured` で失敗した → Go: `testing` には `assertDatabaseCount` 相当が無い。件数検証は自分で書くため「引数の誤用」自体が起きにくいが、メッセージ付き等値検証の作法を固定しておく。

実 DB を使う件数検証（goose migrate 済み DB、ロールバック検証等）では、対象テーブルへ `COUNT(*)` クエリを発行し、`got != want` を自前で比較する。Laravel の `assertDatabaseCount` のように「引数位置を間違えると connection 名扱いになる」罠は Go には無いが、**メッセージは常に `t.Errorf` のフォーマット文字列で明示的に添える**作法に統一する。

```go
// メッセージ付き件数検証ヘルパー（実 DB テスト用に _test.go へ切り出す）
func assertReadingCount(t *testing.T, q repository.Querier, deviceID int64, want int64, msg string) {
	t.Helper()
	got, err := q.CountSensorReadingsByDevice(context.Background(), deviceID)
	if err != nil {
		t.Fatalf("件数取得失敗: %v", err)
	}
	if got != want {
		t.Errorf("%s: 件数 got %d, want %d", msg, got, want)
	}
}

// 使用例: ファイル内重複時は全件ロールバックされること（tx ロールバック後 0 件）
assertReadingCount(t, q, deviceID, 0, "ファイル内重複時は全件ロールバック")
```

**同種の注意**:
- 件数用クエリは一覧取得クエリと**別に持つ**（`db/queries/*.sql` に `CountSensorReadingsByDevice` 等を定義し sqlc に生成させる）。PostgreSQL は `GROUP BY` 非集約列を常にエラーにする（MariaDB の `ONLY_FULL_GROUP_BY` より厳格）ため、件数と一覧を 1 クエリで兼ねようとせず分離するのが安全
- 「存在しないこと」の検証はクエリ結果が `pgx.ErrNoRows` か件数 0 かで判定し、`t.Errorf` の第1引数（フォーマット文字列）にメッセージを必ず添える
- Querier モックで完結する単体テストでは、モックの呼び出し回数（`createCalled bool` のようなフラグ。`sensor_api_test.go` の `fakeSensorRepo` 参照）で「INSERT が呼ばれていない＝ロールバック相当」を検証できる

### 4.10 テストメッセージ内の変数埋め込みは Go の書式指定で行う

> 【DROP】Laravel: PHP の二重引用符文字列で `$label」` の `」` が識別子文字として変数名に取り込まれ `Undefined variable` になった。
> 【Go では該当なし】Go の文字列リテラルに変数展開（補間）構文は無い。`"列ラベル「$label」"` と書いても `$label` はただのリテラル文字列で、変数として解釈されることはない（コンパイルも通る）。転用できる教訓: テストメッセージへ変数を入れるなら `t.Errorf`/`fmt.Sprintf` の書式指定（`%s` / `%q`）か文字列連結を使うのを習慣にする。`%q` を使うと値が引用符付きで出力され、空文字や前後空白の混入を発見しやすい。

```go
// Go: 文字列リテラル内 "$label" はただの文字。変数として展開されない（PHP の罠は存在しない）
for _, label := range labels {
	if !strings.Contains(html, label) {
		// OK パターン 1: 書式指定（%q 推奨。引用符付きで空文字・空白混入を発見しやすい）
		t.Errorf("列ラベル %q が出力に存在しない", label)

		// OK パターン 2: 文字列連結
		t.Error("列ラベル「" + label + "」が出力に存在しない")
	}
}
```

**指針**: テストメッセージで動的な値を見せたいときは `t.Errorf("... %q ...", v)` を既定にする。`%q` は値を Go 構文の引用符付きで出力するため、「空文字」「前後の空白」「制御文字」といった原因をログだけで切り分けられる。`assertContains` ヘルパーは内部で `t.Errorf("出力に %q が含まれていない:\n%s", substr, html)` と `%q` を使っており、この作法に揃っている。

---

## 5. テストデータの準備

### 5.1 fixture ビルダーと Querier モックの戻り値設計

> 分類: ADAPT（Factory → sqlc 行構造体 + Querier モック / 実DB INSERT ヘルパ）

Laravel の Factory に相当するものは Go には無い。本プロジェクトのテストデータは2系統で用意する。

1. **DB 非依存テスト（既定）** — `repository.Querier` の手書きモックが返す **sqlc 生成の行構造体**（`repository.Device{}`、`repository.User{}` 等）を直接組み立てる。`fakeSensorRepo` や `fakeAuthRepo` のように、必要なメソッドだけ持つ最小モックを `*_test.go` 内に定義する（`internal/handler/sensor_api_test.go`、`internal/handler/auth_test.go` 参照）。
2. **実DB テスト（必要時のみ）** — goose migrate 済みの**テスト専用DB**に対して `repository.Querier` の `Create...` メソッド（または直 INSERT）でデータを投入する fixture ビルダーを用意する。

Laravel の教訓「Factory 未作成のモデルで `Class not found` が出る／NOT NULL 外部キー先の Factory が無いと親 Factory が壊れる」に対応する Go の落とし穴は、**NOT NULL カラムを行構造体で埋め忘れる**ことと、**論理リレーション先（`<table>_id`）の親行を先に作らない**ことである。本プロジェクトは外部キー制約を張らない方針（`docs/database_snapshot/table_definitions.md` 冒頭）だが、論理整合は崩れるため fixture ビルダーは親→子の順で作る。

行構造体を作るヘルパは、スキーマの NULL 可否（`table_definitions.md`）に整合させる。例として `devices` の `location` は NULL 可・`is_active` は NOT NULL デフォルト true なので、ビルダーのデフォルト値もそれに合わせる。

```go
// internal/handler/sensor_api_test.go の fakeSensorRepo が体現する
// 「Querier の最小モックが行構造体を返す」パターン。
// 必要なメソッドだけ実装し、テストデータ＝戻り値の行構造体で与える。
type fakeSensorRepo struct {
	device    repository.Device       // GetDevice の戻り値（= テストデータ本体）
	getErr    error
	reading   repository.SensorReading
	createErr error
}

func (f *fakeSensorRepo) GetDevice(_ context.Context, _ int64) (repository.Device, error) {
	return f.device, f.getErr
}

// 呼び出し側: 所有者一致ケースのデータを行構造体で宣言
repo := &fakeSensorRepo{device: repository.Device{ID: 10, UserID: 7}}
```

実DB へ投入する fixture ビルダーは、NOT NULL を埋めつつデフォルトを上書き可能にする「オプション関数」形式が保守しやすい。

```go
// 実DB テスト用 fixture（goose migrate 済みのテストDBに対して使う）。
// NOT NULL を全て埋め、論理リレーション先（users）を先に作る前提。
func insertDevice(t *testing.T, q repository.Querier, userID int64, opts ...func(*repository.CreateDeviceParams)) repository.Device {
	t.Helper()
	p := repository.CreateDeviceParams{
		UserID:     userID,
		Name:       "テスト農場センサー",
		MacAddress: "AA:BB:CC:DD:EE:FF", // CHECK 制約 devices_mac_address_format に合致させる
		Location:   pgconv.TextOrNull("ハウス1"), // location は NULL 可
		IsActive:   true,
	}
	for _, opt := range opts {
		opt(&p)
	}
	d, err := q.CreateDevice(context.Background(), p)
	if err != nil {
		t.Fatalf("insertDevice: %v", err)
	}
	return d
}
```

ポイント: `mac_address` は CHECK 制約 `devices_mac_address_format`（`^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$`）があり、`devices_mac_address_unique_active`（`deleted_at IS NULL` の部分 UNIQUE）もあるため、複数デバイスを作るケースでは MAC を変える。これは Laravel で「NOT NULL 外部キーに Factory が必要」と言っていた注意の Go 版（= スキーマ制約をビルダーで満たす）にあたる。

### 5.2 状態別 fixture ヘルパ（カスタム State 相当）

> 分類: ADAPT（`withStatus()` State → 名前付き fixture ヘルパ / オプション関数）

Laravel の `Project::factory()->withStatus('受注')` のような State 付き生成は、Go ではオプション関数を返すヘルパか、専用ビルダー関数で表現する。本プロジェクトに「ステータス」を持つのは `devices.is_active` や `alert_rules.is_enabled` 等のフラグである。タブ・フィルタ相当のテストでは、状態を表す名前付きオプションを用意すると `t.Run` のサブテストが読みやすくなる。

```go
// オプション関数で「無効デバイス」「有効ルール」等の状態を宣言的に表す。
func withInactive() func(*repository.CreateDeviceParams) {
	return func(p *repository.CreateDeviceParams) { p.IsActive = false }
}

// 呼び出し（is_active でフィルタする一覧テストが読みやすくなる）
active := insertDevice(t, q, userID)
inactive := insertDevice(t, q, userID, withInactive(), func(p *repository.CreateDeviceParams) {
	p.MacAddress = "11:22:33:44:55:66" // UNIQUE 回避
})
```

DB 非依存テスト（Querier モック）の場合は、戻り値の行構造体を直接そのフラグで作るだけで足りる（`repository.Device{IsActive: false}`）。`userWithPassword(id, name, email, password)`（`internal/handler/auth_test.go`）も「パスワード設定済みユーザー」という State 付き fixture の好例で、bcrypt ハッシュ生成を1か所に集約している。

### 5.3 論理削除（soft delete）の有無を確認する

> 分類: ADAPT（SoftDeletes トレイト → `deleted_at` カラム + クエリ側の `deleted_at IS NULL`）

Laravel の `SoftDeletes` トレイト／`withTrashed()` に相当するのは、本プロジェクトの **`deleted_at` カラム + sqlc クエリ側の `WHERE deleted_at IS NULL`** である。トレイトという「モデルに付くか付かないか」の二択ではなく、**テーブルごとに `deleted_at` 列があるか**を `table_definitions.md` で確認する。

`deleted_at` を**持つ**テーブル: `devices`、`sensor_readings`、`alert_rules`、`alert_histories`。
`deleted_at` を**持たない**テーブル: `users`、`device_tokens`、`sessions`。

Laravel では「`User` に `SoftDeletes` が無いのに `withTrashed()` を呼ぶとエラー」だった。Go では論理削除フィルタはクエリに静的に書かれている（例: `db/queries/devices.sql` の `GetDevice` は `WHERE id = $1 AND deleted_at IS NULL`）。したがって**論理削除の確認は「削除済み行を含める専用クエリがあるか」で行う**。

```go
// devices は GetDevice / ListDevicesByUser ともに deleted_at IS NULL で絞る。
// 「削除済みも見る」テストをしたいなら、それ用のクエリ（例 GetDeviceWithTrashed）が
// db/queries/devices.sql に存在することが前提。無ければ withTrashed 相当は不可能。
//
// 論理削除の検証パターン（実DB テスト）:
//   1. SoftDeleteDevice で deleted_at をセット
//   2. GetDevice が pgx.ErrNoRows を返す（= 通常クエリから消える）ことをアサート
func TestSoftDelete_削除後は通常クエリから消える(t *testing.T) {
	d := insertDevice(t, q, userID)
	if err := q.SoftDeleteDevice(context.Background(), d.ID); err != nil {
		t.Fatalf("SoftDeleteDevice: %v", err)
	}
	if _, err := q.GetDevice(context.Background(), d.ID); !errors.Is(err, pgx.ErrNoRows) {
		t.Errorf("論理削除後の GetDevice = %v, want pgx.ErrNoRows", err)
	}
}
```

`users` に `deleted_at` が無い点は、Laravel で「`User` には `SoftDeletes` が無い」と言っていたのと同じ構造の注意であり、`users` を論理削除前提で扱うテストは書けない。

### 5.4 複数リレーションの共存テスト（1対1 と 1対多）

> 分類: ADAPT（Eloquent HasOne/HasMany + Eager Load → sqlc の複数クエリ + N+1 観点）

Eloquent のリレーション（`HasOne`/`HasMany`）や `with()` Eager Load は Go には無い。sqlc では**リレーションごとに別クエリ**を書く。Laravel の「同一テーブルに HasMany と HasOne が併存しても両方壊れていないことをテスト」という教訓は、本プロジェクトでは「**同じ親に対する『1件取得』クエリと『一覧取得』クエリが両立して正しく動く**」というテストに読み替える。

例: 1つの `device` に対し `alert_rules` は1対多（`ListAlertRulesByDevice :many`）、最新の `sensor_readings` を1件だけ取る用途なら1対1的な `:one` クエリを別に持つ、という構成。両方を1つのテストで検証する。

```go
func TestDevice_最新計測1件と全ルール一覧が両立(t *testing.T) {
	d := insertDevice(t, q, userID)
	insertReading(t, q, d.ID, 25.0, 60.0)
	insertReading(t, q, d.ID, 30.0, 55.0) // 後勝ちが「最新」
	insertAlertRule(t, q, d.ID)            // 1対多側

	// 1件取得（HasOne 相当）: 最新の計測が取れること
	latest, err := q.GetLatestReadingByDevice(context.Background(), d.ID)
	if err != nil {
		t.Fatalf("GetLatestReadingByDevice: %v", err)
	}
	if got := pgconv.NumericToFloat(latest.Temperature); got != 30.0 {
		t.Errorf("最新温度 = %v, want 30.0", got)
	}

	// 一覧取得（HasMany 相当）が壊れていないこと
	rules, err := q.ListAlertRulesByDevice(context.Background(), d.ID)
	if err != nil {
		t.Fatalf("ListAlertRulesByDevice: %v", err)
	}
	if len(rules) != 1 {
		t.Errorf("ルール件数 = %d, want 1", len(rules))
	}
}
```

> 【N+1 の観点は Go では別アプローチ】Eloquent の `with('estimate.prospectStatus')` のような Eager Load チェーンは sqlc に無い。N+1 は「ループ内で `:one` クエリを発行していないか」をコードレビューと、必要なら JOIN クエリ（1本で取る `:many`）の有無で担保する。`for` ループ内で `q.GetXxx()` を呼んでいたら N+1 のサインとして扱う。

### 5.5 DB 非依存（Querier モック） vs 実DB テストの使い分け

> 分類: ADAPT（RefreshDatabase / DatabaseTransactions → Querier モック / goose migrate 済みテストDB）

| 方式 | 用途 | 速度 |
|------|------|------|
| **Querier モック**（既定） | handler / middleware / service / authz のロジック。DB 不要 | 最速（DB 起動不要・並列可） |
| **goose migrate 済みテストDB** | sqlc クエリそのもの・CHECK 制約・部分 UNIQUE・トランザクション挙動の検証 | 遅い（DB 必須） |

Laravel の `RefreshDatabase`/`DatabaseTransactions` は「テストを必ず実 DB 上で回す」前提だったが、本プロジェクトの既定は**そもそも DB に触れない**こと（`repository.Querier` interface のモック）である。`internal/handler/`・`internal/middleware/`・`internal/authz/` のテストは全て DB 非依存で書かれている（`fakeSensorRepo`、`fakeAuthRepo`、`fakeDeviceGetter` 等）。これにより `RefreshDatabase` の初回マイグレーション待ちが不要になり、Laravel の「コントローラーテストは RefreshDatabase」というコスト前提自体が消える。

実DB が要るのは**クエリ・制約・トランザクションそのもの**を検証する場合に限る。その場合は以下を守る。

- **接続文字列で本番/開発DBと必ず分離**する。`DATABASE_URL` とは別の `TEST_DATABASE_URL` を環境変数で渡し、未設定なら `t.Skip` する（CI 以外で誤って開発DBを破壊しない）。
- **クリーンアップは TRUNCATE か tx ロールバック**。各テストの先頭で対象テーブルを `TRUNCATE ... RESTART IDENTITY CASCADE`、または `pgx.Tx` でテストを包み末尾で `Rollback()`。

```go
// 実DB テストの入口ヘルパ。TEST_DATABASE_URL 未設定なら skip。
func openTestDB(t *testing.T) (*pgxpool.Pool, repository.Querier) {
	t.Helper()
	dsn := os.Getenv("TEST_DATABASE_URL")
	if dsn == "" {
		t.Skip("TEST_DATABASE_URL 未設定のため実DB テストをスキップ")
	}
	pool, err := pgxpool.New(context.Background(), dsn)
	if err != nil {
		t.Fatalf("test db 接続失敗: %v", err)
	}
	t.Cleanup(pool.Close)
	return pool, repository.New(pool)
}

// 対象テーブルを毎テスト初期化（外部キー制約は張っていないので CASCADE は実質不要だが
// 依存順に消したいなら順序を明示する）。
func truncateAll(t *testing.T, pool *pgxpool.Pool) {
	t.Helper()
	_, err := pool.Exec(context.Background(),
		`TRUNCATE sensor_readings, alert_histories, alert_rules, device_tokens, devices, users RESTART IDENTITY`)
	if err != nil {
		t.Fatalf("truncate: %v", err)
	}
}
```

### 5.6 DDL は Querier モックでは不要 / DB 例外の再現はモックで行う

> 分類: ADAPT（DDL が RefreshDatabase を壊す問題 → 既定が DB 非依存なので問題自体が消える / DB 例外はモックで注入）

Laravel の「DDL が暗黙コミットして `RefreshDatabase` のロールバックを壊し後続テストが連鎖失敗する」問題は、**本プロジェクトの既定テストが DB に触れないため原則発生しない**。`config()` + `DB::purge()` のような接続先すり替えでミドルウェアごと壊す手法も Go では不要・非推奨である。

Laravel が `addGlobalScope()` で「特定モデルのクエリだけ例外を投げ、認可ミドルウェアは通過させる」テクニックで再現していた「**ハンドラの try-catch（DB 例外時のエラー応答）をテストする**」目的は、Go では **Querier モックが任意のメソッドでだけエラーを返す**ことで、はるかに単純に達成できる。認証・認可ミドルウェアは別のモック設定で正常通過させたまま、対象クエリだけ失敗させられる。

```go
// Laravel: Department::addGlobalScope('test-error', fn() => throw ...) で
//          Department クエリのみ例外、Gate は通過。
// Go:     Querier モックの該当メソッドだけ error を返すだけでよい。
//         （sensor_api_test.go の getErr / createErr がまさにこの仕組み）

func TestSensorAPI_Create_保存失敗は500(t *testing.T) {
	repo := &fakeSensorRepo{
		device:    repository.Device{ID: 10, UserID: 7}, // GetDevice は成功（認可は通過）
		createErr: errors.New("insert failed"),          // CreateSensorReading だけ失敗
	}
	w := postSensorData(t, newRouterWithUser(&SensorAPI{Repo: repo}, 7))

	if got, want := w.Code, http.StatusInternalServerError; got != want {
		t.Errorf("status: got %d, want %d, body=%s", got, want, w.Body.String())
	}
}
```

**ポイント:**
- `finally`/`clearBootedModels()` のような後始末は不要。モックはテストローカルで、グローバル状態を汚さない（並列テストでも干渉しない）。
- 「特定のクエリだけ落とし、他は通す」を `getErr`/`createErr` のフィールド分けで自然に表現できる（`internal/auth/device_auth_test.go` の `fakeTokenRepo.getErr` も同型）。
- DDL を扱う検証（マイグレーションそのもの）が必要なら、それは goose のマイグレーションテスト（実DB）として §5.5 の枠組みで分離して書く。

### 5.5 画面確認用シードデータの配置パターン

> 分類: ADAPT（artisan db:seed クラス → `cmd/seed` + 画面ID単位の投入関数）

PHPUnit の Factory とは別に「**ブラウザ実機（MCP chrome-devtools）で画面動作を確認するため**」の検証データを投入する仕組みは Go でも有効である。本プロジェクトの開発用シードは `cmd/seed`（`make seed`）に集約されている。Laravel の「Seeder クラスを画面ID単位で独立配置」を、Go では `cmd/seed` 配下の**画面ID単位の投入関数**（例 `seedW006(ctx, q)`）として実装し、フラグで対象画面だけ投入できるようにする。

**設計原則（Go 版）:**

1. **独立配置** — 全画面共通のマスター投入（ユーザー・デバイス）と、画面別確認データは関数を分ける。`cmd/seed/main.go` から呼ぶ関数を画面単位で切る。
2. **冪等性** — 同一シードを複数回流しても同結果になるよう、先頭で対象データを**物理削除**してから INSERT する。本プロジェクトは soft delete 運用だが、シードのクリーンアップは `DELETE`（物理）で行い `deleted_at` 残骸を残さない。`make seed` のヘルプに「既存のアプリデータは削除される」と明記されている通り、破壊的前提で書く。
3. **対象ユーザーを固定** — 新規ユーザーを毎回作らず、既存の固定ユーザー（例 `demo@example.com`）に紐付ける。`users_email_unique` があるため、ユーザーは `ON CONFLICT (email) DO NOTHING`/`DO UPDATE` で冪等に確保する。
4. **バリエーション網羅** — 温度（下限 -40・上限 125・通常値）・湿度（0/100/中間）・複数デバイス／複数日付の混在で、一覧のソート・集計・空状態・件数表示の挙動を確認できる構成にする（CHECK 制約 `sensor_readings_temperature_range`/`humidity_range` の境界を意図的に突く）。

```go
// cmd/seed 配下の画面別シード関数（冪等・既存ユーザーに集約）。
func seedDashboardData(ctx context.Context, q repository.Querier) error {
	user, err := upsertDemoUser(ctx, q) // ON CONFLICT(email) で冪等確保
	if err != nil {
		return fmt.Errorf("デモユーザー確保: %w", err)
	}

	// 冪等性: このユーザーのデバイスと従属データを物理削除してから作り直す
	if err := purgeUserData(ctx, q, user.ID); err != nil {
		return fmt.Errorf("既存データ削除: %w", err)
	}

	// バリエーション投入: 境界値・複数デバイス・複数日付
	for i, mac := range []string{"AA:BB:CC:DD:EE:01", "AA:BB:CC:DD:EE:02"} {
		d, err := q.CreateDevice(ctx, repository.CreateDeviceParams{
			UserID: user.ID, Name: fmt.Sprintf("ハウス%d", i+1),
			MacAddress: mac, Location: pgconv.TextOrNull("圃場A"), IsActive: true,
		})
		if err != nil {
			return fmt.Errorf("デバイス作成: %w", err)
		}
		// 温湿度の境界値（CHECK 制約 -40〜125 / 0〜100）を含めて投入
		for _, r := range []struct{ temp, hum float64 }{{-40, 0}, {125, 100}, {25.5, 60.3}} {
			if _, err := q.CreateSensorReading(ctx, repository.CreateSensorReadingParams{
				DeviceID: d.ID, Temperature: pgconv.Numeric2(r.temp),
				Humidity: pgconv.Numeric2(r.hum), RecordedAt: pgconv.Timestamptz(time.Now()),
			}); err != nil {
				return fmt.Errorf("計測投入: %w", err)
			}
		}
	}
	return nil
}
```

**実行コマンド:**

```bash
make seed
# あるいは画面を絞るならフラグ運用（cmd/seed 側で -screen を実装した場合）
go run ./cmd/seed -screen dashboard
```

> 【監査ログ削除は Go では該当なし】Laravel の `HasAudit`／`audits` テーブルに相当する仕組みは本プロジェクトに無い。冪等削除で気にすべき従属テーブルは `alert_histories`（`alert_rule_id` 経由）等の論理リレーション先であり、親（`devices`/`alert_rules`）削除前に子から消す順序だけ守ればよい。

#### 5.5.X シード投入順序の落とし穴: 子は親（user/device）の後

> 分類: ADAPT（MasterDataSeeder → PermissionTestSeeder の順序問題 → 親行 ID 解決順）

Laravel で「`PermissionTestSeeder` が `branches` を引く前に `MasterDataSeeder` が未実行だと `branch_id=NULL` のユーザーができる」という順序事故があった。本プロジェクトにも同型の罠がある: **子テーブル（`devices`/`alert_rules`/`sensor_readings`）は親（`users`/`devices`）の ID に依存**するため、親を先に確保しないと外部キー相当の値が解決できない（本プロジェクトは外部キー制約を張らないので**エラーにならず、誤った ID で投入される**点がより危険）。

**正しいシード実行順序:**

1. `users`（`upsertDemoUser`）— `ON CONFLICT(email)` で冪等確保
2. `devices`（`user_id` を 1 で解決）
3. `alert_rules`（`device_id` を 2 で解決）
4. `sensor_readings` / `alert_histories`

Laravel が `firstOrCreate` で「初回だけ NULL 固定」になった罠に対応するのは、Go では**`ON CONFLICT ... DO NOTHING` で既存行をそのまま使う場合に古い値が残る**ケースである。値を最新化したいなら `DO UPDATE SET ...` を使う（§5.7 参照）。

**検証 SQL（投入後の整合確認）:**

```sql
-- user_id / device_id が実在の親を指しているか（外部キー制約が無い分、手で確認する）
SELECT d.id, d.user_id, u.email
FROM devices d
LEFT JOIN users u ON u.id = d.user_id
WHERE u.id IS NULL;          -- 1件でも返れば孤児（投入順序違反）

SELECT r.id, r.device_id
FROM alert_rules r
LEFT JOIN devices d ON d.id = r.device_id AND d.deleted_at IS NULL
WHERE d.id IS NULL;          -- 親デバイス不在のルール = 投入順序違反
```

### 5.6 MCP chrome-devtools での画面操作 Tips

> 分類: ADAPT（Tom Select / Blade x-component → Alpine.js / templ / HTMX。回避策スニペットは概ね転用可）

画面確認テストを MCP chrome-devtools で行う際、`click`/`fill` ツールが効かないケースと回避策。本プロジェクトの UI は **templ + HTMX + Alpine.js** なので、Laravel 版から「Tom Select → Alpine.js / ネイティブ要素」へ読み替える。Querier モックの単体テストでは検出できない UI 操作レベルのバグ検出に使う。

| ケース | `click`/`fill` で動かない理由 | 回避策（`evaluate_script`） |
|---|---|---|
| **Alpine.js チェックボックス・ラジオ** | `click` では `@change`/`x-model` が発火しないことがある | `el.checked = true` + `el.dispatchEvent(new Event('change', { bubbles: true }))` |
| **Alpine.js select（`x-model`）** | DOM の `value` 代入だけでは `x-model` が追従しない | `el.value = v; el.dispatchEvent(new Event('input', {bubbles:true}))` |
| **HTMX リンク／ボタン** | `click` で `hx-get`/`hx-post` が発火しないことがある | `navigate_page` で直接 URL へ、または `htmx.ajax('GET', url, '#target')` を呼ぶ |
| **302 リダイレクト確認** | `fetch()` は自動追跡でリダイレクト先の 200 が返る | `fetch(url, { redirect: 'manual', credentials: 'include' })` → `{type:'opaqueredirect', status:0}` なら 302 発生 |
| **CSRF トークン付き POST** | Web エンドポイントは gorilla/csrf のトークンが必要 | hidden input `input[name="gorilla.csrf.Token"]` の `value`、または `<meta name="csrf-token">` を `X-CSRF-Token` ヘッダに付与 |

**入れ子 DOM バグの検出（HTMX target 誤指定）:**

```javascript
// App レイアウトのヘッダ・サイドバー等は本来1件のみ。
// HTMX の hx-target を誤ると共通コンポーネントが二重描画される。
document.querySelectorAll('#main-content').length === 1   // true 期待
document.querySelectorAll('.app-header').length === 1      // true 期待
```

2 以上が返れば HTMX の `hx-target` 誤指定（`#main-content` 以外を指す等）で入れ子発生の疑い。共通コンポーネント使用時の `hx-target` 必須ルールは [HTMX実装ガイド §4 画面別HTMX操作](HTMX実装ガイド(動的).md)参照。

**HTMX 属性の実反映値を検査:**

```javascript
// 検索フォームの hx 属性が templ 側で正しく出力されているか確認
const form = document.querySelector('form[hx-get]');
({
    hxGet:    form.getAttribute('hx-get'),
    hxTarget: form.getAttribute('hx-target'), // "" は NG、"#xxx-list" が正
    hxSwap:   form.getAttribute('hx-swap'),
})
```

`hx-target` が空文字なら、templ コンポーネント呼び出しで `target` パラメータを渡し忘れている。

> 【Tom Select は Go では該当なし】本プロジェクトは Tom Select を使わない（select は素のネイティブ + Alpine.js `x-model`）。`el.tomselect.setValue()` 相当は不要。
> 【UTF-8 BOM 付き CSV 送信は転用可だが用途限定】CSV インポート機能を実装した場合のみ。BOM 付き Blob 生成（`new Blob([new Uint8Array([0xEF,0xBB,0xBF]), csvText], {type:'text/csv'})`）の技法はそのまま使える。

#### 5.6.X レイアウト不具合は必ず MCP 実測してから CSS 修正（推測で触らない）

> 分類: PORT（教訓はそのまま通用。CSS 実測スニペットは言語非依存）

CSS の「はみ出し」「切れる」「寸法が合わない」のレポートを受けたら、推測で `flex-shrink`/`overflow`/`max-width` を連続修正せず、**最初に MCP chrome-devtools で両側を実測して差分を数値で特定**する。本プロジェクトの CSS は単一ソース運用（正本 `mocks/html/style.css`、`make sync-css` 生成物は手編集禁止・gitignore）なので、修正は**正本に対してのみ**行う。templ は独自クラス禁止（HTMX実装ガイド §31）なので、クラスの追加・改名で逃げる前に実測する。

**最小実測スニペット:**

```javascript
function measure(rowSel) {
    const row = document.querySelector(rowSel);
    if (!row) return { err: 'row not found' };
    const pick = el => {
        if (!el) return null;
        const r = el.getBoundingClientRect();
        const s = window.getComputedStyle(el);
        return {
            rect: { left: Math.round(r.left), right: Math.round(r.right), width: Math.round(r.width) },
            cs: {
                display: s.display, padding: s.padding, width: s.width,
                minWidth: s['min-width'], maxWidth: s['max-width'],
                flex: s.flex, overflow: s.overflow, whiteSpace: s['white-space'],
                gridTemplateColumns: s['grid-template-columns'],
            },
            scrollWidth: el.scrollWidth, clientWidth: el.clientWidth,
        };
    };
    return {
        cell:      pick(row.querySelector('.device-cell')),
        container: pick(row.querySelector('.device-row')),
        label:     pick(row.querySelector('.device-label')),
    };
}
return { GOOD: measure('#good-side tr'), BAD: measure('.bad-side tr') };
```

**比較観点:**

| 観点 | 差が出やすい CSS プロパティ |
|------|---------------------------|
| 幅の違い | `width`, `min-width`, `max-width`, `flex-basis`, `grid-template-columns` |
| 余白 | `padding`, `margin`, 親の `gap` |
| はみ出し挙動 | `overflow`, `white-space`, `text-overflow` |
| flex の伸縮 | `flex-grow`, `flex-shrink`, `flex-basis` |
| ボックス計算 | `box-sizing`, `border-width` |

`scrollWidth > clientWidth` なら要素内コンテンツが溢れている。`rect.right` が親 `rect.right` より大きければ親からはみ出ている。**具体的な差分を数値で示してから**正本 CSS を修正し、`make sync-css` で本番へ反映する。

#### 5.6.Y `<script type="application/json">` の中身は `json.Marshal` の純 JSON にする（`templ.JSONString` 混用の罠）

> 分類: ADAPT（`Js::from()` が `JSON.parse('...')` 式を吐く罠 → templ で `templ.JSONString`/`x-data` 用の式を `<script type="application/json">` に流す罠）

templ で `<script type="application/json">` に JS から読みたいデータを埋め込むとき、**`x-data`/`@click` 用の `templ.JSONString`（式として評価される形式向け）を流用してはいけない**。`<script type="application/json">` の中身は**純 JSON でなければならず**、属性式向けの出力を入れると `JSON.parse(textContent)` 側で構文エラーになり catch で空配列に落ちる。**ロジックは動いているのに値が反映されない**現象が出て、Querier モックの単体テストはパスしても画面は動かない（HTMX実装ガイド §10-E）。

**NG（属性式向けの出力を script に流す）:**
```go
<script type="application/json" id="chart-data">
    @templ.Raw(templ.JSONString(rows)) // 式評価向けの形式。純 JSON にならず壊れうる
</script>
```

**OK（`encoding/json` の `json.Marshal` を `@templ.Raw` で純 JSON 出力）:**
```go
templ ChartData(rows []ChartPoint) {
	<script type="application/json" id="chart-data">
		@templ.Raw(marshalJSON(rows))
	</script>
}

// json.Marshal は < > & を \uXXXX へエスケープするため </script> 混入による
// XSS は起きない（SetEscapeHTML(true) がデフォルト）。
func marshalJSON(v any) string {
	b, err := json.Marshal(v)
	if err != nil {
		return "[]"
	}
	return string(b)
}
```

**テストで検出するパターン（templ Render → 文字列検証）:**

```go
func TestChartData_純JSONが埋め込まれJSONParse可能(t *testing.T) {
	rows := []ChartPoint{{MeasuredAt: "2026-04-21T00:00:00Z", Metric: "temperature", Value: 25.5}}
	html := render(t, ChartData(rows)) // page_test.go の render ヘルパと同型

	// 1. 属性式向けの残骸（JSON.parse( 等）が混入していないこと
	if strings.Contains(html, "JSON.parse(") {
		t.Error("script 内に式形式の残骸 (JSON.parse() が混入している")
	}

	// 2. script 内容が正しい JSON として Unmarshal 可能であること
	body := extractScriptBody(t, html, `id="chart-data"`)
	var got []ChartPoint
	if err := json.Unmarshal([]byte(body), &got); err != nil {
		t.Fatalf("埋め込み JSON が JSON.parse 可能でない: %v\nbody=%s", err, body)
	}
}
```

MCP 実機検証では `JSON.parse(document.getElementById('chart-data').textContent)` が例外を投げないことを確認する。

### 5.7 PostgreSQL の `INSERT ... ON CONFLICT` は競合対象を列で明示する（部分 UNIQUE の罠）

> 分類: ADAPT（MariaDB upsert の全 UNIQUE silent update → PostgreSQL ON CONFLICT は競合制約を明示。NULL 複数許容は共通）

MariaDB の `INSERT ... ON DUPLICATE KEY UPDATE`（Laravel `upsert`）は `$uniqueBy` 指定列だけでなく**テーブル上の全 UNIQUE 制約**を重複判定に使い、他の UNIQUE カラムが衝突すると**既存行を silent update**した。PostgreSQL の `INSERT ... ON CONFLICT` は**競合対象の制約／列を明示する**ため、この silent update は起きにくいが、別の落とし穴がある。

本プロジェクトの UNIQUE は**部分 UNIQUE（partial index）**が多い: `devices_mac_address_unique_active` は `WHERE deleted_at IS NULL` 付き、`users_email_unique`・`device_tokens_token_hash_unique` は全行 UNIQUE。**部分 UNIQUE を `ON CONFLICT` の競合対象に使うには、インデックスと同じ `WHERE` 述語を書く**必要がある（書かないと「該当する一意制約が無い」エラーになる）。

```sql
-- ❌ NG: 部分 UNIQUE なのに述語を書かないと
--        "there is no unique or exclusion constraint matching the ON CONFLICT specification"
INSERT INTO devices (user_id, name, mac_address, is_active)
VALUES ($1, $2, $3, true)
ON CONFLICT (mac_address) DO UPDATE SET name = EXCLUDED.name;

-- ✅ OK: 部分 UNIQUE と同じ WHERE 述語を明示
INSERT INTO devices (user_id, name, mac_address, is_active)
VALUES ($1, $2, $3, true)
ON CONFLICT (mac_address) WHERE deleted_at IS NULL
DO UPDATE SET name = EXCLUDED.name, updated_at = NOW();

-- ✅ users は全行 UNIQUE なので述語不要（シードの冪等確保で使う）
INSERT INTO users (name, email, password_hash)
VALUES ($1, $2, $3)
ON CONFLICT (email) DO UPDATE SET name = EXCLUDED.name, updated_at = NOW();
```

**UNIQUE + NULL 挙動**は MariaDB と同じく PostgreSQL も標準準拠で **NULL は重複扱いしない**（NULL は複数行で許容）。`false` は 1 値なので衝突する点も同じ。Laravel が `is_initial=false` の衝突を `null` で回避したのと同じ判断が PostgreSQL でも有効。

**シード／upsert テストで担保する項目**（sqlc クエリ + 実DB テスト、§5.5 の枠組み）:
- 対象行が**正しく INSERT される**こと（`q.GetDeviceByMacAddress` 等で取得して全カラム比較）
- **既存行が意図せず書き換わっていない**こと（`DO UPDATE` で更新する列以外が保持されているか各カラム assert）
- 再実行しても件数が期待値のまま（冪等性）— `ON CONFLICT DO NOTHING` か `DO UPDATE` かでこの結果が変わる
- ハマったら `SELECT id, mac_address, name, deleted_at FROM devices ORDER BY id` で全行ダンプし silent な変化を可視化する

```go
func TestUpsertDevice_既存をDO_UPDATEし他行は不変(t *testing.T) {
	pool, q := openTestDB(t)        // §5.5 のヘルパ（TEST_DATABASE_URL 必須）
	truncateAll(t, pool)
	u := insertUser(t, q)
	a := insertDevice(t, q, u.ID)   // mac AA:BB:... name "ハウス1"
	b := insertDevice(t, q, u.ID, func(p *repository.CreateDeviceParams) {
		p.MacAddress = "11:22:33:44:55:66"
		p.Name = "ハウス2"
	})

	// a の mac を競合させて name だけ更新するクエリを実行
	if _, err := q.UpsertDeviceName(context.Background(), repository.UpsertDeviceNameParams{
		UserID: u.ID, MacAddress: a.MacAddress, Name: "ハウス1-改",
	}); err != nil {
		t.Fatalf("upsert: %v", err)
	}

	got, _ := q.GetDeviceByMacAddress(context.Background(), a.MacAddress)
	if got.Name != "ハウス1-改" {
		t.Errorf("対象行が更新されていない: name=%q", got.Name)
	}
	other, _ := q.GetDeviceByMacAddress(context.Background(), b.MacAddress)
	if other.Name != "ハウス2" { // ← MariaDB upsert の silent update に相当する事故の検出
		t.Errorf("無関係な行が書き換わった (silent update): name=%q", other.Name)
	}
}
```

### 5.8 table-driven test を使う（PHPUnit DataProvider Attribute 相当）

> 分類: ADAPT（`#[DataProvider]` Attribute → `[]struct{...}` + `for` + `t.Run`）

PHPUnit 12 の `#[DataProvider]` Attribute（旧 PHPDoc `@dataProvider` は非推奨）に相当するのは、Go の **table-driven test**（`[]struct{...}` のケース表を `for` で回し各ケースを `t.Run` で分離）である。本プロジェクトの標準作法であり、`internal/domain/comparison_operator_test.go`・`internal/handler/sensor_api_test.go`・`internal/infra/pgconv/pgconv_test.go` 全てがこの形を取る。Attribute のような言語機構の deprecation 移行は Go には存在せず、最初からこの1パターンで統一する。

```go
// PHPUnit: #[DataProvider('classNameProvider')] public function test_xxx(string $c)
// Go:      ケース表 + for + t.Run（言語標準・移行不要）
func TestComparisonOperator_Valid(t *testing.T) {
	tests := []struct {
		input ComparisonOperator
		want  bool
	}{
		{OpGreaterThan, true},
		{OpLessThan, true},
		{ComparisonOperator("=="), false}, // 不正値
		{ComparisonOperator(""), false},
	}
	for _, tt := range tests {
		t.Run(string(tt.input), func(t *testing.T) { // サブテスト名 = ケース識別子
			if got := tt.input.Valid(); got != tt.want {
				t.Errorf("Valid() = %v, want %v", got, tt.want)
			}
		})
	}
}
```

ポイント:
- **サブテスト名を付ける**（`t.Run(name, ...)`）。失敗時に `go test -run TestXxx/ケース名` で1ケースだけ再実行でき、CI ログでも特定が容易（PHPUnit の named data set 相当）。
- ケース表の各行が「仕様の1行」になるよう、境界値・異常系を網羅する（`comparison_operator_test.go` の `35.00 > 35.00 → false` のような等値境界の明示が好例）。
- ループ内で `tt` をクロージャに閉じ込める場合、Go 1.22+ ではループ変数の再利用問題は解消済み（本プロジェクトは Go 1.26 なので `tt := tt` の再代入は不要）。

### 5.9 アップロード／ダウンロードのテスト（multipart httptest と excelize）

> 分類: ADAPT（openspout `.xlsx` 動的生成 + `UploadedFile::fake` → multipart httptest リクエスト / excelize / zip XML 検証）

`.xlsx`/CSV アップロードを受ける handler のテストは、固定フィクスチャをリポジトリに置くより**テスト内で動的生成**する方が保守しやすい（列・行・値を各テストメソッドで宣言できる）という Laravel の教訓はそのまま通用する。Go では Laravel の `UploadedFile::fake` の代わりに **`mime/multipart` で httptest リクエストを組む**。`.xlsx` 生成は openspout の代わりに **`xuri/excelize`** を使う（または CSV なら `encoding/csv`）。

> ⚠️ 現状、本プロジェクトにアップロード／エクスポート機能は未実装。以下は「将来こう書く」指針として提示する。

**入力側（Importer/handler）— multipart httptest + excelize でオンザフライ生成:**

```go
// 各行の各セルを宣言的に渡すと .xlsx を含む multipart リクエストを返すヘルパ。
func makeXlsxUpload(t *testing.T, field, filename string, rows [][]string) (*bytes.Buffer, string) {
	t.Helper()
	f := excelize.NewFile()
	defer f.Close()
	for i, cells := range rows {
		for j, v := range cells {
			cell, _ := excelize.CoordinatesToCellName(j+1, i+1)
			_ = f.SetCellValue("Sheet1", cell, v)
		}
	}
	var xlsxBuf bytes.Buffer
	if _, err := f.WriteTo(&xlsxBuf); err != nil {
		t.Fatalf("xlsx 生成: %v", err)
	}

	body := &bytes.Buffer{}
	mw := multipart.NewWriter(body)
	part, _ := mw.CreateFormFile(field, filename)
	_, _ = part.Write(xlsxBuf.Bytes())
	_ = mw.Close()
	return body, mw.FormDataContentType() // Content-Type ヘッダに使う
}

// テストから呼ぶ（httptest + gin）
func TestImport_正常な行を取り込む(t *testing.T) {
	body, contentType := makeXlsxUpload(t, "file", "timesheet.xlsx", [][]string{
		{"社員コード", "日付", "温度", "湿度"},
		{"E0001", "2026-04-01", "25.5", "60.3"},
	})
	req := httptest.NewRequest(http.MethodPost, "/import", body)
	req.Header.Set("Content-Type", contentType)
	w := httptest.NewRecorder()
	router.ServeHTTP(w, req) // sensor_api_test.go の httptest+gin 作法と同型

	if w.Code != http.StatusOK {
		t.Fatalf("import = %d, want 200, body=%s", w.Code, w.Body.String())
	}
}
```

**利点**（Laravel 版と同じ）:
- 各ケースで列・行・値を宣言的に書け、レビュー時に仕様意図が明確
- バイナリフィクスチャ（`testdata/*.xlsx`）を git に置かなくて済む（diff 不可で保守困難）
- 異常系（空セル・形式不正・文字化け）も1行書き換えで生成可能。固定ファイルが要る場合だけ `testdata/` に置く（CSV は人間可読なので `testdata/*.csv` 直置きも可）

**出力側（Exporter）— Content-Type / Content-Disposition ヘッダ + 本文を検証:**

```go
func TestExport_CSVヘッダと行を返す(t *testing.T) {
	req := httptest.NewRequest(http.MethodGet, "/export.csv", nil)
	w := httptest.NewRecorder()
	router.ServeHTTP(w, req)

	if got := w.Header().Get("Content-Type"); !strings.HasPrefix(got, "text/csv") {
		t.Errorf("Content-Type = %q, want text/csv", got)
	}
	if cd := w.Header().Get("Content-Disposition"); !strings.Contains(cd, "attachment") {
		t.Errorf("Content-Disposition = %q, want attachment", cd)
	}
	if !strings.Contains(w.Body.String(), "2026-04-10") {
		t.Error("本文に対象データ行が含まれていない")
	}
}
```

**`.xlsx` 出力の中身検証は zip 内 XML を直接読む**（Laravel の `ZipArchive` → Go の `archive/zip`）。`.xlsx` は zip であり、セル値は `xl/worksheets/sheet1.xml` に格納される共通フォーマットなので、excelize で書いても手書き XML でもこの検証パターンが使える。

```go
func TestExport_XLSXのセルXMLに値が入る(t *testing.T) {
	req := httptest.NewRequest(http.MethodGet, "/export.xlsx", nil)
	w := httptest.NewRecorder()
	router.ServeHTTP(w, req)

	zr, err := zip.NewReader(bytes.NewReader(w.Body.Bytes()), int64(w.Body.Len()))
	if err != nil {
		t.Fatalf("xlsx を zip として開けない: %v", err)
	}
	var sheetXML string
	for _, f := range zr.File {
		if f.Name == "xl/worksheets/sheet1.xml" {
			rc, _ := f.Open()
			b, _ := io.ReadAll(rc)
			_ = rc.Close()
			sheetXML = string(b)
		}
	}
	if !strings.Contains(sheetXML, "2026-04-10") {
		t.Error("sheet1.xml に対象データが含まれていない")
	}
}
```

---

## 6. 認可テストのパターン（所有者認可 / 認証ガード）

> 【機構読替（ADAPT）】本プロジェクトに spatie permission 相当の RBAC・role・permission・`can:` ミドルウェアは **未導入**。原本の「権限テスト」は、本プロジェクトでは次の 2 軸に読み替える。
> - **認証ガード**: 未ログイン時、Web ルートは `/login` へ 302（`middleware.RequireAuth`）、API ルートは 401 JSON（`auth.DeviceAuth`）。
> - **所有者認可（BOLA）**: 他人の資源へのアクセスを `internal/authz`（`RequireDeviceOwner` 等）で弾く。`devices.user_id` とログイン中 `user_id` の一致で判定。
>
> §6.1〜§6.8 のうち spatie 固有の機構（ロール付与順序・キャッシュ・catch-all マッチング・`booted` フック）は DROP するが、各節に残る本質的教訓（「テストユーザーの状態を暗黙の自動付与に任せず明示的に固定せよ」「分離検証は専用の最小権限主体で行え」「全権バイパス経路でガードを検証するな」）は本プロジェクトの BOLA テストにそのまま転用する。

### 6.1 テスト主体（認証済み user_id）のセットアップ

> 【機構読替（ADAPT）】Laravel は `setUp()` で `Role`/`Permission`/`User::factory()` を組み立て、`View::share('errors', ...)` で Blade レンダリングの暗黙バッグを準備していた。本プロジェクトには role/permission も `View::share` の暗黙共有も無い。

本プロジェクトのハンドラテストは原則 **DB 非依存**で、認証済み状態は「ミドルウェアが `auth.SetUserID(c, id)` を済ませた状態」をルータに直接注入して再現する。`internal/handler/sensor_api_test.go` の `newRouterWithUser` がその標準形。

```go
// 認証済み (auth.SetUserID 済み) 状態を再現するルータ。
// セッション/Bearer の実機構を通さず、認可経路だけを単体で検証できる。
func newRouterWithUser(h *SensorAPI, userID int64) *gin.Engine {
    r := gin.New()
    r.POST("/api/sensor-data", func(c *gin.Context) {
        auth.SetUserID(c, userID) // ← Laravel の actingAs($user) 相当
        c.Next()
    }, h.Create)
    return r
}
```

- 「権限ありユーザー」「権限なしユーザー」という 2 主体の代わりに、**資源の所有者 `user_id`** と **テスト主体の `user_id`** の組合せで認可結果が決まる。所有者一致＝アクセス可、不一致＝拒否。
- テンプレ描画に必要だった `View::share('errors', new ViewErrorBag)` は不要。templ はエラーを view struct のフィールド（例 `LoginView{Errors map[string]string}`）で明示的に受け取るため、暗黙バッグの事前共有は存在しない（`internal/view/page/page_test.go` 参照）。

### 6.2 未認証は 302（Web）/ 401（API）、他人の資源は 403/404（BOLA）

> 【機構読替（ADAPT）】Laravel は未認証 401・権限なし 403 の 2 値だった。本プロジェクトは「Web か API か」「未認証か所有者違反か」で応答が分かれる。

| 状況 | Web ルート（HTML/HTMX） | API ルート（JSON） |
|---|---|---|
| 未認証 | 302 → `/login`（`RequireAuth`） | 401 JSON（`DeviceAuth`） |
| 認証済み・他人の資源 | 403 または 404（所有権漏洩を避けるなら 404） | 403 JSON（`authz.ErrNotOwner`） |

```go
// 未認証 (Web): /login へ 302。internal/middleware/require_auth_test.go の作法。
func TestRequireAuth_未認証は302でloginへ(t *testing.T) {
    gin.SetMode(gin.TestMode)
    r := gin.New()
    r.GET("/dash", RequireAuth(), func(c *gin.Context) { c.Status(http.StatusOK) })

    w := httptest.NewRecorder()
    r.ServeHTTP(w, httptest.NewRequest(http.MethodGet, "/dash", nil))

    if w.Code != http.StatusFound {
        t.Fatalf("未認証アクセス: got %d, want 302", w.Code)
    }
    if loc := w.Header().Get("Location"); loc != "/login" {
        t.Errorf("Location = %q, want /login", loc)
    }
}

// 認証済みだが他ユーザーのデバイス (API): 403 + 副作用なし。
// internal/handler/sensor_api_test.go の作法。
func TestSensorAPI_Create_他ユーザーのデバイスは403かつ保存しない(t *testing.T) {
    repo := &fakeSensorRepo{device: repository.Device{ID: 10, UserID: 999}}
    w := postSensorData(t, newRouterWithUser(&SensorAPI{Repo: repo}, 7))

    if got, want := w.Code, http.StatusForbidden; got != want {
        t.Errorf("status: got %d, want %d", got, want)
    }
    if repo.createCalled {
        t.Error("403 のはずが CreateSensorReading が呼ばれた (BOLA リスク)")
    }
}
```

**本質的教訓（PORT）:** 認可拒否の検証では「ステータスコード」だけでなく **「副作用が発生していないこと（DB 書込が呼ばれていないこと）」** まで固定する。`repo.createCalled == false` のアサートが BOLA の本丸。Laravel の `assertStatus(403)` だけでは、コントローラが 403 を返しつつ裏で更新していても検出できない。

### 6.3 「所有者だが境界条件」を検証するパターン

> 【機構読替（ADAPT）】原本は「individual ロールに特定権限を直付けして支社フィルタを検証」していた。本プロジェクトに支社・ロールは無いが、**「認証は通るが資源の境界で挙動が変わる」境界テスト**という構造は同型なので転用する。

最も重要な境界は **ゼロ値の取り違え**。`user_id == 0`（未認証）と資源の `user_id == 0` がうっかり「一致」と判定されると BOLA が破れる。`internal/authz/ownership_test.go` はこの境界を明示的に固定している。

```go
func TestRequireDeviceOwner_userIDがゼロなら認証前としてErrUnauthenticated(t *testing.T) {
    // device.UserID も 0 にして 0==0 で誤って所有者一致しないこと (BOLA 境界) を固定する。
    q := fakeDeviceGetter{device: repository.Device{ID: 10, UserID: 0}}

    _, err := RequireDeviceOwner(context.Background(), q, 10, 0)
    if !errors.Is(err, ErrUnauthenticated) {
        t.Errorf("userID=0: got %v, want ErrUnauthenticated", err)
    }
    if errors.Is(err, ErrNotOwner) {
        t.Error("ゼロ値の未認証を所有者判定(ErrNotOwner)へ流してはならない")
    }
}
```

**本質的教訓（PORT）:** 原本の「`branch_id` の比較は `(int)` キャストで行う（DB 値が文字列だと厳密比較が失敗する）」という注意は、Go では型が `int64` に固定されているため言語レベルで回避済み。ただし **ゼロ値の意味の二重性（「未設定」と「ID=0」）** が新たな落とし穴になるため、境界テストの焦点は型キャストからゼロ値分岐に移る。

### 6.4 認可ヘルパーの単体テストでキャッシュ汚染問題を構造的に排除する

> 【Go では該当なし（DROP）】spatie の `PermissionRegistrar` メモリキャッシュ（`forgetCachedPermissions()` を setUp 冒頭・末尾で呼ぶ必要があった問題）は、本プロジェクトに permission キャッシュ機構が無いため発生しない。

ただし転用できる教訓がある。**テスト間でグローバル/プロセス共有状態が漏れると、付与していない主体に意図しない結果が返る**という構造的問題は普遍的だ。本プロジェクトでこれを防ぐ作法は次の通り。

- 認可判定（`authz.RequireDeviceOwner`）を **引数だけで決定する純関数**に保ち、グローバルキャッシュを一切持たせない。`ownership_test.go` は毎ケース新しい `fakeDeviceGetter` を生成し、テスト間共有が原理的に起こらない。
- spatie で必要だった「setUp 冒頭と末尾の 2 回キャッシュクリア」は、本プロジェクトでは **「各 t.Run / 各テスト関数でモックを新規生成する」**という table-driven の規律で代替する。共有スライス・共有 map をテスト本体で書き換えない（イミュータブル運用）。

**本質的教訓（ADAPT）:** 「権限判定をキャッシュに依存させると検証が壊れる」→「認可判定を副作用のない純関数に閉じ込め、入力で完結させる」。これにより原本 §6.4 のデバッグ地獄（2 時間ハマる類）が設計段階で消える。

### 6.5 所有者認可ヘルパーへの集約（BOLA の単一窓口）

> 【機構読替（ADAPT）】原本は「`hasRole('individual')` ではなく `!hasRole('administrator')` で判定せよ（キャッシュ汚染で誤判定するため）」という防御的判定の指針だった。本プロジェクトに role は無いが、**「認可ロジックを各ハンドラに散らさず単一ヘルパーに集約し、そこを集中的にテストする」**という設計原則は完全に転用する。

本プロジェクトは所有者認可を `internal/authz` に集約している。各ハンドラは個別に `device.UserID == userID` を書かず、`authz.RequireDeviceOwner` を呼ぶ。エラー種別（`ErrUnauthenticated` / `ErrNotOwner` / `pgx.ErrNoRows` / 生 DB エラー）が明確に分離されており、誤分類しないことをテストで固定する。

```go
// 存在しない資源と所有権違反を取り違えない (誤分類禁止)。
func TestRequireDeviceOwner_存在しなければErrNoRowsを透過(t *testing.T) {
    q := fakeDeviceGetter{err: pgx.ErrNoRows}

    _, err := RequireDeviceOwner(context.Background(), q, 10, 7)
    if !errors.Is(err, pgx.ErrNoRows) {
        t.Errorf("存在しない: got %v, want pgx.ErrNoRows", err)
    }
    if errors.Is(err, ErrNotOwner) {
        t.Errorf("存在しないエラーを ErrNotOwner に誤分類してはならない")
    }
}
```

**ハンドラ側のマトリクステスト**（`sensor_api_test.go` の所有者一致 201 / 他人 403 / 未認証 401 / 不在 422 / DB エラー 500）が、原本の「administrator は全支社可・individual は他支社 403」テストの本プロジェクト版に相当する。所有者なら成功・非所有者なら拒否・不在なら 4xx という 3 経路を必ず両側から固定する。

**本質的教訓（PORT）:** 認可判定を 1 箇所（`internal/authz`、カバレッジ目標 100%）に集約し、各ハンドラからはそれを呼ぶだけにする。判定ロジックを各ハンドラへコピペすると、原本の「S007B には実装されたが S007A の edit には未実装だった」のような **片側だけガード漏れ**が必ず起きる。集約 + 集約点の網羅テストが唯一の防御。

### 6.6 ルーティングの認可適用範囲は前方一致でなく明示登録で固定する

> 【Go では該当なし（DROP）】`permission_routes.php` の `str_starts_with($uri, $prefix.'/')` による catch-all マッチング副作用（新プレフィックス追加で同接頭辞の既存ルート全部に権限ミドルウェアが波及する）は、本プロジェクトのルーティングに前方一致ベースのミドルウェア自動適用が無いため発生しない。

本プロジェクトの Gin ルーティングは、各ルートにミドルウェアを **明示的に**付ける（`r.GET("/devices/:device", RequireAuth(), h.Show)` のように個別指定、またはグループ単位の `r.Group("/devices", RequireAuth())`）。接頭辞文字列マッチで暗黙適用されることはない。

ただし転用できる教訓がある。**「あるルートにどのガードが効いているか」をテストで固定化する**という観点。グループにミドルウェアを足したつもりが、別のルートが意図せずガード対象（または対象外）になっていないかは、Gin でも起こりうる。

```go
// 認証必須グループに後から追加したルートも 302 ガードが効いていることを固定する。
func TestProtectedGroup_全ルートが未認証で302になる(t *testing.T) {
    gin.SetMode(gin.TestMode)
    r := gin.New()
    g := r.Group("/", RequireAuth())
    g.GET("/devices", func(c *gin.Context) { c.Status(http.StatusOK) })
    g.GET("/devices/:device/edit", func(c *gin.Context) { c.Status(http.StatusOK) })

    for _, path := range []string{"/devices", "/devices/1/edit"} {
        t.Run(path, func(t *testing.T) {
            w := httptest.NewRecorder()
            r.ServeHTTP(w, httptest.NewRequest(http.MethodGet, path, nil))
            if w.Code != http.StatusFound {
                t.Errorf("%s: got %d, want 302 (ガード漏れ)", path, w.Code)
            }
        })
    }
}
```

**本質的教訓（ADAPT）:** 「ルートのガード適用範囲は暗黙のマッチングに任せず、テストで一覧化して固定せよ」。Laravel では catch-all の波及を、Go では明示登録の付け忘れを、それぞれ table-driven のルートテストで検出する。

### 6.7 認可の分離検証では「全権バイパス主体」を使わない

> 【機構読替（ADAPT）】原本は「`administrator` / `system_administrator` ロールは全権限保有扱いなので、権限キー分離テストに使うと全ルート 200 で分離が検証できない。専用スリムロール（`w006_only` 等）を作れ」という教訓。本プロジェクトに全権ロールは無いが、**「ガードを素通りする主体で分離を検証してはならない」**という原則はそのまま生きる。

本プロジェクトで「ガードを素通りする主体」に相当するのは **資源の所有者 user_id** や **Bearer 認証済みのデバイストークン主体**。「他人の資源を拒否すること」を検証したいテストで、うっかり所有者 user_id を注入すると常に成功し、分離が検証できない。

```go
// ❌ NG: 検証対象が「他人は拒否」なのに、所有者と一致する user_id を注入している
repo := &fakeSensorRepo{device: repository.Device{ID: 10, UserID: 7}}
w := postSensorData(t, newRouterWithUser(&SensorAPI{Repo: repo}, 7)) // 7==7 で常に通過
// → これでは「他人なら 403」が検証できない

// ✅ OK: 拒否を検証したいなら必ず非所有者 user_id を注入する
repo := &fakeSensorRepo{device: repository.Device{ID: 10, UserID: 999}}
w := postSensorData(t, newRouterWithUser(&SensorAPI{Repo: repo}, 7)) // 7 != 999
```

**使い分け（PORT）:**
- **「所有者は成功 / 非所有者は拒否」の基本動作検証** → 所有者一致 user_id と非所有者 user_id の 2 ケースで十分（§6.2）。
- **「資源 A と資源 B は別の所有者に属する」の分離検証** → 各資源にそれぞれの所有者 user_id を割り当て、片方の所有者で他方へアクセスして拒否されることを確認する。Laravel の「片方のみ権限を持つスリムロール」が、本プロジェクトでは「片方のみ所有する user_id」に対応する。

### 6.8 テスト主体の状態を暗黙の自動付与に委ねず明示的に固定する

> 【Go では該当なし（DROP）】`User::factory()->create()` が `User::booted()` フックで `individual` ロールを自動付与する罠（`assignRole()` では残存するので `syncRoles([...])` で上書きせよ）は、本プロジェクトに ORM のライフサイクルフックも自動ロール付与も無いため発生しない。テストデータは sqlc の Params 構造体で全フィールドを明示生成する。

それでも本質的教訓は重要で、本プロジェクトにも形を変えて存在する。**「テスト主体の状態を、暗黙のデフォルトに任せず明示的に組み立てよ」**。本プロジェクトで暗黙のデフォルトに化ける箇所は次の通り。

- **ゼロ値の暗黙性**: Go の構造体は未指定フィールドがゼロ値（`int64` の `0`、`bool` の `false`）になる。`repository.Device{ID: 10}` と書くと `UserID` は暗黙の `0`。これが §6.3 のゼロ値境界バグの温床。テストでは**所有者を検証したいフィールドを必ず明示**する（`repository.Device{ID: 10, UserID: 7}`）。
- **実 DB テストの残存データ**: 実 DB を使う検証では、前テストの行が残ると「自分は作っていない資源」が紛れ込み、原本の「意図しないロールが混じる」と同型の事故になる。goose migrate 済みテスト DB で対象テーブルを `TRUNCATE` するか tx ロールバックで毎テスト初期化し、接続文字列で本番/開発 DB と必ず分離する。

```go
// 実 DB fixture を使う場合: 所有者を含め全フィールドを明示生成する。
// (Querier モックで足りる場合は DB 非依存を優先)
func insertDevice(t *testing.T, q *repository.Queries, userID int64) repository.Device {
    t.Helper()
    d, err := q.CreateDevice(context.Background(), repository.CreateDeviceParams{
        UserID:     userID, // ← 暗黙のデフォルトに任せず必ず指定
        Name:       "テスト用デバイス",
        MacAddress: "AA:BB:CC:DD:EE:FF",
    })
    if err != nil {
        t.Fatalf("CreateDevice: %v", err)
    }
    return d
}
```

**再発防止のデバッグ手順（ADAPT）:** 認可テストで期待と違う結果になったら、まず**テスト主体 user_id と資源の `user_id` を両方ダンプ**する（`t.Logf("userID=%d device.UserID=%d", userID, repo.device.UserID)`）。原本の「`dump($user->roles)` で意図しないロールを確認」に相当する。両者が意図せず一致（典型は両方 `0`）していれば本ケース。

---

## 7. HTMX リクエストのテスト

### 7.1 `HX-Request` ヘッダーの有無で分岐するハンドラを両系統テストする

> 【機構読替（ADAPT）】Laravel の `deleteJson()` は `Accept: application/json` を立てるが `HX-Request: true` は立てないため、`isHtmxRequest()` 分岐の HTMX 側に入らなかった。本プロジェクトのハンドラは `c.GetHeader("HX-Request") != ""` で分岐する（HTMX 実装ガイド §部分コンポーネント返却）。テストでは `req.Header.Set("HX-Request", "true")` を**明示的に**立てない限り HTMX 分岐に入らない、という点は同じ罠。

```go
// ハンドラ側 (HTMX 実装ガイドのパターン):
//   if c.GetHeader("HX-Request") != "" { 部分コンポーネントを返す } else { フルページ }

// NG: HX-Request を立てないと非 HTMX 分岐 (フルページ or リダイレクト) に入る
req := httptest.NewRequest(http.MethodDelete, "/devices/10", nil)
w := httptest.NewRecorder()
r.ServeHTTP(w, req)
// → 期待: 204 + HX-Trigger  実際: 303 リダイレクト

// OK: HX-Request を明示的に立てる
req := httptest.NewRequest(http.MethodDelete, "/devices/10", nil)
req.Header.Set("HX-Request", "true")
w := httptest.NewRecorder()
r.ServeHTTP(w, req)
// → 204 No Content + HX-Trigger ヘッダー
```

**本質的教訓（PORT）:** HTMX 分岐は「ヘッダーで切り替わる暗黙の経路」。テストでヘッダーを立て忘れると、検証したい分岐とは別経路を踏んでいることに気付けない。HTMX を検証するテストでは必ず `HX-Request` を立てる。

### 7.2 HTMX 系統と非 HTMX 系統を両方テストする

削除など HTMX/非 HTMX で挙動が異なるエンドポイントは、両パターンを table-driven または個別 t.Run で固定する。本プロジェクトの設計（HTMX 実装ガイド §7）では、HTMX は 204 + `HX-Trigger`、通常フォームはリダイレクト（`c.Redirect(http.StatusSeeOther, ...)`）。

```go
func TestDeviceDelete_両系統(t *testing.T) {
    deviceID := "10"

    t.Run("HTMX_204とHX-Trigger", func(t *testing.T) {
        r := newRouterWithUser(handlerWithDevice(deviceID, 7), 7)
        req := httptest.NewRequest(http.MethodDelete, "/devices/"+deviceID, nil)
        req.Header.Set("HX-Request", "true")
        w := httptest.NewRecorder()
        r.ServeHTTP(w, req)

        if w.Code != http.StatusNoContent {
            t.Fatalf("HTMX 削除: got %d, want 204", w.Code)
        }
        if w.Header().Get("HX-Trigger") == "" {
            t.Error("HTMX 削除で HX-Trigger ヘッダーが無い")
        }
    })

    t.Run("非HTMX_303リダイレクト", func(t *testing.T) {
        r := newRouterWithUser(handlerWithDevice(deviceID, 7), 7)
        req := httptest.NewRequest(http.MethodDelete, "/devices/"+deviceID, nil)
        w := httptest.NewRecorder()
        r.ServeHTTP(w, req)

        if w.Code != http.StatusSeeOther {
            t.Fatalf("通常削除: got %d, want 303", w.Code)
        }
        if loc := w.Header().Get("Location"); loc != "/devices" {
            t.Errorf("Location = %q, want /devices", loc)
        }
    })
}
```

**本質的教訓（PORT）:** 1 つのエンドポイントが 2 系統の応答契約を持つなら、両系統を別ケースで固定する。片方しか書かないと、もう片方の回帰（例: 非 HTMX のリダイレクト先変更）に気付けない。

### 7.3 `HX-Trigger` ヘッダーの検証（存在確認とイベント名・複数イベント）

> 【機構読替（ADAPT）】Laravel は `$response->headers->get('HX-Trigger')` + `assertStringContainsString`。本プロジェクトは `w.Header().Get("HX-Trigger")` + `strings.Contains`。単純文字列なら `Contains`、複数イベント（JSON 形式）なら `json.Unmarshal` で構造検証する。

```go
// 単純なイベント名: 文字列で存在確認
trigger := w.Header().Get("HX-Trigger")
if trigger == "" {
    t.Fatal("HX-Trigger ヘッダーが無い")
}
if !strings.Contains(trigger, "device-list-refresh") {
    t.Errorf("HX-Trigger に device-list-refresh が無い: %q", trigger)
}

// 複数イベント (JSON 形式 {"modal-close":null,"device-list-refresh":null}):
// 文字列 Contains だとキー順序やフォーマット差で脆くなるため、Unmarshal して構造検証する
var events map[string]any
if err := json.Unmarshal([]byte(w.Header().Get("HX-Trigger")), &events); err != nil {
    t.Fatalf("HX-Trigger が JSON でない: %q (%v)", trigger, err)
}
if _, ok := events["modal-close"]; !ok {
    t.Error("HX-Trigger に modal-close イベントが無い")
}
if _, ok := events["device-list-refresh"]; !ok {
    t.Error("HX-Trigger に device-list-refresh イベントが無い")
}
```

**本質的教訓（PORT）:** 複数イベントを `HX-Trigger` に載せる場合、JSON 文字列のキー順序やスペースは保証されない。`strings.Contains` での部分一致は脆い。`json.Unmarshal` して**キーの存在**を検証するのが堅牢。Go の `encoding/json` でマップを Marshal するとキー順序がソートされるが、それに依存せず構造でアサートする。

### 7.4 HTMX フォームのバリデーションエラーは 422 + 部分コンポーネントを検証する

> 【機構読替（ADAPT）】Laravel では `HX-Request` だけでは `ValidationException` ハンドラが `expectsJson()` で判定するため 302 になり、422 を得るには `Accept: application/json` も必要、という**フレームワーク側の罠**があった。本プロジェクトには `ValidationException` の暗黙ハンドラが無く、バリデーションは Handler 内で `c.ShouldBind` し、`c.Status(http.StatusUnprocessableEntity)` を**明示的に呼んでから**部分コンポーネントを `Render` する（HTMX 実装ガイド §7）。よって 422 を得るのに `Accept` ヘッダーは不要で、`HX-Request` だけで決まる。

```go
// HTMX フォームのバリデーションエラー: 422 + フォーム部分コンポーネント (エラー文言入り)
func TestAlertRuleAdd_HTMX_不正値で422とエラー文言(t *testing.T) {
    r := newRouterWithUser(&AlertRuleHandler{Repo: &fakeAlertRepo{
        device: repository.Device{ID: 10, UserID: 7},
    }}, 7)

    // threshold を非数値にしてバリデーション失敗を誘発
    form := strings.NewReader("metric=temperature&operator=>&threshold=not-a-number")
    req := httptest.NewRequest(http.MethodPost, "/devices/10/alert-rules", form)
    req.Header.Set("Content-Type", "application/x-www-form-urlencoded")
    req.Header.Set("HX-Request", "true")
    w := httptest.NewRecorder()
    r.ServeHTTP(w, req)

    if w.Code != http.StatusUnprocessableEntity {
        t.Fatalf("HTMX バリデーション失敗: got %d, want 422, body=%s", w.Code, w.Body.String())
    }
    body := w.Body.String()
    // 部分コンポーネントのみ (レイアウトの <html> を含まない) であること
    if strings.Contains(body, "<html") {
        t.Error("422 でフルページが返っている (部分コンポーネントを返すべき)")
    }
    // エラー文言がインライン (.error-message) で含まれること
    if !strings.Contains(body, "error-message") {
        t.Error("エラー文言クラス error-message が含まれていない")
    }
}
```

**§7.1 との違い（ADAPT）:** §7.1 は「`HX-Request` 分岐に入るか（Handler 側の分岐）」の問題。本件は「バリデーション失敗時の応答契約（422 + 部分コンポーネント）」の検証。Laravel では追加で `Accept: application/json` が要る場面だったが、本プロジェクトは Handler が明示的にステータスとレンダリングを独立制御するため、**`HX-Request` だけで 422 が決まる**。テストも `HX-Request` のみで足り、`Accept` 偽装は不要。

**本質的教訓（PORT）:** バリデーション失敗時の応答は「ステータス 422」「レイアウトを含まない部分コンポーネント」「インラインのエラー文言」の 3 点を同時に固定する。1 つでも欠けると、HTMX の `htmx.config.responseHandling`（422 を swap 対象に含める設定）と組み合わさったときにエラー表示が壊れる。

### 7.5 `hx-swap` 属性（`outerHTML` 等）を出力 HTML 内で固定化する

> 【機構読替（ADAPT）】原本は PHPUnit で `$response->content()` に対し正規表現で `hx-target` + `hx-swap="outerHTML"` の同居を検証していた。本プロジェクトは templ コンポーネントを `Render` して得た HTML を `strings.Contains`（または `regexp`）で検証する。`afterSwap` 等の JS 挙動はテストでは検証できないが、**サーバーが返す断片の `hx-swap` 属性を固定化する**ことで後工程の JS 破綻を未然に検出する点は同じ。

```go
// 編集ボタンの行は outerHTML swap で tr を置き換える契約を固定する。
func TestDeviceRow_編集ボタンがouterHTMLswap(t *testing.T) {
    html := render(t, component.DeviceRow(repository.Device{ID: 10, UserID: 7}))

    // hx-target と hx-swap="outerHTML" が揃っていること
    if !strings.Contains(html, `hx-target="#device-row-10"`) {
        t.Errorf("hx-target=#device-row-10 が無い:\n%s", html)
    }
    if !strings.Contains(html, `hx-swap="outerHTML"`) {
        t.Errorf("hx-swap=outerHTML が無い (tr 置換が壊れる):\n%s", html)
    }

    // 属性の同居を厳密に固定したい場合は正規表現で順序非依存に検証する
    matched, _ := regexp.MatchString(
        `hx-target="#device-row-10"[^>]*hx-swap="outerHTML"`, html)
    if !matched {
        t.Error("編集ボタンは outerHTML swap で tr を置き換える必要がある")
    }
}
```

**MCP 実機検証と組み合わせる（ADAPT）:**

- templ Render テスト: `hx-swap` 属性の値を固定化（CI で回るユニット層）。
- MCP chrome-devtools 検証（開発時の目視）: 実 swap 後に DOM/Alpine.js の状態が期待通りか確認。原本の TomSelect 再初期化チェックに相当する観点は、本プロジェクトでは Alpine.js コンポーネント（`x-data`）が `afterSwap` 後に再初期化されるかの確認に読み替える。

```javascript
// chrome-devtools MCP で outerHTML swap 後の Alpine.js 再初期化を確認
const row = document.querySelector('tr[id^="device-row-"]');
({
    hasAlpineData: !!row?.__x,                                // Alpine が初期化済みか
    rowAttached:   document.body.contains(row),              // detach されていないか
});
```

> 【部分的に Go 非該当（DROP）】原本の Tom Select（`select.tomselect` / `ts-wrapper`）固有の検証は、本プロジェクトが Tom Select を採用していないため該当しない。

**本質的教訓（PORT）:** `outerHTML` swap では `event.detail.target` が DOM から detach され、`afterSwap` ハンドラが空走しうる。共通レイアウト（`internal/view/layout/App.templ`）の `htmx:afterSwap` ハンドラで `document.body.contains(target)` フォールバックが効いているかを実機で確認する。サーバー側テストでは `hx-swap` 属性を固定し、クライアント側の再初期化は MCP 実機検証で補完する、という二層構えを取る。

---

## 8. 連鎖論理削除のテスト

本プロジェクトの論理削除は全テーブルの `deleted_at timestamptz NULL`（`NULL = 有効`）で表現する。sqlc の各クエリは `WHERE id = $1 AND deleted_at IS NULL` を共通条件に持ち、`SoftDeleteDevice` 等は `SET deleted_at = NOW()` を発行する（`db/queries/devices.sql` / `alert_rules.sql` 参照）。Laravel の `assertSoftDeleted('table', [...])` に相当する単一ヘルパは存在しないため、検証は (a) Querier モックで「論理削除メソッドが正しい id 順序で呼ばれたか」を、(b) 実 DB テストで「`deleted_at IS NOT NULL` になったか」を確認する2層で行う。

### 8.1 論理削除の検証は「呼び出し記録」または「再取得結果」で行う（ADAPT）

> Laravel では `$this->assertSoftDeleted('devices', ['id' => $id])` 一行。Go では sqlc が PK + `deleted_at IS NULL` を WHERE に持つので、**論理削除後に GetXxx が `pgx.ErrNoRows` を返すこと**が「論理削除済み」の同義になる。

DB 非依存（既定）では、Querier モックに呼び出し履歴を記録させて検証する。

```go
// fakeDeleteQuerier は連鎖論理削除の「どの id を、どの順序で」消したかを記録する。
type fakeDeleteQuerier struct {
	repository.Querier
	softDeletedRules     []int64 // SoftDeleteAlertRule に渡った id
	softDeletedDevices   []int64 // SoftDeleteDevice に渡った id
	softDeletedHistories []int64
}

func (f *fakeDeleteQuerier) SoftDeleteAlertRule(_ context.Context, id int64) error {
	f.softDeletedRules = append(f.softDeletedRules, id)
	return nil
}

func (f *fakeDeleteQuerier) SoftDeleteDevice(_ context.Context, id int64) error {
	f.softDeletedDevices = append(f.softDeletedDevices, id)
	return nil
}
```

実 DB テスト（goose migrate 済み・対象テーブルを TRUNCATE してから INSERT）では、削除後に再取得して `pgx.ErrNoRows` を確認する。

```go
// 実 DB テスト: 論理削除後は GetDevice が ErrNoRows を返す (deleted_at IS NULL で弾かれる)
_, err := q.GetDevice(ctx, deviceID)
if !errors.Is(err, pgx.ErrNoRows) {
	t.Errorf("論理削除後の GetDevice: got %v, want pgx.ErrNoRows", err)
}
```

### 8.2 連鎖削除は子→親の順序で検証する（ADAPT）

デバイス削除時は子（`alert_rules` → さらにその子 `alert_histories`）を先に論理削除し、最後に親（`devices`）を削除する。Laravel と同様、検証順は実装の意図を示すため子→親で並べると読みやすい。Querier モックなら**呼び出し順序まで**固定できるのが Go の強み。

```go
func TestDeleteDevice_子から親の順で論理削除する(t *testing.T) {
	q := &fakeDeleteQuerier{}
	svc := NewDeviceService(q)

	if err := svc.DeleteDeviceCascade(context.Background(), 10); err != nil {
		t.Fatalf("DeleteDeviceCascade: %v", err)
	}

	// 子 (alert_histories → alert_rules) が親 (devices) より先に消えていること
	assertDeleted(t, "alert_histories", q.softDeletedHistories, 100)
	assertDeleted(t, "alert_rules", q.softDeletedRules, 50)
	assertDeleted(t, "devices", q.softDeletedDevices, 10)
}
```

> ※ 順序を厳密にアサートしたい場合は、各 SoftDelete モックに連番カウンタ（`f.seq++` した値を記録）を持たせ、`historySeq < ruleSeq < deviceSeq` を検証する。本質的教訓（子→親順で削除し、外部キーを張らない設計でも孤児レコードを残さない）は PORT。

### 8.3 子エンティティがないケースもテストする（PORT）

ビジネスロジック上、子が存在しないパターン（例: アラートルールを一度も設定していないデバイスの削除）も必ずテストする。nil/空スライスに対する論理削除ループが安全に skip されることを固定し、「子がいないと panic / 余計な DELETE が走る」リグレッションを防ぐ。

```go
func TestDeleteDevice_子ルールが無くても親を削除できる(t *testing.T) {
	q := &fakeDeleteQuerier{} // alert_rules を1件も用意しない
	svc := NewDeviceService(q)

	if err := svc.DeleteDeviceCascade(context.Background(), 10); err != nil {
		t.Fatalf("子なしデバイス削除: %v", err)
	}
	if len(q.softDeletedRules) != 0 {
		t.Errorf("子ルールが無いのに SoftDeleteAlertRule が呼ばれた: %v", q.softDeletedRules)
	}
	assertDeleted(t, "devices", q.softDeletedDevices, 10) // 親は削除される
}
```

HTMX の DELETE で「中身なし」を返す設計なら、ハンドラ側は `c.Status(http.StatusNoContent)`（Laravel の `assertNoContent` 相当）または削除後の一覧コンポーネント再描画を返す。どちらにするかは画面要件次第（HTMX実装ガイド C06「削除確認モーダル → hx-delete」参照）。

---

## 9. 検証用コードの削除とテスト影響

### 9.1 検証用ハンドラ/ルート削除時の手順（ADAPT）

> Laravel では Controller / Blade / web.php ルート / テストの4点を消す。Go では **handler（`internal/handler/*.go`）・templ（`internal/view/**/*.templ` と生成物 `*_templ.go`）・ルート登録（`cmd/server` のルータ組み立て）・同居テスト（`*_test.go`）** が対象。

プロトタイプの検証用ハンドラを本実装へ置き換えたら、以下の順で削除する。

1. **機能重複の確認**: 削除対象ハンドラの全メソッドが新ハンドラでカバーされていること
2. **削除対象を特定**: 参照を grep で洗い出す
3. **一括削除を実行**（templ は `.templ` と生成物 `_templ.go` の両方を消す）
4. **ビルド + 全テストで影響を確認**

```bash
# 1. 参照箇所を洗い出す（型名・ルートパス両方）
grep -rn "ProbeHandler\|/probe" /Users/c/Desktop/dev/go_iot --include="*.go"

# 2. 削除（templ は .templ と生成物の両方）
rm /Users/c/Desktop/dev/go_iot/internal/handler/probe.go
rm /Users/c/Desktop/dev/go_iot/internal/handler/probe_test.go
rm /Users/c/Desktop/dev/go_iot/internal/view/page/probe.templ
rm /Users/c/Desktop/dev/go_iot/internal/view/page/probe_templ.go
# cmd/server のルータからルート登録行を手動削除

# 3. ビルド + 全テスト（コンパイルが通ることが第一関門）
go build ./... && go test ./...
```

> 【Laravel との差】Go は静的型なので、削除漏れ（存在しない関数への参照）は**実行前に `go build` がコンパイルエラーで弾く**。Laravel の「ルート登録時にランタイムで初めて落ちる」事故が起きにくい。逆に言えば、ビルドが通れば参照は確実に消えている。

### 9.2 削除後のテスト失敗の切り分け（ADAPT）

`go test ./...` は無関係な既存失敗も出る。切り分けは「削除した識別子名で grep」する。Laravel の `--compact` 相当は標準では無いが、`-run` で対象を絞れる。

```bash
# 削除した型/関数名に紐づく失敗だけを抽出
go test ./... 2>&1 | grep -i "ProbeHandler"
# → 出力なし = 削除関連の失敗はない

# 全体の失敗概要だけ見る
go test ./... 2>&1 | grep -E "^(FAIL|--- FAIL)" | head -10

# 特定パッケージ/テストだけ再実行（無関係失敗を排除して判断）
go test ./internal/handler/ -run 'Device' -v
```

**重要**: 全体で `FAIL` が複数出ても、削除した識別子名・ファイルパスがエラーに含まれていなければ、削除は安全に完了している。判断材料はあくまで「ビルドが通る」＋「削除識別子が失敗ログに出ない」の2点。

### 9.3 ルート登録の削除漏れに注意（ADAPT）

> Laravel の `$webRoutes` 配列 + 明示 `Route::delete()` の二重定義に相当するのは、本プロジェクトでは **ルータ組み立て箇所の `r.GET/POST/...` 行と、それを束ねるグループ登録**。

`cmd/server` のルータ構築や `newHTTPHandler` 系ヘルパでルートを登録しているため、ハンドラを消したら登録行も消す。Go の場合、削除漏れ（消した関数 `h.Probe` をルートが参照）は `go build` で即コンパイルエラーになるので発見は容易。テスト用ルータ（`internal/handler/auth_test.go` の `newAuthApp`、`sensor_api_test.go` の `newRouter`）にも同じルートを書いていることがあるので、**本番ルータとテストルータの両方**を確認する。

```go
// 本番ルータ（cmd/server）から削除する
web.GET("/probe", h.Probe) // ← 削除

// テストルータ（internal/handler/*_test.go の newXxx ヘルパ）にも残っていないか確認
```

---

## 10. 条件付き必須バリデーション（`required_with`）のテスト

> Laravel の `required_with:field1,field2` に直接対応する binding タグは無い。go-playground/validator では `required_with`/`required_without`/`required_with_all` が同名で使えるが、**「グループのどれか1つでも入っていたら全員必須、全部空なら丸ごと任意」** のような分岐は、struct タグだけで表現しきれないことが多い。本プロジェクトでは「タグで書ける範囲はタグ、書けない条件はハンドラ/サービスの明示分岐」に読み替える。

### 10.1 テストすべき3パターン（ADAPT）

例として「アラートルールはフォームで任意設定だが、`metric` を指定したら `operator` と `threshold` も必須」というグループ必須を考える。validator のタグは次のように書ける。

```go
type AlertRuleForm struct {
	Metric    string  `form:"metric"    binding:"omitempty,oneof=temperature humidity"`
	Operator  string  `form:"operator"  binding:"required_with=Metric,omitempty,oneof=> < >= <="`
	Threshold float64 `form:"threshold" binding:"required_with=Metric"`
}
```

検証すべき3パターンを table-driven で固定する。Web フォームのバリデーション失敗は **422 + HTML 再描画**（または設計次第で 200 再描画）、成功は 303 リダイレクトという本プロジェクトの方針に合わせる。

```go
func TestAlertRuleForm_条件付き必須の3パターン(t *testing.T) {
	cases := []struct {
		name      string
		form      url.Values
		wantCode  int      // 422 = 検証失敗(再描画), 303 = 保存成功
		wantErrFields []string // 422 時に再描画 HTML に出る日本語エラー対象
	}{
		{
			name: "一部入力 → グループ必須エラー",
			form: url.Values{"metric": {"temperature"}}, // operator/threshold 未入力
			wantCode: http.StatusUnprocessableEntity,
			wantErrFields: []string{"operator", "threshold"},
		},
		{
			name: "全空 → 検証通過 (ルール無し扱いで保存)",
			form: url.Values{}, // metric も空 → required_with 発火せず
			wantCode: http.StatusSeeOther,
		},
		{
			name: "全必須入力 → 検証通過 (保存成功)",
			form: url.Values{"metric": {"temperature"}, "operator": {">"}, "threshold": {"30.0"}},
			wantCode: http.StatusSeeOther,
		},
	}

	for _, tc := range cases {
		t.Run(tc.name, func(t *testing.T) {
			app := newAlertApp(&fakeAlertRepo{})
			w := postForm(app, "/alerts/rules", tc.form)

			if w.Code != tc.wantCode {
				t.Fatalf("status: got %d, want %d (body=%s)", w.Code, tc.wantCode, w.Body.String())
			}
			for _, f := range tc.wantErrFields {
				if !strings.Contains(w.Body.String(), japaneseErr(f)) {
					t.Errorf("再描画 HTML にフィールド %q のエラーが含まれない", f)
				}
			}
		})
	}
}
```

> パターン2「全空で通過」は Laravel の「論理削除パス」に相当する。本プロジェクトでは「アラートルール未設定として扱う（既存ルールがあれば `SoftDeleteAlertRule`）」というサービス側分岐で再現する。**validator のタグだけでこの分岐は書けない**ため、`form == 全空` の判定はハンドラ/サービスで明示し、その分岐自体も上記テストの「全空 → 303」でカバーする。

### 10.2 日本語バリデーションメッセージの検証（ADAPT）

> Laravel の `session('errors')->first('estimate_date')` に相当する仕組みは無い。本プロジェクトはエラーを **view struct のフィールド**（例 `Errors map[string]string`）で運び、`internal/handler/auth_validation.go` の `field + tag → 日本語` マップで文言を生成する（`auth_validation_test.go` 参照）。

検証は2段で行う。(1) マップ関数の単体テスト（`validationMessage(field, tag)` が正しい日本語を返す）、(2) ハンドラ経由で再描画 HTML に文言が出ること。

```go
// (1) マップ関数単体（auth_validation_test.go と同じ作法）
func TestAlertValidationMessage(t *testing.T) {
	tests := []struct{ field, tag, want string }{
		{"operator", "required_with", "比較演算子を入力してください"},
		{"threshold", "required_with", "閾値を入力してください"},
		{"metric", "oneof", "指標の値が不正です"},
	}
	for _, tt := range tests {
		if got := validationMessage(tt.field, tt.tag); got != tt.want {
			t.Errorf("validationMessage(%q,%q) = %q, want %q", tt.field, tt.tag, got, tt.want)
		}
	}
}

// (2) 再描画 HTML に日本語エラーが埋まること（templ の view struct 経由）
func TestAlertRulePost_検証失敗で再描画HTMLに日本語エラー(t *testing.T) {
	app := newAlertApp(&fakeAlertRepo{})
	w := postForm(app, "/alerts/rules", url.Values{"metric": {"temperature"}})
	if w.Code != http.StatusUnprocessableEntity {
		t.Fatalf("status = %d, want 422", w.Code)
	}
	if !strings.Contains(w.Body.String(), "閾値を入力してください") {
		t.Error("422 再描画 HTML に閾値のグループ必須エラーが出ていない")
	}
}
```

---

## 11. ファイルダウンロードのテスト

> 【Go では大幅に読替】本プロジェクトに現状ファイルアップロード/ダウンロード機能は無く、`Storage::fake()` 相当の抽象も無い。将来 CSV/画像エクスポート等を実装する場合に備え、「Gin の `c.Data` / `c.Stream` でレスポンスヘッダ + 本文を検証する」作法として教訓を残す（PORT できる本質: ファイル名・404・Content-Type をテストで固定する）。

### 11.1 ダウンロードレスポンスのヘッダと本文を検証する（ADAPT）

> Laravel の `Storage::fake('local')` + `assertDownload('テスト.pdf')` は、Go では「ストレージを `interface` でモックし、ハンドラが返す `Content-Disposition` / `Content-Type` / body をアサート」に置き換える。

```go
// FileStore はダウンロード対象を抽象化する DB ポート相当の interface。
// 実装は infra (ローカルFS/S3 等)、テストはこのモックを差し込む。
type FileStore interface {
	Open(ctx context.Context, path string) (io.ReadCloser, error)
}

type fakeFileStore struct {
	files map[string][]byte // path → 中身。存在しない path は ErrNotExist
}

func (f fakeFileStore) Open(_ context.Context, path string) (io.ReadCloser, error) {
	b, ok := f.files[path]
	if !ok {
		return nil, fs.ErrNotExist
	}
	return io.NopCloser(bytes.NewReader(b)), nil
}

func TestDownload_ヘッダとファイル名と本文(t *testing.T) {
	store := fakeFileStore{files: map[string][]byte{
		"attachments/test.pdf": []byte("dummy PDF content"),
	}}
	app := newFileApp(store, &fakeFileMetaRepo{
		meta: fileMeta{ID: 1, Path: "attachments/test.pdf", DisplayName: "テスト見積.pdf"},
	})

	w := getWithCookies(app, "/files/1/download", authedCookies(t, app))

	if w.Code != http.StatusOK {
		t.Fatalf("status = %d, want 200", w.Code)
	}
	// assertDownload('テスト見積.pdf') 相当: Content-Disposition のファイル名
	cd := w.Header().Get("Content-Disposition")
	if !strings.Contains(cd, "attachment") || !strings.Contains(cd, "テスト見積.pdf") {
		t.Errorf("Content-Disposition = %q, want attachment + 日本語ファイル名", cd)
	}
	if got := w.Body.String(); got != "dummy PDF content" {
		t.Errorf("body = %q, want ダウンロード対象の中身", got)
	}
}
```

> ※ 日本語ファイル名は `Content-Disposition: attachment; filename*=UTF-8''...`（RFC 5987）でエンコードして出すのが安全。テストでは元の日本語が（生／パーセントエンコード後の）いずれかで含まれることを検証する。ハンドラ側で `url.PathEscape` する場合はテストもそれに合わせる。

### 11.2 メタはあるが実体が無いケースは404（PORT）

DB（メタ）にレコードはあるがストレージに実体が無い場合の 404。`fakeFileStore` に当該 path を登録しないことで `fs.ErrNotExist` を誘発し、ハンドラが 404 にマップすることを固定する。

```go
func TestDownload_実体が無ければ404(t *testing.T) {
	store := fakeFileStore{files: map[string][]byte{}} // Open は ErrNotExist
	app := newFileApp(store, &fakeFileMetaRepo{
		meta: fileMeta{ID: 1, Path: "nonexistent.pdf", DisplayName: "x.pdf"},
	})

	w := getWithCookies(app, "/files/1/download", authedCookies(t, app))
	if w.Code != http.StatusNotFound {
		t.Errorf("status = %d, want 404", w.Code)
	}
}
```

> 本質的教訓（PORT）: 「メタの存在」と「実体の存在」は別レイヤなので両方テストする。所有者認可も忘れず併せる（他人のファイル meta を指定したら 404/403 = §16 / BOLA）。

---

## 12. `<fieldset disabled>` のテスト

### 12.1 templ の disabled 制御をテストする（ADAPT）

> Laravel の `assertSee('<fieldset disabled', false)` は、templ では「コンポーネントを `render(t, Comp(view))` で文字列化 → `strings.Contains` で `disabled` 属性の有無を検証」に置き換える。`false`（エスケープ無し生比較）の概念は不要で、Go は常に出力 HTML をそのまま比較する。

templ では disabled を view struct のフラグ（例 `ReadOnly bool`）で制御し、`if v.ReadOnly { ... disabled ... }` で属性を出し分ける設計にする。テストはコンポーネント単体 Render で両分岐を固定する（`component_test.go` の `render`/`assertContains` ヘルパと同作法）。

```go
func TestDeviceForm_読取専用ならfieldsetがdisabled(t *testing.T) {
	// 読取専用モード: fieldset に disabled が付く
	roHTML := render(t, DeviceForm(DeviceFormView{ReadOnly: true}))
	assertContains(t, roHTML, "<fieldset disabled")

	// 編集モード: disabled が付かない（素の <fieldset>）
	editHTML := render(t, DeviceForm(DeviceFormView{ReadOnly: false}))
	assertContains(t, editHTML, "<fieldset>")
	if strings.Contains(editHTML, "<fieldset disabled") {
		t.Error("編集モードなのに fieldset disabled が出力された")
	}
}
```

> 否定検証（`assertDontSee('<fieldset disabled')` 相当）は `if strings.Contains(...) { t.Error }` で書く。文字列 `<fieldset disabled` は十分長く一意なので §16.2 のサブストリング誤マッチ罠は起きにくいが、短い属性名を否定する場合は正規表現の語境界（後述 §16.2）を使う。

### 12.2 個別フォーム要素の disabled 制御をテストする（ADAPT）

> Laravel の `assertMatchesRegularExpression('/name="estimate_type_id"[^>]*disabled/', $content)` は、Go なら `regexp.MustCompile(...).MatchString(html)` でそのまま移植できる。

`<fieldset disabled>` で一括 disable せず、特定の `<select>`/`<input>` だけを個別に disable する場合は、`name="..."` と同じ要素内に `disabled` があるかを正規表現で確認する。

```go
func TestDeviceForm_特定selectのdisabled制御(t *testing.T) {
	re := regexp.MustCompile(`name="metric"[^>]*disabled`)

	// 読取専用: metric の select に disabled が付く
	if !re.MatchString(render(t, DeviceForm(DeviceFormView{ReadOnly: true}))) {
		t.Error("読取専用なのに name=\"metric\" の要素に disabled が無い")
	}
	// 編集: disabled が付かない
	if re.MatchString(render(t, DeviceForm(DeviceFormView{ReadOnly: false}))) {
		t.Error("編集モードなのに name=\"metric\" の要素に disabled が付いている")
	}
}
```

> ※ HTMX で部分返却するフォーム（例: 編集モーダル）の場合は、ハンドラ経由（`httptest` + gin）で `HX-Request: true` を付けて取得した部分 HTML に対しても同じ正規表現を当てると、view struct の組み立て漏れまで検出できる。

---

## 13. Alpine.js / Tom Select のクライアントサイドロジック

### 13.1 go test で検証できる範囲（PORT）

Alpine.js（本プロジェクトの動的 UI 制御）や Tom Select のクライアント挙動は、`go test`（httptest + templ Render）では実行されない。検証可能なのは **サーバが返す HTML に正しい JS コード/属性/JSON が埋め込まれているか** のみ。`strings.Contains`（Laravel の `assertSee` 相当）で固定する。

```go
func TestDevicePage_AlpineとJSONマッピングが埋め込まれる(t *testing.T) {
	html := render(t, DevicePage(view))

	// Alpine.js のディレクティブ存在
	assertContains(t, html, `x-data`)
	assertContains(t, html, `@change`)
	// サーバから注入する JSON マッピング（例: metric→ラベル）
	assertContains(t, html, `"temperature":`)
	assertContains(t, html, `"humidity":`)
	// Tom Select 対象クラス（§13.3 参照）
	assertContains(t, html, `js-tom-select`)
}
```

### 13.2 go test では検証できない範囲（PORT）

以下はブラウザ実行が要るため、開発時は **MCP chrome-devtools で目視**、将来の自動 E2E は **chromedp / Playwright**（現状不使用）でのみ検証可能。

- Tom Select の実操作（クリック・選択・検索絞り込み）
- Alpine.js の `@change` / `x-on` イベント伝播
- HTMX swap 後の Tom Select 再初期化（破棄→再生成。HTMX実装ガイド C12）
- `x-show` / `x-cloak` による表示切替、モーダル開閉アニメーション

> 本質的教訓（PORT）: 「サーバ責務（HTML/JSの中身）」と「ブラウザ責務（DOM操作の実挙動）」を分け、前者は go test で網羅、後者は E2E に委ねる。go test で `x-show` の**存在**は確認できても**実際に隠れるか**は確認できない、という境界を意識する。

### 13.3 Tom Select 対象クラスの strings.Contains 検証時の注意（ADAPT）

> Laravel の `tom-select` vs `js-tom-select` 混同罠は、本プロジェクトでもそのまま当てはまる。グローバル初期化（HTMX実装ガイド C12 / TS-6）は `select.js-tom-select` セレクタで対象を探すため、templ 側のクラスとテストの検証文字列を `js-tom-select` に統一する。

```go
// ❌ 誤: グローバル初期化セレクタ (select.js-tom-select) に一致しないクラス
assertContains(t, html, `class="form-select tom-select"`)

// ✅ 正: js- プレフィックス付きで統一
assertContains(t, html, `js-tom-select`)
```

**チェックポイント:**
- グローバル初期化は `select.js-tom-select` セレクタで検索する（HTMX実装ガイド C12 / TS-6）
- templ の `<select>` クラスを変えたら、テストの `assertContains` 文字列も併せて更新する
- カスケード（連動）で返す部分コンポーネントの `<select>` クラスも `js-tom-select` で統一する
- 独自クラスの新設は禁止（HTMX実装ガイド §31「独自クラス禁止」/ モック→本番 CSS 単一ソース運用）。モックの class をそのまま写経する

### 13.4 Alpine.js `x-data` 変数の配置移動と検証への影響（ADAPT）

> Laravel の「共通 Blade コンポーネントへ x-data 変数を昇格させると assertSee 対象が変わる」は、本プロジェクトでは「共通 templ コンポーネント（例: モーダル枠 `ModalShell`）へ Alpine 変数を昇格させると、`strings.Contains` の検証スコープが変わる」に対応する。

`dirty`（フォーム変更検知フラグ）のような Alpine 変数を子（フォーム本体）から親（モーダル枠）へ移すと、出力 HTML 内のどのコンポーネントから当該文字列が出るかが変わる。**ページ全体を Render すれば文字列としては引き続き含まれる**が、コンポーネント単体テスト（`render(t, FormBody(...))`）では含まれなくなる点に注意。

```go
// 移動前: フォーム本体コンポーネント単体に dirty があった
assertContains(t, render(t, FormBody(v)), "dirty: false")

// 移動後: dirty はモーダル枠へ昇格。フォーム本体単体には出ない
//   → ページ全体（枠 + 本体）の Render で検証する
assertContains(t, render(t, FormModal(v)), "dirty: false")     // 枠側で出力
assertContains(t, render(t, FormBody(v)), `@input="dirty = true"`) // 本体側に残るのは入力ハンドラのみ
```

**チェックポイント:**
- 共通コンポーネントへ変数を昇格したら、検証文字列が「どのコンポーネントから出るか」を意識してテストの Render 対象を選ぶ
- 複数の x-data プロパティを1回の `assertContains` でまとめて検証する場合、**同一スコープ（同一コンポーネント）のプロパティだけ**を連結文字列にする（昇格でスコープが割れた瞬間に壊れる）

---

## 14. 楽観的ロック（`updated_at` ベース）のテスト

> 【スキーマ整合の前提】現行スナップショット（`docs/database_snapshot/table_definitions.md`）に `sync_token` / `version` カラムは存在しない。全テーブルが `updated_at timestamptz NOT NULL DEFAULT now()` を持つため、本プロジェクトで楽観ロックを実装するなら **`updated_at` を WHERE 条件に入れた UPDATE の `RowsAffected() == 0` で競合検出**する（Laravel の `sync_token`＝`updated_at` のタイムスタンプ運用と本質同型）。ここでは「将来そう実装する」前提でパターンを示す。

### 14.1 競合検出テスト（ADAPT）

楽観ロック対応の更新クエリは、クライアントが持つ `updated_at`（取得時点の値）を WHERE に加える。sqlc では `:execrows` で `RowsAffected` を返すようにし、`0` 件なら競合（他者が先に更新 → `updated_at` が一致しない）として 409 を返す。

```sql
-- db/queries/alert_rules.sql （楽観ロック版の例）
-- name: UpdateAlertRuleOptimistic :execrows
UPDATE alert_rules
   SET metric    = $2,
       operator  = $3,
       threshold = $4,
       updated_at = NOW()
 WHERE id = $1
   AND deleted_at IS NULL
   AND updated_at = sqlc.arg(expected_updated_at);
```

```go
// fakeOptimisticRepo は RowsAffected を差し替えて競合/成功を再現する。
type fakeOptimisticRepo struct {
	repository.Querier
	rowsAffected int64 // 1 = 成功, 0 = 競合
}

func (f fakeOptimisticRepo) UpdateAlertRuleOptimistic(
	_ context.Context, _ repository.UpdateAlertRuleOptimisticParams,
) (int64, error) {
	return f.rowsAffected, nil
}

func TestUpdateAlertRule_古いタイムスタンプは409(t *testing.T) {
	app := newAlertApp(fakeOptimisticRepo{rowsAffected: 0}) // 競合 = 0 件更新
	form := url.Values{
		"metric":    {"temperature"},
		"operator":  {">"},
		"threshold": {"30.0"},
		"sync_token": {"1700000000"}, // 古い updated_at の epoch
	}
	w := postForm(app, "/alerts/rules/1", form)
	if w.Code != http.StatusConflict {
		t.Errorf("古い sync_token: status = %d, want 409", w.Code)
	}
}
```

> 実 DB テストでは、`pgx` の `CommandTag.RowsAffected()` を直接確認する。実装側で `tag.RowsAffected() == 0` を 409 にマップしていることを、二者更新シナリオ（A が取得 → B が先に更新 → A が古い `updated_at` で更新 → 0 件）で固定する。

### 14.2 `sync_token` 未送信時のバイパス（ADAPT）

新規登録直後の初回更新等で `sync_token`（= 期待する `updated_at`）が未送信の場合、ロック検証をスキップして保存を許可する設計が要る。ハンドラで「`sync_token` が空なら `updated_at` 条件を付けない通常 UPDATE に切替」する分岐を入れ、その分岐自体をテストする。

```go
func TestUpdateAlertRule_sync_token未送信はバイパスして成功(t *testing.T) {
	app := newAlertApp(fakeOptimisticRepo{rowsAffected: 1})
	form := url.Values{
		"metric":    {"temperature"},
		"operator":  {">"},
		"threshold": {"30.0"},
		// sync_token を付けない → ロック検証スキップ
	}
	w := postForm(app, "/alerts/rules/1", form)
	if w.Code != http.StatusSeeOther { // 保存成功
		t.Errorf("sync_token 無し: status = %d, want 303 (body=%s)", w.Code, w.Body.String())
	}
}
```

**実装注意**: ハンドラで `form.Get("sync_token") == ""`（または parse 失敗）のとき、`updated_at` 条件無しの通常更新へ分岐する。タイムスタンプの parse は `strconv.ParseInt` → `time.Unix(...)` し、`pgtype.Timestamptz` に詰める（`internal/infra/pgconv` の作法に合わせる）。

### 14.3 「取得 or 作成」+ 楽観ロック + トランザクションの一括保存テスト（ADAPT）

> Laravel の `firstOrCreate` + 楽観ロック + `DB::transaction` は、Go では「`INSERT ... ON CONFLICT (...) DO NOTHING/UPDATE` でヘッダーを取得/作成 → 明細を同一 tx 内で一括 INSERT → 失敗時 Rollback」に対応する。新規時はロック検証をスキップ、既存時は `updated_at` 照合という分岐は §14.1/14.2 と同じ。

例として「ある `device_id` + 計測単位ごとに1件だけ存在するサマリ行（ヘッダー）を取得/作成し、明細を一括保存」する想定（本プロジェクトの実テーブルでなく、将来の一括保存の型として示す）。

```go
// テスト1: 新規保存（既存ヘッダー無し → 楽観ロック検証なし → 成功）
func TestBulkSave_新規ヘッダーは検証なしで成功(t *testing.T) {
	tx := &fakeTxQuerier{rowsAffected: 1, headerExisted: false}
	svc := NewBulkSaveService(tx)

	err := svc.Save(context.Background(), validBulkInput())
	if err != nil {
		t.Fatalf("新規一括保存: %v", err)
	}
	if !tx.committed {
		t.Error("成功時に Commit されていない")
	}
}

// テスト2: 既存ヘッダーあり → updated_at 不一致 → 409 相当のエラー + Rollback
func TestBulkSave_既存ヘッダーで競合ならRollback(t *testing.T) {
	tx := &fakeTxQuerier{rowsAffected: 0, headerExisted: true} // 0 件 = 競合
	svc := NewBulkSaveService(tx)

	err := svc.Save(context.Background(), validBulkInput())
	if !errors.Is(err, ErrConflict) {
		t.Errorf("競合: got %v, want ErrConflict", err)
	}
	if !tx.rolledBack {
		t.Error("競合時に Rollback されていない（部分コミットの危険）")
	}
}

// テスト3: 同条件で2回保存 → ON CONFLICT によりヘッダーは1件のみ（実 DB テスト）
func TestBulkSave_ON_CONFLICTでヘッダー重複しない(t *testing.T) {
	// goose migrate 済みテスト DB を使用。対象テーブルを TRUNCATE してから実施。
	q := repository.New(testPool)
	saveOnce(t, q) // 1回目
	saveOnce(t, q) // 2回目（sync_token 更新済み）

	n := countHeaders(t, q, deviceID)
	if n != 1 {
		t.Errorf("ヘッダー件数 = %d, want 1 (ON CONFLICT 効いていない)", n)
	}
}
```

**テストすべき観点（PORT）:**
- 新規保存成功（保存メソッドが呼ばれ、Commit される）
- 楽観ロック競合 → 409 相当 + **Rollback**（部分コミット防止）
- `ON CONFLICT` によるヘッダー重複防止（実 DB で件数=1）
- スナップショット値の固定（例: 発火時点の閾値を `alert_histories.threshold` に非正規化保持。後で `alert_rules.threshold` を変えても履歴は不変 — スキーマ上 `alert_histories` は `metric/operator/threshold` を非正規化保持する設計なので、ここはそのままテスト観点になる）
- `numeric(5,2)` カラムへの値の丸め（`pgtype.Numeric` 変換は `internal/infra/pgconv` 経由）
- NULL 許容フィールドのデフォルト適用（DB の DEFAULT が効くこと）

---

## 15. 共有 templ コンポーネントのテスト戦略

### 15.1 コンポーネント単体テスト + 統合テストの併用（ADAPT）

> Laravel の `Blade::render('<x-... />', [...])` 単体 + 呼び出し元ページ統合の二段構えは、本プロジェクトでは **(a) `render(t, Comp(view))` でコンポーネントを直接 Render（`component_test.go` の作法）+ (b) ハンドラ経由（httptest + gin）で呼び出し元ページ全体を取得して `strings.Contains`** に対応する。

共有 templ コンポーネント（例: ファイルプレビュー、フラッシュメッセージ、サイトヘッダー）の変更は、単体と統合の両方で検証する。

```go
// (a) コンポーネント単体: render ヘルパで直接 Render
func TestFlashMessage_単体描画(t *testing.T) {
	html := render(t, FlashMessage("保存しました"))
	assertContains(t, html, `id="flash-message"`)
	assertContains(t, html, "保存しました")
}

// (b) 統合: 呼び出し元ページ（ハンドラ経由）でも出ることを確認
func TestDashboard_フラッシュがレイアウト経由で描画される(t *testing.T) {
	app := newAuthApp(repoWithUser(t))
	cookies := loginCookies(t, app)
	w := getWithCookies(app, "/dashboard", cookies)
	assertContains(t, w.Body.String(), `id="flash-message"`)
}
```

**理由（PORT）**: コンポーネント単体だけでは、呼び出し元での view struct の渡し方（フィールド埋め忘れ・`templ.WithChildren` の渡し漏れ）を検出できない。統合だけでは、コンポーネント自体のロジック不具合（分岐・空値ハンドリング）の切り分けが難しい。

> ※ `children` を取るコンポーネント（レイアウト枠等）は `templ.WithChildren(ctx, child)` を使って Render する。`children` を渡す/渡さない両方を単体テストで固定すると、`{ children... }` の埋め込み位置のリグレッションを防げる（`layout_test.go` 参照）。

---

## 16. 選択モーダル（子画面）のテスト知見

### 16.1 DB スキーマとテストデータの乖離に注意（PORT）

> Laravel の「設計書のカラム名と実 DB のカラム名がズレる」罠は、本プロジェクトでも同型。Go は静的型なので**存在しないカラムは sqlc 生成構造体に無くコンパイルエラー**になるが、`pgtype` の型ミスマッチや、CHECK 制約（`metric_valid` / `operator_valid` / `mac_address_format` / `*_range`）違反は**実 DB テストでランタイムエラー**になる。

実 DB テストで業務カラムへ直接 INSERT する場合は、先に `docs/database_snapshot/table_definitions.md`（権威ある現状スキーマ）で実在カラム名・型・CHECK 制約を確認する。

```go
// NG: CHECK 制約 sensor_readings_temperature_range (-40〜125) 違反でINSERT失敗
insertReading(t, q, repository.CreateSensorReadingParams{Temperature: num(999.0), ...})

// NG: devices_mac_address_format (正規表現) 違反
insertDevice(t, q, repository.CreateDeviceParams{MacAddress: "not-a-mac", ...})

// OK: スナップショットの制約レンジ・書式に整合した値を使う
insertDevice(t, q, repository.CreateDeviceParams{
	MacAddress: "AA:BB:CC:DD:EE:FF", // mac_address_format に適合
	IsActive:   true,
})
```

> 補足: enum 相当のカラム（`metric`/`operator`）は DB では文字列 + CHECK、Go では `domain.Metric`/`domain.ComparisonOperator`（`internal/domain`）。テストデータは `string(domain.MetricTemperature)` のように domain 値から文字列化して渡すと、CHECK 許容リストとのズレが起きない。

### 16.2 否定検証とサブストリング罠（PORT）

`strings.Contains` の否定（`!strings.Contains`）は部分一致のため、短いクラス名・id を否定すると、それをプレフィックスに含む別クラスへ誤マッチして検証が常にすり抜ける/落ちる。語境界付きの正規表現（`\b`）で完全一致を検証する。

```go
// NG: "device-tab" が "device-table-min-width" にマッチして誤判定
if strings.Contains(html, "device-tab") {
	t.Error("device-tab が含まれている")
}

// OK: 正規表現の語境界で完全一致
re := regexp.MustCompile(`\bdevice-tab\b`)
if re.MatchString(html) {
	t.Error("device-tab クラスが含まれている（語境界一致）")
}
```

> 本質的教訓（PORT）: 「含まないこと」を短い文字列で検証するのは危険。否定検証では語境界 or 要素単位の正規表現を使う。

### 16.3 否定検証と `<option>` 選択肢の干渉（ADAPT）

> Laravel の「論理削除されたデータ名がテーブル行には出ないが、検索コンボの `<option>` には出るため `assertDontSee` が失敗」は、本プロジェクトでも同型。論理削除（`deleted_at IS NOT NULL`）で一覧から除外したマスター名が、別途渡された選択肢 `<select>` の `<option>` には残っている、というケース。

ページ全体に対する否定検証ではなく、**テーブル行内（`<td>`）だけ**を正規表現で検証する。

```go
// NG: コンボの <option> にも「削除済みデバイス」が出るので失敗
if strings.Contains(html, "削除済みデバイス") {
	t.Error("削除済みデバイスが表示されている")
}

// OK: <td> 内だけを正規表現で検証
re := regexp.MustCompile(`<td[^>]*>[^<]*削除済みデバイス[^<]*</td>`)
if re.MatchString(html) {
	t.Error("削除済みデバイスがテーブル行に表示されている")
}
```

**回避策**: テストデータに一意な名前（例 `"削除専用デバイスZ"`）を付けると選択肢との名前衝突を避けられる場合があるが、マスターを全件 `<option>` に渡す設計では根本解決にならない。

### 16.4 論理削除済みの除外はクエリ側（`deleted_at IS NULL`）で担保する（ADAPT）

> Laravel の `belongsToMany(...)->whereNull('pivot.deleted_at')` のような ORM リレーション定義は本プロジェクトに無い。除外は **sqlc クエリの `WHERE deleted_at IS NULL`** で担保する（`db/queries/*.sql` は全 SELECT がこの条件を持つ）。「リレーション定義に除外条件を埋める」発想を「クエリに除外条件を埋め、クエリ単位でテストする」に読み替える。

```go
// 実 DB テスト: 論理削除した行が一覧クエリに出ないこと
//   + 別の有効な行はちゃんと出ること（除外しすぎの検出）
func TestListAlertRules_論理削除済みは除外し有効分は返す(t *testing.T) {
	q := repository.New(testPool)
	activeID := insertRule(t, q, deviceID)   // deleted_at IS NULL
	deletedID := insertRule(t, q, deviceID)  // この後 SoftDelete
	mustExec(t, q.SoftDeleteAlertRule(ctx, deletedID))

	rules, err := q.ListAlertRulesByDevice(ctx, deviceID)
	if err != nil {
		t.Fatalf("ListAlertRulesByDevice: %v", err)
	}
	ids := ruleIDs(rules)
	assertContainsID(t, ids, activeID)      // 有効分は出る（除外しすぎでない）
	assertNotContainsID(t, ids, deletedID)  // 削除分は出ない
}
```

> 本質的教訓（PORT）: 「除外されること」だけでなく「除外しすぎていない（有効な隣接データは残る）こと」も同じテストで固定する。`deleted_at IS NULL` を付け忘れた/付けすぎたクエリの両方向のバグを捕まえる。

### 16.5 hx-target とフラグメント返却範囲の DOM ネストバグ（ADAPT）

> Laravel の `respondWithFragment()` がフラグメント全体を返す問題は、本プロジェクトの **「HTMX 時の Handler は部分コンポーネント全体を返す」**（HTMX実装ガイド §1「hx-target とコンポーネント返却範囲の整合性」）と完全に同型。検索ボタン等の `hx-target` がコンポーネントの**内部要素**（例 `#select-device-result`）を指すと、コンポーネント全体が内部要素の innerHTML にネストされ、二重ヘッダー等の壊れた DOM になる。`strings.Contains`（テキスト存在のみ）ではこのバグを検出できない。

検出には **構造的な重複（特定要素の出現回数）** を数える。

```go
// NG: strings.Contains はテキスト存在のみ → DOM ネストを見逃す
assertContains(t, html, "発注者A") // PASS してしまう（テキストは存在）

// OK: 構造の重複を回数で検出する
func countOccurrences(s, sub string) int { return strings.Count(s, sub) }

func TestSelectDeviceModal_DOMネストしていない(t *testing.T) {
	app := newSelectApp(repo)
	req := httptest.NewRequest(http.MethodGet, "/devices/select?q=A", nil)
	req.Header.Set("HX-Request", "true") // HTMX 部分返却を誘発
	w := httptest.NewRecorder()
	app.ServeHTTP(w, req)

	if n := strings.Count(w.Body.String(), `class="modal-header"`); n != 1 {
		t.Errorf("modal-header の出現回数 = %d, want 1（>1 は DOM ネスト = hx-target 内部要素誤指定）", n)
	}
}
```

> 対策は HTMX実装ガイドの A案（`hx-target` をコンポーネントのコンテナに統一し常に全体返却）または C案（`hx-select` で必要部分のみ抽出）。テストは「`modal-header` / `x-data` 等が1回だけ出る」ことを固定し、設計変更時のリグレッションを防ぐ。

### 16.6 JOIN ベースソートのテストデータ設計（PORT）

> リレーション先カラム（例: デバイス名でアラート一覧をソート）でソートするテストは、Laravel/Go を問わず同じ落とし穴。**ソート対象キーの順序とデフォルトソート（例 `created_at`）の順序が一致するデータ**だと、JOIN ソートが未実装でもデフォルトフォールバックで偶然パスしてしまう。

ソートキー順とデフォルト順が**逆転**するデータを作る。

```go
// NG: 名前昇順(あ社<わ社)と作成順(あ社→わ社)が一致 → フォールバックでもパス
insertDevice(t, q, "あ社デバイス") // created_at 先
insertDevice(t, q, "わ社デバイス") // created_at 後

// OK: 名前昇順と作成順を逆転させる → JOIN ソートでのみ正しい順序になる
insertDevice(t, q, "わ社デバイス") // created_at 先（デフォルト順では先頭）
insertDevice(t, q, "あ社デバイス") // created_at 後
// sort=device_name&order=asc → 「あ社デバイス」が先頭になるはず。
//   フォールバック（created_at順）では「わ社デバイス」が先頭 → ソート未実装を検出できる
```

**適用対象**: JOIN したカラム（デバイス名・指標ラベル等）でソートする全クエリのテスト。

> 本質的教訓（PORT）: 「ソートが正しいか」のテストは、デフォルト順とテスト対象順が一致しない入力で初めて意味を持つ。最初の実装で全パスしたら、まずテストデータの順序設計を疑う。

### 16.7 `<script>` 埋め込み JSON の XSS セーフ検証（ADAPT）

> Laravel の `addslashes()` → `Js::from()` 移行の XSS 検証は、本プロジェクトでは **`templ.JSONScript` / `templ.JSONString`（または `encoding/json` + templ の自動エスケープ）で `<script>` にデータを埋め込み、生の `<script>` がエスケープされずに出力されないこと**の検証に対応する。Go の `encoding/json` は `<`,`>`,`&` を `\u003c` 等にエスケープするため HTML 安全だが、それが実際に効いていることをテストで固定する。

```go
func TestDevicePage_埋め込みJSONがXSSセーフ(t *testing.T) {
	v := DevicePageView{
		Devices: []DeviceVM{{Name: "XSS<script>テスト"}},
	}
	html := render(t, DevicePage(v))

	// 生の <script> タグがそのまま出力されていないこと（エスケープされている）
	if strings.Contains(html, "XSS<script>テスト") {
		t.Error("デバイス名の <script> が未エスケープで出力された（XSS リスク）")
	}
	// エスケープ済み（\u003c 等）で埋め込まれていること
	assertContains(t, html, `XSS\u003cscript\u003eテスト`)
}
```

> 本質的教訓（PORT）: ユーザー入力由来の文字列を `<script>` に JSON で埋める箇所は、必ず「危険文字を含むデータで生タグが漏れない」テストを置く。手書きの `\` エスケープ（`addslashes` 相当）を使わず、`encoding/json` / `templ.JSONScript` の自動エスケープに任せる。

### 16.8 否定検証とフラグメント外パネルの干渉（ADAPT）

> Laravel の「2カラムレイアウトで、右パネルから除外したデータが左メインテーブルには正常表示されるため、ページ全体 `assertDontSee` が失敗」は本プロジェクトでも同型（§16.3 の `<option>` 干渉と同じ構造）。例: ダッシュボードの「最近の通知パネル」から除外された行（既読/他デバイス等）が、メインのデバイス一覧テーブルには表示される。

ページ全体の否定検証をやめ、**パネル固有の件数表示・空メッセージ・パネル内リンク URL** で除外を検証する。

```go
// NG: パネルから除外されても、メインテーブルにデバイス名が出るため失敗する
if strings.Contains(html, "完了済みデバイス") {
	t.Error("完了済みデバイスが表示されている")
}

// OK: パネル固有の表示で除外を検証する
assertContains(t, html, "0件")              // パネルの件数表示
assertContains(t, html, "通知はありません")    // パネルの空メッセージ
// パネル内リンク URL の有無で、特定デバイスがパネルに含まれるか検証
if strings.Contains(html, `href="/devices/`+strconv.FormatInt(excludedID, 10)+`"`) {
	t.Error("除外対象デバイスがパネル内リンクに含まれている")
}
```

**回避策（PORT）:**
- パネルの**件数表示**（`N件`）で除外が反映されていることを確認する
- パネル内 `<a href>` リンク URL の有無で、特定データがパネルに含まれるかを検証する
- ページ全体を走査する否定検証（素の `!strings.Contains`）は、同じデータが複数箇所（メイン + パネル + 選択肢）に出る画面では避ける。検証対象を要素単位（§16.3 の `<td>` 限定や、パネルコンポーネント単体 Render）に絞る

---

## 18. templ.JSONString / encoding/json の文字エンコードとテスト検証

### 18.1 問題（Laravel との決定的な違い）

> 【ADAPT】Laravel の `Js::from()` は日本語を `\uXXXX` に Unicode エスケープするため `assertSee('日本語')` が失敗した。Go の `encoding/json`（および `templ.JSONString`）は**逆に日本語をそのまま UTF-8 で出力する**。エスケープ対象は `<` `>` `&` のみ（HTML 安全化）。

Alpine.js の `@click` / `x-data` へ Go 側の値を渡すとき（§10-C）、本プロジェクトでは `templ.JSONString`（内部で `json.Marshal`）を使う。Laravel の罠は逆向きに作用する。

```go
// json.Marshal の実際の出力（検証済み）
json.Marshal("入力内容が破棄されます")
//=> "入力内容が破棄されます"   ← 日本語はそのまま UTF-8。\uXXXX にならない

// HTML 危険文字だけ \u エスケープされる（SetEscapeHTML=true がデフォルト）
json.Marshal(map[string]any{"msg": `<script>あ&"`})
//=> {"msg":"\u003cscript\u003eあ\u0026\""}   ← < > & のみ \u003c 等に化ける
```

このため検証方針は Laravel と真逆になる。

```go
// 日本語は素直に strings.Contains で検証できる（Laravel と違いエスケープされない）
assertContains(t, html, "入力内容が破棄されます")

// 逆に「< > & を含む値が生のまま出ていない」ことを XSS 観点で逆検証する
if strings.Contains(html, "<script>") {
    t.Error("script タグが \\u003c にエスケープされず生出力されている (XSS リスク)")
}
```

### 18.2 確認ダイアログ文言の検証パターン

本プロジェクトの削除確認は Alpine.js モーダルではなく `hx-confirm` 属性（HTMX 組み込み、§11 / §C06）を採用する。`hx-confirm` の値は templ 属性式で `fmt.Sprintf` で組み立てるため、日本語はそのまま属性値に入る。

```go
// templ 側（HTMX実装ガイド §C06）
//   hx-confirm={ fmt.Sprintf("「%s」を削除しますか？", d.Name) }

func TestDeviceCard_削除確認文言を描画(t *testing.T) {
    html := render(t, DeviceCard(repository.Device{ID: 1, Name: "ハウスA"}))

    // 日本語の確認文言は \uXXXX にならないため、そのまま含有検証できる
    assertContains(t, html, `hx-confirm="「ハウスA」を削除しますか？"`)
    assertContains(t, html, `hx-delete="/devices/1"`)
}
```

ASCII の構造文字列（属性名・URL・記号）はどの経路でもそのまま出るため、Alpine.js の `@click` 式を検証する場合も周囲の ASCII で構造を確認できる。

```go
// @click 式の構造を ASCII 部分で確認（Laravel の「対策1」に相当）
assertContains(t, html, `$dispatch('modal-close')`)
assertContains(t, html, "selectedDevice = ")
```

### 18.3 templ.JSONString による XSS 安全性の逆検証

`templ.JSONString` に危険文字を含む値を渡しても、`json.Marshal` の HTML エスケープにより `<` `>` `&` が `\u003c` `\u003e` `\u0026` に変換される。これを「タグ閉じが生出力されないこと」で逆検証する。

```go
func TestDeviceSelector_危険文字を含むデバイス名をエスケープ(t *testing.T) {
    // 悪意ある名前を Querier モックの戻り値で注入
    html := render(t, DeviceSelector(ptr(int64(1)), `</script><img src=x onerror=alert(1)>`))

    // 生の閉じタグ・属性が出ていない（json.Marshal が \u003c 化している）
    if strings.Contains(html, "</script>") || strings.Contains(html, "onerror=alert") {
        t.Error("危険文字が JSON エスケープされず生出力されている (XSS リスク)")
    }
    // エスケープ済みの痕跡が含まれることも確認
    assertContains(t, html, `\u003c`)
}

func ptr[T any](v T) *T { return &v }
```

**ルール:**
- 日本語の検証 → `strings.Contains` でそのまま（Laravel と逆。`\uXXXX` 化を疑わなくてよい）。
- XSS 安全性の検証 → `< > &` が生出力されていないこと（`!strings.Contains(html, "<script>")`）と `\u003c` 等の存在を併用。
- ASCII の構造（属性名・URL・Alpine 式の骨格）はどの経路でもそのまま出るため、構造検証は ASCII を基点にする。

> 教訓の本質（Laravel から転用）: 「動的値の JSON 化は必ずフレームワークのエンコーダ経由で行い、文字列連結や自前エスケープを使わない」。Go では `templ.JSONString` / `encoding/json` がこれを担う（§10-C）。検証の向きだけが逆になる。

---

## 19. Alpine.js x-data の複数ブロック識別と検証

> 【ADAPT】PHP の `preg_match_all` + `substr` でのセグメント抽出を、Go の `strings.Index` / `regexp` に読み替える。教訓（単純な含有検証では「どの x-data に属するか」を区別できない）はそのまま通用する。

### 19.1 問題: 複数の x-data が 1 ページに共存する場合

本プロジェクトでは Alpine.js は「サーバー往復不要の UI 開閉」に限定される（§C07/C08/C09）。それでも共通レイアウトの `x-data="{ navOpen: false }"`（layout.App、layout_test.go で固定済み）とページ固有の確認モーダル `x-data="{ confirmOpen: false }"`（§C09）が共存しうる。

```html
<!-- レイアウト側（共通） -->
<div x-data="{ navOpen: false }">
  ...
  <!-- ページ固有の保存確認モーダル -->
  <div x-data="{ confirmOpen: false }" class="device-form-container">
```

単純な `assertContains(html, "confirmOpen")` では「どちらの `x-data` に属するか」を区別できない。

### 19.2 セグメント抽出による特定ブロックの検証

目的のコンポーネントを近隣の CSS class で特定し、その前方の HTML セグメントだけを切り出して検証する。`strings.LastIndex` で「class の直前に最も近い `x-data`」を取る。

```go
func TestDeviceForm_確認モーダルのxdataを特定検証(t *testing.T) {
    html := render(t, DeviceCreatePage(deviceCreateView()))

    // 1. 目的のコンポーネントを CSS class で特定（独自クラス禁止 §31 のため、
    //    モック由来の確定クラス名を基点にする）
    anchor := strings.Index(html, "device-form-container")
    if anchor < 0 {
        t.Fatal("device-form-container が見つからない")
    }

    // 2. その前方 2000 文字のセグメントを切り出す
    start := anchor - 2000
    if start < 0 {
        start = 0
    }
    segment := html[start:anchor]

    // 3. セグメント内で「最も近い（=末尾側の）」x-data を取得
    re := regexp.MustCompile(`x-data="([^"]*)"`)
    matches := re.FindAllStringSubmatch(segment, -1)
    if len(matches) == 0 {
        t.Fatal("セグメント内に x-data が無い")
    }
    nearest := matches[len(matches)-1][1] // end() 相当

    // 4. 特定ブロックのプロパティを検証
    if !strings.Contains(nearest, "confirmOpen") {
        t.Errorf("確認モーダルの x-data に confirmOpen が無い: %q", nearest)
    }
    // 共通レイアウトの navOpen がこのブロックに混入していないこと
    if regexp.MustCompile(`(^|[\s{])navOpen\s*:`).MatchString(nearest) {
        t.Errorf("device-form-container の x-data に navOpen が混入: %q", nearest)
    }
}
```

### 19.3 複数行展開される x-data の正規表現制約

PHP と同じ落とし穴が Go の `regexp` でも起こる。Go の `regexp`（RE2）は既定で `.` が改行にマッチしない。複数行の `x-data` を貪欲に取りたいときは `(?s)` フラグで dotall にする必要がある。

```go
// NG: [^}]* は改行を含まないため最初の } で止まる
regexp.MustCompile(`x-data="\{([^}]*)\}"`)

// NG: (?s) なしの .* は改行にマッチしない（Go の RE2 既定）
regexp.MustCompile(`x-data="(\{.*?\})"`)

// 注意: x-data 内に Alpine メソッド定義があると内部の } を超える必要があり複雑化する。
// → §19.2 のセグメント抽出で範囲を限定してから FindAllStringSubmatch する方が安全。
```

> 補足: 本プロジェクトの設計では Alpine.js は開閉状態のみ（メソッド定義をほぼ持たない）に限定しているため、`x-data="{ confirmOpen: false }"` のような単一行が大半。複雑な複数行 `x-data` を書きそうになったら、それは「HTMX 化すべき操作」を Alpine に押し込んでいるサインの可能性がある（§4 判断基準を再確認）。

**チェックポイント:**
- 単純な `strings.Contains(html, "confirmOpen")` は「どの x-data か」を区別しない。
- 特定ブロック検証は「CSS class を anchor → 前方セグメント → 末尾側の x-data」で行う。
- **参照**（`this.dirty = true`）と**宣言**（`confirmOpen: false`）を区別するには行頭/語境界マッチの正規表現（`(^|[\s{])navOpen\s*:`）を使う。

---

## 20. templ コンポーネントの単体レンダリングテスト

> 【ADAPT】Laravel の `$this->blade('<x-... />')` / `Blade::render()` は、templ コンポーネントを `Render(ctx, &buf)` で直接描画する `render(t, Comp(...))` ヘルパに置き換わる。`assertSee`/`assertDontSee` は `assertContains`/`!strings.Contains` に対応。

### 20.1 render ヘルパ（本プロジェクトの定石）

各 view パッケージの `_test.go` に同名の `render` / `assertContains` ヘルパが置かれている（component_test.go / layout_test.go / page_test.go で実体確認済み）。HTTP レイヤを一切通さず、templ コンポーネントを `bytes.Buffer` に描画して文字列検証する。

```go
func render(t *testing.T, c templ.Component) string {
    t.Helper()
    var buf bytes.Buffer
    if err := c.Render(context.Background(), &buf); err != nil {
        t.Fatalf("render: %v", err)
    }
    return buf.String()
}

func assertContains(t *testing.T, html, substr string) {
    t.Helper()
    if !strings.Contains(html, substr) {
        t.Errorf("出力に %q が含まれていない:\n%s", substr, html)
    }
}

// 使用例（component_test.go より）
func TestSiteHeader_ユーザー名とログアウトフォームを描画(t *testing.T) {
    html := render(t, SiteHeader("テストユーザー", "tok-123"))
    assertContains(t, html, "テストユーザー")
    assertContains(t, html, `name="gorilla.csrf.Token"`)
}
```

### 20.2 children を渡すコンポーネント（@slot / 子要素相当）

Blade の `<x-... >子</x-...>` のスロットは、templ では `templ.WithChildren(ctx, child)` で渡す（layout_test.go の Guest レイアウトで実体確認済み）。

```go
func TestGuest_カードでchildrenを描画(t *testing.T) {
    var buf bytes.Buffer
    // 子要素を ctx に載せてから Render する
    ctx := templ.WithChildren(context.Background(), templ.Raw("<p>子要素</p>"))
    if err := Guest("ログイン - 農業IoTシステム", "/static/css/style.css?v=dev").Render(ctx, &buf); err != nil {
        t.Fatalf("render: %v", err)
    }
    html := buf.String()

    assertContains(t, html, "guest-card")
    assertContains(t, html, "子要素") // children が差し込まれている
}
```

### 20.3 「描画していないこと」の逆検証（assertDontSee 相当）

Blade の `assertDontSee` は `!strings.Contains` で表現する。条件付きで出ない要素を検証するときに使う。

```go
func TestFlashMessage_メッセージ未指定なら本文を描画しない(t *testing.T) {
    html := render(t, FlashMessage(""))
    assertContains(t, html, `id="flash-message"`) // 領域は常にある
    // 中身のテキストノードは無い、等を必要に応じて !strings.Contains で確認
}
```

### 20.4 使い分け表

| Laravel | 本プロジェクト | 用途 |
|---------|---------------|------|
| `$this->blade('<x-... />')` (TestResponse) | `render(t, Comp(args...))` + `assertContains` | コンポーネント出力の含有検証 |
| `Blade::render(..., $data)` (string) | 同上（templ は常に string を返す） | 生 HTML の部分一致検証 |
| `<x-... >子</x-...>` のスロット | `templ.WithChildren(ctx, child)` | 子要素差し込み |
| `assertSee` / `assertDontSee` | `assertContains` / `!strings.Contains` | 含有 / 非含有 |

> 【Laravel では `$this->blade()` が setUp の `View::share('errors')` を自動継承した】Go の templ には共有マジックが無い。エラーは view struct のフィールド（例 `LoginView{Errors map[string]string}`）で明示的に渡す（page_test.go で確認）。`@error` ディレクティブ相当のコンテキスト共有を期待してはならない — エラー表示のテストは struct にエラーを詰めて Render するだけで完結する（テストが単純化される）。

```go
// エラー表示の単体テスト（page_test.go の実パターン）
func TestLoginPage_入力値再表示とフィールドエラー(t *testing.T) {
    v := LoginView{
        CSRFToken: "tk",
        Email:     "user@example.com",
        Errors:    map[string]string{"email": "メールアドレス形式で入力してください"},
    }
    html := render(t, LoginPage(v))
    assertContains(t, html, `value="user@example.com"`)      // 入力値再表示
    assertContains(t, html, "メールアドレス形式で入力してください") // フィールドエラー
}
```

---

## 21. domain 型（enum 相当）の「型値の比較」と「DB 文字列の比較」の区別

> 【ADAPT】Laravel の `$casts` による Enum キャストが無いため「`assertEquals`（Enum オブジェクト）と `assertDatabaseHas`（`->value`）の使い分け」という罠そのものは消える。だが本質的教訓 ——「アプリ層で扱う型値」と「DB に格納される文字列」を意識的に区別せよ —— は domain 型 + CHECK 制約の検証に読み替えられる。

### 21.1 本プロジェクトの構造（int キャストではなく文字列）

Laravel は `project_type` を `ProjectType::class` で int 値へキャストしていた。本プロジェクトの enum 相当は `internal/domain` の文字列ベース型で、DB には**文字列のまま** CHECK 制約付きで格納される（int 化は無い）。スキーマで確認済み:

- `alert_rules.metric` `varchar(20)` — CHECK `('temperature','humidity')`（domain.Metric と対応）
- `alert_rules.operator` `varchar` — CHECK `('>','<','>=','<=')`（domain.ComparisonOperator と対応）

```go
// domain 型は文字列の基底型（comparison_operator_test.go / metric_test.go で確認）
//   type Metric string;  MetricTemperature Metric = "temperature"
//   type ComparisonOperator string;  OpGreaterThanOrEqual = ">="
```

### 21.2 「アプリ層 = 型値で比較」「DB 層 = 文字列で比較」

Laravel の「`assertEquals` は Enum、`assertDatabaseHas` は `->value`」に対応する区別はこうなる。

```go
// アプリ層（domain 型値どうしで比較）— Querier モックの戻り値や service の結果を検証
func TestAlertRuleService_保存後の比較演算子(t *testing.T) {
    got := rule.Operator                  // repository 行 → domain 型へ変換済みの値
    if got != domain.OpGreaterThanOrEqual {
        t.Errorf("Operator = %q, want %q", got, domain.OpGreaterThanOrEqual)
    }
}

// DB 層（格納される文字列で比較）— sqlc が生成する Params/行構造体は string
func TestCreateAlertRuleParams_DB格納値は文字列(t *testing.T) {
    params := repository.CreateAlertRuleParams{
        Metric:   string(domain.MetricTemperature),       // "temperature"
        Operator: string(domain.OpGreaterThanOrEqual),    // ">="
        // Threshold は numeric(5,2) → pgconv.Numeric2(35.00) で pgtype.Numeric に
    }
    if params.Operator != ">=" {
        t.Errorf("DB 格納される operator = %q, want %q", params.Operator, ">=")
    }
}
```

**ルール:**
- **アプリ層の比較**: domain 型値で比較（`domain.OpGreaterThanOrEqual`）。`String()` ではなく型値そのもの。
- **DB 層の比較（sqlc Params / 行）**: `string(domain.Xxx)` の素の文字列で比較（`">="`, `"temperature"`）。
- enum の妥当性は `Valid()` / `Parse...()` の table-driven test で網羅（comparison_operator_test.go / metric_test.go の既存パターンを踏襲）。

### 21.3 CHECK 制約の許容リストと domain の許容リストの一致を固定する

Laravel の `$casts` 一覧の代わりに、Go では「domain が `Valid()` で許す集合」と「DB の CHECK 制約が許す集合」が一致していることをテストで固定すると、スキーマ変更時のズレを検知できる。

```go
// domain 側の許容集合（comparison_operator_test.go の TestComparisonOperator_Valid を発展）
func TestComparisonOperator_DB_CHECK制約と一致(t *testing.T) {
    // DB CHECK: ('>','<','>=','<=')（table_definitions.md alert_rules_operator_valid）
    dbAllowed := []string{">", "<", ">=", "<="}
    for _, s := range dbAllowed {
        if op, err := domain.ParseComparisonOperator(s); err != nil || !op.Valid() {
            t.Errorf("DB が許す %q を domain が拒否している（CHECK と domain がズレ）", s)
        }
    }
}
```

> 教訓の本質: 「アプリの型」と「永続化される値」は別物として常に意識する。Laravel では int 化のせいで `assertEquals` が型エラーになる罠だったが、Go では「domain 型 vs 文字列」の取り違え（`rule.Operator == ">="` を domain 型に対してやると別型比較でコンパイルエラー / 逆に Params の string を domain 値と比較する）として現れる。

---

## 22. ファイル数上限を 422 で返すテスト（バリデーション後のビジネスルール）

> 【ADAPT】Laravel の「`response()->json([], 422)` が Web ルートで 200 に化ける」罠は **Gin には無い** — `c.JSON(422, ...)` はそのまま 422 を返す。教訓「バリデーション通過後のビジネスルール違反も正しいステータスで返す」はそのまま通用する。`abort(422,...)` は Gin では `c.AbortWithStatusJSON` / `c.JSON` + `return` に対応。

### 22.1 ハンドラ実装（API は JSON、422 をそのまま返す）

```go
// 本プロジェクトは API=JSON 限定。Gin はステータスが化けないので素直に 422 を返す。
const maxFilesPerDevice = 10

func (h *DeviceFileAPI) Upload(c *gin.Context) {
    // ... 所有者認可（internal/authz / BOLA）通過後 ...

    count, err := h.Repo.CountDeviceFiles(c.Request.Context(), deviceID)
    if err != nil {
        c.JSON(http.StatusInternalServerError, gin.H{"error": "内部エラー"})
        return
    }
    if count >= maxFilesPerDevice {
        // Laravel の abort(422,...) 相当。Gin はこのまま 422 を返す（200 化けしない）
        c.JSON(http.StatusUnprocessableEntity, gin.H{"error": "ファイル数の上限に達しています"})
        return
    }
    // ... 保存処理 ...
}
```

### 22.2 テスト（multipart httptest + Querier モック）

アップロードは `multipart.Writer` でリクエストを組み立てる（`UploadedFile::fake()` 相当）。件数は Querier モックの戻り値で制御する（DB 不要）。

```go
// 上限到達を返す Querier モック
type fakeFileRepo struct {
    device repository.Device
    count  int64
}

func (f *fakeFileRepo) GetDevice(context.Context, int64) (repository.Device, error) {
    return f.device, nil
}
func (f *fakeFileRepo) CountDeviceFiles(context.Context, int64) (int64, error) {
    return f.count, nil
}

func newMultipartUpload(t *testing.T, field, filename string, content []byte) (*http.Request, string) {
    t.Helper()
    var body bytes.Buffer
    w := multipart.NewWriter(&body)
    fw, err := w.CreateFormFile(field, filename)
    if err != nil {
        t.Fatalf("CreateFormFile: %v", err)
    }
    if _, err := fw.Write(content); err != nil {
        t.Fatalf("write: %v", err)
    }
    _ = w.Close()
    req := httptest.NewRequest(http.MethodPost, "/devices/10/files", &body)
    return req, w.FormDataContentType()
}

func TestDeviceFileAPI_Upload_上限到達で422(t *testing.T) {
    repo := &fakeFileRepo{device: repository.Device{ID: 10, UserID: 7}, count: 10}
    r := newRouterWithUser(&DeviceFileAPI{Repo: repo}, 7) // sensor_api_test.go の定石

    req, ct := newMultipartUpload(t, "file", "extra.pdf", []byte("dummy"))
    req.Header.Set("Content-Type", ct)
    w := httptest.NewRecorder()
    r.ServeHTTP(w, req)

    if got, want := w.Code, http.StatusUnprocessableEntity; got != want {
        t.Errorf("status: got %d, want %d, body=%s", got, want, w.Body.String())
    }
}
```

> 補足: Web（HTML）でバリデーション/ビジネスルール違反を扱う場合は、API と異なり 200 で HTML 再描画、または 422 + 部分 HTML が設計判断（HTMX実装ガイド §7、§125 行目「`c.Status(422)` の後に部分コンポーネントを Render」）。Gin はステータスとレンダリングを独立に制御でき、Laravel の `fragment()->withStatus()` のようなチェーン制約は無い。

---

## 23. CSV 取込テストパターン

> 【ADAPT】`UploadedFile::fake()->createWithContent()` は Go では `multipart.Writer` でメモリ上に CSV を組み立てる。CSV パースは `encoding/csv`。教訓（正常 / 列数不一致 / 空 / DB 書込み有無 / 固定値 / 全件ロールバック）はすべて通用する。

> 注記: 現時点のスキーマ（table_definitions.md）に CSV 取込専用テーブルは無い。以下は将来の一括取込機能（センサーデータ / アラート履歴のバルク投入等）を想定した「こう書く」パターンとして提示する。テーブル・カラムは実装時に当時のスナップショットへ整合させること。

### 23.1 CSV リクエストの組み立て（createWithContent 相当）

```go
func newCSVUpload(t *testing.T, field, filename, content string) (*http.Request, string) {
    t.Helper()
    var body bytes.Buffer
    w := multipart.NewWriter(&body)
    fw, err := w.CreateFormFile(field, filename)
    if err != nil {
        t.Fatalf("CreateFormFile: %v", err)
    }
    if _, err := io.WriteString(fw, content); err != nil {
        t.Fatalf("write: %v", err)
    }
    _ = w.Close()
    req := httptest.NewRequest(http.MethodPost, "/readings/csv-import", &body)
    return req, w.FormDataContentType()
}

func TestReadingsImport_正常CSVをパースしてJSONを返す(t *testing.T) {
    // 4 列: 計測時刻, 温度, 湿度, デバイス名
    csv := "2026-04-21T00:00:00Z,28.50,65.30,ハウスA\n" +
        "2026-04-21T01:00:00Z,29.10,66.00,ハウスA\n"
    req, ct := newCSVUpload(t, "csv_file", "readings.csv", csv)
    req.Header.Set("Content-Type", ct)

    r := newRouterWithUser(&ReadingsAPI{ /* ... */ }, 7)
    w := httptest.NewRecorder()
    r.ServeHTTP(w, req)

    if w.Code != http.StatusOK {
        t.Fatalf("status: got %d, body=%s", w.Code, w.Body.String())
    }
    // JSON レスポンス構造を検証
    var rows []struct {
        Temperature float64 `json:"temperature"`
        Humidity    float64 `json:"humidity"`
    }
    if err := json.Unmarshal(w.Body.Bytes(), &rows); err != nil {
        t.Fatalf("unmarshal: %v", err)
    }
    if len(rows) != 2 {
        t.Fatalf("rows = %d, want 2", len(rows))
    }
    if rows[0].Temperature != 28.50 {
        t.Errorf("rows[0].Temperature = %v, want 28.50", rows[0].Temperature)
    }
}
```

### 23.2 テストすべきパターン

| パターン | 期待 | 検証方法 |
|---------|------|---------|
| 正常 CSV（4 列） | 200 + JSON 配列 | `w.Code==200` + `json.Unmarshal` で件数/値 |
| 列数不一致 | 422 | `w.Code==http.StatusUnprocessableEntity` |
| 空ファイル | 422 | 同上 |
| DB 書込みなし確認（パースのみ） | Querier の Create 系が呼ばれない | モックの `createCalled` フラグが false |
| 固定値（is_own=false 等） | 全行 false | `json.Unmarshal` 後に各行のフィールドを検証 |

`encoding/csv` は `FieldsPerRecord` で列数を固定でき、不一致時に自動でエラーを返す。これを 422 に写像する。

```go
reader := csv.NewReader(f)
reader.FieldsPerRecord = 4 // 4 列固定。不一致行で err が返る → 422 に写像
```

### 23.3 注意: パースのみのケースは DB を書かない

S007A 相当（パース→JSON 返却→Alpine.js 配列更新のみ、最終保存は別ボタン）の設計では、取込ハンドラは DB に書かない。テストでは **Querier モックの Create 系が呼ばれていないこと**を固定する（Laravel の `assertDatabaseMissing` 相当 = DB 非依存で表現）。

```go
func TestReadingsImport_パースのみでDBに書かない(t *testing.T) {
    repo := &fakeReadingRepo{}
    r := newRouterWithUser(&ReadingsAPI{Repo: repo}, 7)
    req, ct := newCSVUpload(t, "csv_file", "readings.csv", "2026-04-21T00:00:00Z,28.50,65.30,ハウスA\n")
    req.Header.Set("Content-Type", ct)
    w := httptest.NewRecorder()
    r.ServeHTTP(w, req)

    if repo.createCalled {
        t.Error("パースのみのはずが CreateSensorReading が呼ばれた（DB 書込み混入）")
    }
}
```

### 23.4 DB 書込みを伴う CSV 全件ロールバックパターン（実 DB テスト）

> 【ADAPT】Laravel の `DB::transaction()` クロージャ + `ValidationException` throw による自動 rollback は、Go では `pgx` のトランザクション（`BeginTx` → 途中失敗で `Rollback`）に置き換わる。バルク INSERT + 全件ロールバックの教訓は通用する。これは Querier モックでは検証できないため、goose migrate 済みのテスト用 DB（接続文字列で本番/開発と分離）を使う数少ないケース。

#### 23.4.1 実装方針

1 行でも解決失敗（外部キー不在など）したら全件ロールバックする。pgx の tx を service 層で開き、ループ内で失敗したら `tx.Rollback(ctx)` する（defer Rollback + 成功時 Commit が定石）。

```go
func (s *ImportService) Import(ctx context.Context, rows [][]string) error {
    tx, err := s.pool.BeginTx(ctx, pgx.TxOptions{})
    if err != nil {
        return fmt.Errorf("BeginTx: %w", err)
    }
    defer tx.Rollback(ctx) // Commit 済みなら no-op

    q := s.q.WithTx(tx) // sqlc 生成の Querier をトランザクションに束ねる

    for lineNo, row := range rows {
        device, err := q.GetDeviceByName(ctx, row[3])
        if err != nil {
            // 1 行でも失敗したら error 返却 → defer Rollback で全件取消
            return fmt.Errorf("%d 行目: デバイス名が見つかりません", lineNo+1)
        }
        if _, err := q.CreateSensorReading(ctx, buildParams(device.ID, row)); err != nil {
            return fmt.Errorf("%d 行目: 保存に失敗しました", lineNo+1)
        }
    }
    return tx.Commit(ctx)
}
```

#### 23.4.2 UTF-8 BOM 検証（先頭 3 バイト）

Laravel の `withValidator()` での BOM 検査は、Go ではハンドラ/サービス入口でバイト列を見るか、`encoding/csv` に渡す前に BOM をスキップする。BOM 必須なら明示チェックする。

```go
// BOM 必須を強制する場合（Laravel の "UTF-8 BOM付きCSVを指定してください" 相当）
var utf8BOM = []byte{0xEF, 0xBB, 0xBF}

func requireBOM(head []byte) error {
    if len(head) < 3 || !bytes.Equal(head[:3], utf8BOM) {
        return errors.New("UTF-8 BOM 付きの CSV を指定してください")
    }
    return nil
}

// 逆に BOM を許容して読み飛ばす場合（多くはこちらが実用的）
content = bytes.TrimPrefix(content, utf8BOM)
```

> 補足: 拡張子/MIME 検証は Laravel の `mimes:csv,txt` / `extensions:csv` ルールに相当する仕組みが Gin の binding には無いため、ハンドラでファイル名拡張子 + 先頭バイトの sniff（`http.DetectContentType` 等）を自前で行う。

#### 23.4.3 テストパターン（実 DB）

| ケース | 期待 | 検証方法 |
|-------|------|---------|
| 正常 CSV（全行パス） | 全件 INSERT + フラッシュ | `q.CountSensorReadings(ctx, deviceID)` == N |
| 1 行エラー（デバイス名不在等） | 0 件登録 + 422 | テーブル件数 == 0（全件ロールバック） |
| UTF-8 BOM なし（必須運用時） | 422 + エラー文言 | レスポンス body にエラーメッセージ |
| 非 CSV（.xlsx 等） | 422 | 同上 |
| ファイル未選択 | 422 + 「ファイルを選択してください」 | 同上 |

```go
func TestImport_1行失敗で全件ロールバック(t *testing.T) {
    db := setupTestDB(t)      // goose migrate 済みテスト用 DB（接続文字列で分離）
    truncate(t, db, "sensor_readings")
    seedDevice(t, db, /* name */ "ハウスA")

    csv := "\xEF\xBB\xBF計測時刻,温度,湿度,デバイス名\n" +
        "2026-04-21T00:00:00Z,28.50,65.30,ハウスA\n" +    // 1 行目: 解決可
        "2026-04-21T01:00:00Z,29.10,66.00,存在しないハウス\n" // 2 行目: 解決不可 → 全件取消
    req, ct := newCSVUpload(t, "csv_file", "import.csv", csv)
    req.Header.Set("Content-Type", ct)
    w := httptest.NewRecorder()
    newRealDBRouter(db, /* userID */ 7).ServeHTTP(w, req)

    if w.Code != http.StatusUnprocessableEntity {
        t.Errorf("status: got %d, want 422", w.Code)
    }
    // 1 行目も含めて 0 件（ロールバック検証）
    if n := countRows(t, db, "sensor_readings"); n != 0 {
        t.Errorf("ロールバック後の件数 = %d, want 0", n)
    }
}
```

#### 23.4.4 監査ログとトランザクションの整合性

> 【Go では該当なし（直接の対応物）】`owen-it/laravel-auditing` の `HasAudit` トレイトのような ORM イベント連動の監査ログ機構は本プロジェクトに無い。

転用できる教訓: もし将来、監査ログを INSERT する処理を入れるなら、**同一トランザクション内（同じ `tx` の Querier）で書く**こと。そうすればロールバック時に監査行も一緒に取り消され、本処理と監査の整合性が保たれる。別トランザクション/別接続で監査を書くと、ロールバックされた処理の監査だけが残る不整合が起きる。ユーザー ID は session（scs）から取り出して明示的に Params に渡す（Laravel の `Auth::id()` 自動取得に相当するマジックは無い）。

---

## 24. UI 論理反転フィールドのテスト

> 【ADAPT】Laravel の `$request->boolean('exclude_weekend')` + 否定は、Gin の binding（`*bool` / チェックボックス値）+ 否定に読み替える。HTML チェックボックスが OFF 時にキー自体を送らない挙動はフロント共通なので教訓はそのまま通用する。

### 24.1 問題

UI のチェックボックスラベルと DB カラムの論理が反転する場合がある。スキーマには `devices.is_active`（boolean, NO, default true）があり、これを「無効化」ラベルで操作するケースが典型例になる。

| UI ラベル | DB カラム | 関係 |
|----------|----------|------|
| 「このデバイスを無効化」ON | `is_active = false` | 論理反転 |
| 「このデバイスを無効化」OFF | `is_active = true` | 論理反転 |

### 24.2 ハンドラでの処理（送信有無 → bool → 否定）

HTML フォームのチェックボックスは OFF のときキー自体を送らない。Gin では「キーがあれば true」を取り、否定して DB 値にする。

```go
type DeviceUpdateForm struct {
    Name     string `form:"name" binding:"required,max=255"`
    Disable  bool   `form:"disable"` // チェックON時のみ "1"/"on" が来る。未送信なら false
}

func (h *DeviceHandler) Update(c *gin.Context) {
    var form DeviceUpdateForm
    if err := c.ShouldBind(&form); err != nil {
        // ... バリデーションエラー再描画 ...
        return
    }
    params := repository.UpdateDeviceParams{
        ID:       deviceID,
        Name:     form.Name,
        IsActive: !form.Disable, // 論理反転: 「無効化」ON → is_active=false
    }
    // ...
}
```

### 24.3 テスト

論理反転は「ハンドラ → Querier Params の `IsActive`」で固定する。DB 不要、Querier モックが受け取った Params をキャプチャして検証する。

```go
type captureDeviceRepo struct {
    device     repository.Device
    gotParams  repository.UpdateDeviceParams
    updateCall bool
}

func (r *captureDeviceRepo) GetDevice(context.Context, int64) (repository.Device, error) {
    return r.device, nil
}
func (r *captureDeviceRepo) UpdateDevice(_ context.Context, p repository.UpdateDeviceParams) (repository.Device, error) {
    r.updateCall = true
    r.gotParams = p // Params をキャプチャ
    return r.device, nil
}

func TestDeviceUpdate_論理反転(t *testing.T) {
    tests := []struct {
        name         string
        form         string // application/x-www-form-urlencoded ボディ
        wantIsActive bool
    }{
        // チェック ON（disable=1）→ is_active=false
        {"無効化ON → is_active=false", "name=ハウスA&disable=1", false},
        // チェック OFF（disable キーなし）→ is_active=true
        {"無効化OFF → is_active=true", "name=ハウスA", true},
    }
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            repo := &captureDeviceRepo{device: repository.Device{ID: 10, UserID: 7}}
            r := newRouterWithUser(&DeviceHandler{Repo: repo}, 7)

            req := httptest.NewRequest(http.MethodPost, "/devices/10", strings.NewReader(tt.form))
            req.Header.Set("Content-Type", "application/x-www-form-urlencoded")
            w := httptest.NewRecorder()
            r.ServeHTTP(w, req)

            if !repo.updateCall {
                t.Fatal("UpdateDevice が呼ばれていない")
            }
            if repo.gotParams.IsActive != tt.wantIsActive {
                t.Errorf("IsActive = %v, want %v", repo.gotParams.IsActive, tt.wantIsActive)
            }
        })
    }
}
```

**注意:** HTML フォームのチェックボックスは OFF のとき**キー自体がリクエストに含まれない**。Gin の `bool` 型 binding は未送信時に false になるため、`!false = true` → `is_active = true` で正しく動作する。`*bool` を使うと「未送信 = nil」と「false 送信」を区別できるが、チェックボックスでは送信時必ず truthy なので素の `bool` で十分。method override（PUT/PATCH を POST + `_method` で表現、middleware/method_override_test.go）を併用する画面では、フォームに `_method` を含めること。

---

## 25. binding 構造体バリデーションの単体テストパターン

**背景:** Laravel の S007B では FormRequest（`ContractUpdateRequest`）を導入し、バリデーションルールをコントローラーから独立して単体テストできるようにした。Go には FormRequest 相当の仕組みは無い（HTMX実装ガイド §7「採用方針」参照）。本プロジェクトでは binding タグ付き構造体 + go-playground/validator でバリデーションし、`validator.New().Struct(s)` を直接呼ぶことで Handler から独立した単体テストを書く。日本語化は `internal/handler/auth_validation.go` の field+tag マップ（`validationMessage` / `translateValidationErrors`）に集約する。

> Laravel: `Validator::make($data, $request->rules(), $request->messages())` → Go: 検証は `validator.New().Struct(s)`、メッセージ変換は `translateValidationErrors(err)`。ルールは構造体の `binding:"..."` タグに宿るので、テスト対象は「タグの付き方」と「翻訳マップ」の2層になる。

### 25.1 基本パターン: validator.Struct で直接検証（ADAPT）

binding 構造体に対して `validator.New().Struct()` を呼び、`validator.ValidationErrors` のフィールド名・タグを検証する。`validData()` 相当のヘルパで有効な構造体のベースを作り、サブテストごとに 1 フィールドだけ壊す。

```go
package handler

import (
	"testing"

	"github.com/go-playground/validator/v10"
)

// loginInput は LoginPost が c.ShouldBind で受ける binding 構造体（実コードと同じタグを写経する）。
type loginInput struct {
	Email    string `form:"email"    binding:"required,email"`
	Password string `form:"password" binding:"required"`
}

// validLoginInput は有効データのベース。overrides で差分だけ与える。
func validLoginInput(mut func(*loginInput)) loginInput {
	in := loginInput{Email: "user@example.com", Password: "password123"}
	if mut != nil {
		mut(&in)
	}
	return in
}

func TestLoginInput_有効データは通過(t *testing.T) {
	v := validator.New()
	if err := v.Struct(validLoginInput(nil)); err != nil {
		t.Fatalf("有効データで検証失敗: %v", err)
	}
}

func TestLoginInput_フィールド単位の必須検証(t *testing.T) {
	// Laravel の test_client_id_is_required 相当: フィールド名がエラーに含まれることを確認
	tests := []struct {
		name      string
		mut       func(*loginInput)
		wantField string // validator.FieldError.Field() の値
		wantTag   string
	}{
		{"email 空は required", func(in *loginInput) { in.Email = "" }, "Email", "required"},
		{"email 形式不正は email", func(in *loginInput) { in.Email = "not-an-email" }, "Email", "email"},
		{"password 空は required", func(in *loginInput) { in.Password = "" }, "Password", "required"},
	}
	v := validator.New()
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			err := v.Struct(validLoginInput(tt.mut))
			if err == nil {
				t.Fatal("検証は失敗するはずが通過した")
			}
			var ve validator.ValidationErrors
			if !errorsAs(t, err, &ve) {
				return
			}
			fe := findFieldError(ve, tt.wantField)
			if fe == nil {
				t.Fatalf("%q のエラーが見つからない: %v", tt.wantField, ve)
			}
			if fe.Tag() != tt.wantTag {
				t.Errorf("tag = %q, want %q", fe.Tag(), tt.wantTag)
			}
		})
	}
}
```

**ポイント:**
- `validData()` ヘルパは「ベース構造体 + `func(*T)` で差分注入」の形が table-driven と相性が良い（イミュータブルに 1 フィールドだけ変える）。
- Laravel の `exists:clients,id` のような DB 参照ルールは validator のタグでは表現しない。本プロジェクトでは「存在チェックは Handler/service が Querier 越しに行う」ので、DB 依存の検証は Querier モックを使った Handler テスト（§25.3）側で担保する。
- Laravel の `$request->authorize()` に相当する認可は、本プロジェクトでは binding ではなく `internal/authz`（所有者認可）で行う。バリデーション単体テストに認可を混ぜない（責務分離）。

### 25.2 条件付きバリデーション（required_if 相当）のテスト（ADAPT）

validator は `required_if`・`required_with`・`required_unless` をタグで表現できる。Laravel の「register_tecris=true のとき number が必須」は Go では `binding:"required_if=RegisterX true"` に読み替える。アラート機能を例にすると「`is_enabled=true` のとき `threshold` 必須」のように書ける。

```go
type alertRuleInput struct {
	Metric    string  `form:"metric"    binding:"required,oneof=temperature humidity"`
	Operator  string  `form:"operator"  binding:"required,oneof=> < >= <="`
	IsEnabled bool    `form:"is_enabled"`
	Threshold float64 `form:"threshold" binding:"required_if=IsEnabled true"`
}

func TestAlertRuleInput_有効時はthreshold必須(t *testing.T) {
	v := validator.New()
	// IsEnabled=true なのに Threshold ゼロ値 → required_if で失敗
	err := v.Struct(alertRuleInput{Metric: "temperature", Operator: ">", IsEnabled: true})
	if err == nil {
		t.Fatal("is_enabled=true で threshold 未指定なら失敗するはず")
	}
	var ve validator.ValidationErrors
	if errorsAs(t, err, &ve) {
		if fe := findFieldError(ve, "Threshold"); fe == nil || fe.Tag() != "required_if" {
			t.Errorf("Threshold の required_if エラーが必要: %v", ve)
		}
	}
}

func TestAlertRuleInput_無効なら threshold 不要(t *testing.T) {
	v := validator.New()
	// IsEnabled=false なら threshold ゼロ値でも通過
	if err := v.Struct(alertRuleInput{Metric: "temperature", Operator: ">", IsEnabled: false}); err != nil {
		t.Errorf("is_enabled=false なら threshold 不要のはず: %v", err)
	}
}
```

> `oneof=temperature humidity` / `oneof=> < >= <=` は `alert_rules` の CHECK 制約（`metric_valid` / `operator_valid`、`docs/database_snapshot/table_definitions.md` §alert_rules）と**同じ許容リスト**を写経すること。タグと DB CHECK が二重防御になる。許容値そのものは `internal/domain`（`Metric` / `ComparisonOperator`）の `Valid()` でも検証できるので、列挙の単体テストは §21 系（domain 型）側に寄せ、binding テストでは「タグが効いているか」だけを確認する。

### 25.3 クロス項目・DB 依存バリデーションのテスト（ADAPT）

Laravel のカスタムクロージャ（「成績評定登録にはテクリス登録が前提」のような項目間相関）に相当するロジックは、validator のタグで書けない範囲は Handler/service 内で手書きし、Querier モックを使ってテストする。validator の `eqfield`（パスワード一致）のようにタグで書ける相関はタグで、DB 状態に依存する相関は Handler テストで担保する。

```go
// register binding は確認用パスワード一致を eqfield で表現（auth_validation.go と整合）
type registerInput struct {
	Name                 string `form:"name"                  binding:"required,max=255"`
	Email                string `form:"email"                 binding:"required,email"`
	Password             string `form:"password"              binding:"required,min=8"`
	PasswordConfirmation string `form:"password_confirmation" binding:"required,eqfield=Password"`
}

func TestRegisterInput_確認用不一致はeqfield(t *testing.T) {
	v := validator.New()
	err := v.Struct(registerInput{
		Name: "太郎", Email: "a@example.com",
		Password: "password123", PasswordConfirmation: "different1",
	})
	var ve validator.ValidationErrors
	if errorsAs(t, err, &ve) {
		if fe := findFieldError(ve, "PasswordConfirmation"); fe == nil || fe.Tag() != "eqfield" {
			t.Errorf("PasswordConfirmation の eqfield エラーが必要: %v", ve)
		}
	}
}
```

> DB の状態に依存する相関（例: メール重複は `exists`、所有デバイスへの紐付けは存在確認）は binding では検出できない。これは `fakeAuthRepo` / `fakeSensorRepo` のような Querier モックを使った Handler テスト（`auth_test.go` の `TestRegisterPost_メール重複は200でエラー`、`sensor_api_test.go` の `TestSensorAPI_Create_存在しないデバイスは422`）で担保する。バリデーション層と DB 整合層のテストを混ぜない。

### 25.4 日本語メッセージの検証（PORT）

Laravel は `$request->messages()` をアサートした。本プロジェクトの相当は `auth_validation.go` の `validationMessage(field, tag)` / `translateValidationErrors(err)`。これは `auth_validation_test.go` で table-driven の単体テスト済み。新しいフィールド・タグを追加したら、必ず翻訳マップとそのテストを併せて更新する。

```go
func TestValidationMessage_日本語であること(t *testing.T) {
	tests := []struct{ field, tag, want string }{
		{"email", "required", "メールアドレスを入力してください"},
		{"password", "min", "8文字以上で入力してください"},
		{"email", "unknown_tag", "入力内容を確認してください"}, // フォールバック
	}
	for _, tt := range tests {
		if got := validationMessage(tt.field, tt.tag); got != tt.want {
			t.Errorf("validationMessage(%q,%q) = %q, want %q", tt.field, tt.tag, got, tt.want)
		}
		// 「ください」を含む = 日本語メッセージである、の最低限チェック（Laravel 版と同じ観点）
	}
}
```

**ヘルパ補足（本節で使った小道具）:** `errorsAs` / `findFieldError` は各テストファイル冒頭に置く小ヘルパ。

```go
func errorsAs(t *testing.T, err error, target *validator.ValidationErrors) bool {
	t.Helper()
	if errors.As(err, target) {
		return true
	}
	t.Fatalf("validator.ValidationErrors ではない: %v", err)
	return false
}

func findFieldError(ve validator.ValidationErrors, field string) validator.FieldError {
	for _, fe := range ve {
		if fe.Field() == field {
			return fe
		}
	}
	return nil
}
```

> 📅 2026-04-01（原本）— S007B FormRequest で確立した「ルールをコントローラーから独立検証」「日本語メッセージ検証」という教訓は、Go では「binding タグ + validator.Struct」「auth_validation.go の翻訳マップ + そのテスト」へそのまま移植する。

---

## 26. JSON 一括保存（hidden input → サーバー一括反映）のテストパターン

**背景:** S007B では Alpine.js 配列 → JSON hidden input → サーバー一括保存パターンを確立。これは HTMX実装ガイドの「Alpine.js 一括保存型」（同ガイドの x-for インライン編集テーブル + `<input type="hidden" name="rules_json" x-bind:value="JSON.stringify(rules)">`）に対応する。本プロジェクトの代表例はアラートルール一括保存（`alert_rules` テーブル）。Handler は `rules_json` を文字列フィールドで受け取り `encoding/json` でデコードしてから保存するため、テストは「JSON 文字列をフォーム値として送る」形になる。

このパターンは DB への一括反映ロジックの検証なので、原則は **Querier モックで「どの Params で何回呼ばれたか」を検証**し、実 DB を要する全置換（delete→insert）の整合は実 DB テスト（§26.5）で補う。

### 26.1 基本: JSON をフォーム値として送り、デコード結果を検証（ADAPT）

```go
// fakeAlertRuleRepo は一括保存に必要な操作のみ実装した Querier モック。
type fakeAlertRuleRepo struct {
	deletedDeviceID int64                                   // 全置換で削除対象になった device_id
	created         []repository.CreateAlertRuleParams      // 作成された行の記録
}

func (f *fakeAlertRuleRepo) SoftDeleteAlertRulesByDevice(_ context.Context, deviceID int64) error {
	f.deletedDeviceID = deviceID
	return nil
}

func (f *fakeAlertRuleRepo) CreateAlertRule(_ context.Context, arg repository.CreateAlertRuleParams) (repository.AlertRule, error) {
	f.created = append(f.created, arg)
	return repository.AlertRule{ID: int64(len(f.created))}, nil
}

func TestAlertRules_一括保存でJSONがデコードされ保存される(t *testing.T) {
	repo := &fakeAlertRuleRepo{}
	// 認証済み + 所有デバイス前提のルータを組む（sensor_api_test.go の newRouterWithUser を踏襲）
	app := newAlertRulesApp(repo, /*userID=*/ 7, /*ownedDeviceID=*/ 10)

	rulesJSON := `[{"metric":"temperature","operator":">","threshold":35.0}]`
	w := postForm(app, "/devices/10/alert-rules", url.Values{"rules_json": {rulesJSON}})

	if w.Code != http.StatusSeeOther {
		t.Fatalf("status = %d, want 303 (body=%s)", w.Code, w.Body.String())
	}
	if len(repo.created) != 1 {
		t.Fatalf("作成行数 = %d, want 1", len(repo.created))
	}
	got := repo.created[0]
	if got.DeviceID != 10 || got.Metric != "temperature" || got.Operator != ">" {
		t.Errorf("保存 Params が不正: %+v", got)
	}
	// numeric(5,2) は pgconv.Numeric2 で詰める。floatに戻して比較する。
	if v := pgconv.NumericToFloat(got.Threshold); v != 35.0 {
		t.Errorf("threshold = %v, want 35.0", v)
	}
}
```

> Laravel: `put('/contract/update/{id}', ['amount_details_json' => json_encode([...])])` → Go: `postForm(app, path, url.Values{"rules_json": {jsonStr}})`。`assertDatabaseHas` の代わりに、Querier モックが受け取った `CreateAlertRuleParams` を直接アサートする。`metric`/`operator` は `alert_rules` の CHECK 許容リストに整合する値だけを使う。

### 26.2 既存レコードの全置換（delete → create）の検証（ADAPT）

Laravel は「論理削除して 1 件だけ残る」を `count()` で確認した。Querier モックでは「全削除が呼ばれた」+「新規 create が指定件数」の両方を検証する。**呼び出し順序**（delete が先）も保存ロジックの肝なので、フラグではなく記録順で確認できると堅い。

```go
func TestAlertRules_一括保存は全削除してから新規作成(t *testing.T) {
	repo := &fakeAlertRuleRepo{}
	app := newAlertRulesApp(repo, 7, 10)

	rulesJSON := `[{"metric":"humidity","operator":"<","threshold":40.0}]`
	postForm(app, "/devices/10/alert-rules", url.Values{"rules_json": {rulesJSON}})

	if repo.deletedDeviceID != 10 {
		t.Errorf("全置換のため device 10 の既存ルール削除が必要: deletedDeviceID=%d", repo.deletedDeviceID)
	}
	if len(repo.created) != 1 {
		t.Errorf("新規作成は1件のはず: %d", len(repo.created))
	}
}
```

> 「古い行が消えて新しい行だけ残る」という最終状態の整合は、論理削除（`deleted_at`）+ 再 INSERT を実 DB で回さないと完全には固定できない。モックでは「delete と create の呼び出し」を、実 DB テスト（§26.5）では「最終的に有効行が 1 件」を検証する二段構えにする。

### 26.3 空配列の送信（PORT）

「全行クリア」のケース。空配列でも全削除は呼ばれ、create は 0 件であることを確認する。HTMX実装ガイドのチェックリストでも空配列ケースは必須項目。

```go
func TestAlertRules_空配列で全削除のみ実行され作成は0件(t *testing.T) {
	repo := &fakeAlertRuleRepo{}
	app := newAlertRulesApp(repo, 7, 10)

	w := postForm(app, "/devices/10/alert-rules", url.Values{"rules_json": {"[]"}})
	if w.Code != http.StatusSeeOther {
		t.Fatalf("status = %d, want 303", w.Code)
	}
	if repo.deletedDeviceID != 10 {
		t.Error("空配列でも全削除は実行されるべき")
	}
	if len(repo.created) != 0 {
		t.Errorf("空配列なら作成 0 件のはず: %d", len(repo.created))
	}
}
```

### 26.4 連動計算（サーバーサイドが正）のテスト（ADAPT）

Laravel の「変更記録 → 契約金額の再計算」は「クライアント送信値ではなくサーバー側集計が正」という教訓。本プロジェクトの相当は「アラート判定はサーバー側 domain ロジックで再評価する」（クライアントのプレビューは信用しない）。連動計算ロジックは純粋関数（domain）に切り出して単体テストし、Handler では「保存値から正しく算出されるか」を検証する。

```go
// 例: しきい値超過の集計など「サーバーで再計算する値」を domain の純関数で検証する。
// クライアントがプレビューで出した値は信用せず、保存後にサーバーが Evaluate し直す前提。
func TestAlertEvaluation_保存済み閾値で再評価(t *testing.T) {
	op, err := domain.ParseComparisonOperator(">")
	if err != nil {
		t.Fatal(err)
	}
	// 35.00 のしきい値に対する境界。サーバー側が actual を再評価する。
	if !op.Evaluate(35.01, 35.00) {
		t.Error("35.01 > 35.00 はサーバー側で true になるべき")
	}
	if op.Evaluate(35.00, 35.00) {
		t.Error("等値は > では false（クライアントのプレビューに引きずられない）")
	}
}
```

**ポイント:**
- 連動計算・判定はサーバーサイドが正（クライアントは UX プレビューのみ）。再計算ロジックは Handler ではなく `internal/domain` の純関数に置き、境界値を `comparison_operator_test.go` 流に単体テストする。
- numeric(5,2) の値（threshold 等）は `pgconv.Numeric2` で詰め、比較は `pgconv.NumericToFloat` で float に戻して `math.Abs(diff) < 0.001` で評価する（`pgconv_test.go` 参照）。文字列等価比較や生 float == は丸め誤差で偽陰性になりうる。
- フィールド名が英数字なら JSON エンコードで日本語エスケープを気にする必要は無い。値に日本語（デバイス名等）が混ざる場合は §10-E / 33.6 の `<,>,&` エスケープ観点を流用する。

### 26.5 楽観ロック（sync_token 相当）の競合検出テスト（ADAPT）

Laravel は `sync_token = updated_at->timestamp` を送って楽観ロックした。本プロジェクトには専用の version 列は無い（`docs/database_snapshot/table_definitions.md` に sync_token / version 列は存在しない）。楽観ロックを導入する場合は **`updated_at` を WHERE 条件に入れた UPDATE の RowsAffected==0 で競合検出**する設計に読み替える。pgx の `CommandTag.RowsAffected()` を返す Exec 系メソッドを Querier に持たせ、モックで 0 を返して競合経路を固定する。

```go
// updated_at をガードに入れた UPDATE が 0 行 → 競合（他者が先に更新）を表現するモック。
type fakeOptLockRepo struct{ rowsAffected int64 }

func (f fakeOptLockRepo) UpdateAlertRuleIfUnchanged(
	_ context.Context, _ repository.UpdateAlertRuleIfUnchangedParams,
) (int64, error) {
	return f.rowsAffected, nil // 実装は pgx CommandTag.RowsAffected() を返す
}

func TestAlertRule_楽観ロック競合は409(t *testing.T) {
	repo := fakeOptLockRepo{rowsAffected: 0} // 0 = 既に他者が更新済み
	app := newAlertRuleUpdateApp(repo, 7, 10)

	w := postForm(app, "/devices/10/alert-rules/1",
		url.Values{"threshold": {"40"}, "expected_updated_at": {"2026-06-01T00:00:00Z"}})
	if w.Code != http.StatusConflict { // 409 で競合を返す設計の場合
		t.Errorf("status = %d, want 409 (competing update)", w.Code)
	}
}
```

> Laravel: `sync_token` 未送信時のバイパス検証 → Go: `expected_updated_at` 未送信時に WHERE ガードが効かず無条件 UPDATE にならないか（= ガードのバイパス）を必ずテストする。RowsAffected==0 を「競合」と「対象不存在」のどちらに割り当てるかは設計判断なので、両ケースのテストを書き分ける。

> 📅 2026-04-01（原本）— S007B 4テーブル一括保存で確立。Go では「JSON をフォーム値として送り、Querier モックの受領 Params をアサート」+「全置換の最終状態は実 DB テスト」+「連動計算は domain 純関数で境界値テスト」の3点に再構成する。

---

## 27. チェックリスト（本プロジェクト版）

新しい Handler/templ テストを書く際の確認事項（本プロジェクト＝Gin/templ/sqlc/pgx に合わせて読み替え済み）:

- [ ] **API（JSON）と Web（HTML）でハンドラを分けたか**。API は `c.JSON(code, ...)` でステータスをそのまま返す。Web のバリデーション失敗は 200 で HTML 再描画、または 422 + 部分 HTML（設計判断）。Laravel の「`response()->json([],4xx)` が Web ルートで 200 に化ける」罠は Gin には無いが、HTML/JSON の分岐方針はテストで固定する（§28 / HTMX実装ガイド §7）
- [ ] **HTMX 部分返却（fragmentIf 相当）のテスト**で、フル HTML ではなく部分用 templ コンポーネントが Render されることを `strings.Contains` で検証したか（`HX-Request: true` ヘッダで分岐）
- [ ] 論理削除（`deleted_at`）を扱うクエリで、SELECT に `deleted_at IS NULL` が入っているか（`db/queries/*.sql` 参照）。削除済みが混ざらないことをテストで確認したか
- [ ] 必要な Querier モックメソッドを実装したか（埋め込み `repository.Querier`（nil）+ 必要メソッドだけオーバーライド。`cmd/server/integration_test.go` の `fakeUserQuerier` 方式）
- [ ] **API のバリデーションエラーが 422 で返るか**（`sensor_api_test.go` の `TestSensorAPI_Create_ValidationFailureReturns422`）。JSON 構文エラーは 400、型不一致も 400 と区別したか
- [ ] HTMX 分岐のあるエンドポイントで、`HX-Request: true` あり/なしの**両方のレスポンスパターン**をテストしたか
- [ ] 連鎖削除で子エンティティが存在しないケース（pgx.ErrNoRows / 空スライス）もテストしたか
- [ ] LEFT JOIN するクエリの JOIN/WHERE 条件に `deleted_at IS NULL` を含めたか（論理削除対応）。PostgreSQL では JOIN 後の NULL 判定がそのまま効く
- [ ] **所有者認可（BOLA）**を通したか。他人のリソースは 403/404、未認証（userID=0）は 401、保存系は「認可失敗時に Querier の書き込みが呼ばれない」ことまで検証したか（`sensor_api_test.go` の `他ユーザーのデバイスは403かつ保存しない`、`authz/ownership_test.go`）
- [ ] templ コンポーネントの条件分岐（空状態 `empty-message` 等）に合わせた期待値になっているか（`page_test.go` 参照）
- [ ] テスト用の文字列（デバイス名等）が出力 HTML 内で**一意**か（`strings.Index` でのソート順検証に影響。§31）
- [ ] 検証用に追加したルート/ハンドラを後で削除する場合、`cmd/server` のルート登録箇所と該当 handler の両方を削除したか
- [ ] 削除後に `go build ./...` と全テストが通り、削除したシンボル名がコンパイルエラーに出ないことを確認したか
- [ ] `required_with` / `required_if` バリデーションで3パターン（一部入力/全空/全入力）をテストしたか（§25.2）
- [ ] ファイルダウンロードテストで Content-Type / Content-Disposition と本文の先頭（BOM 等）を検証したか（§33）
- [ ] `<fieldset disabled>` 等の有効/無効状態を出力 HTML の `strings.Contains` / `!strings.Contains` で検証したか
- [ ] Alpine.js のクライアントロジックは、JS コード文字列の**存在確認**（`strings.Contains`）に留めているか（実 DOM 操作の検証は将来の chromedp/Playwright。§29）
- [ ] 楽観ロック（`updated_at` ガード UPDATE）で expected 値未送信時のバイパスをテストしたか（§26.5）
- [ ] 共有 templ コンポーネント変更時に、コンポーネント単体テスト（`render(t, Comp(...))`）+ Handler 統合テストの両方で検証したか
- [ ] 実 DB テストで DDL（DROP/RENAME）を発行していないか（tx ロールバック/TRUNCATE 方針を崩す）。テスト用 DB は接続文字列で本番/開発と分離したか
- [ ] テストで使うカラム名・型が `docs/database_snapshot/table_definitions.md` と一致しているか（存在しないカラム/型を発明していないか）
- [ ] CSS クラス名の否定検証で、短いクラス名がサブストリングとして誤マッチしないか（必要なら `regexp` で語境界を見る。§31 独自クラス禁止＝モック写経を守る）
- [ ] `!strings.Contains` の対象文字列が `<option>` 等にも出現しないか（行内だけを検証したい場合は出力をセグメント抽出してから判定。§32）
- [ ] `hx-target` がフラグメントの部分要素を指していないか（フル部分がネストして DOM が壊れる。HTMX実装ガイド該当節）
- [ ] JOIN/ORDER BY ソートのテストで、名前順とカテゴリ順が逆転するデータを使い、デフォルトフォールバックで偶然通る偽陽性を防いだか（§31）
- [ ] Alpine.js / hx-vals に Go の値を渡す箇所で、生連結ではなく `templ.JSONString` / `json.Marshal` を使い、`<script>` タグ混入の XSS 検証（`!strings.Contains(body, "<script>")`）を書いたか（§33.6 / HTMX実装ガイド §10-C・§10-E）
- [ ] `<script type="application/json">` の中身が**純 JSON**で、`JSON.parse(` のような JS 式が混ざっていないことを `!strings.Contains(body, "JSON.parse(")` で確認したか（HTMX実装ガイド §10-E）
- [ ] domain 型（`Metric` / `ComparisonOperator`）の比較は型値（`OpGreaterThan` 等）で、DB 文字列の比較は許容リスト文字列（`">"` 等）で行い、両者を混同していないか（§21 系）
- [ ] **CSRF**: 通常フォームのテストで GET → トークン取得 → POST 往復をしているか。dev(HTTP) では `csrf.PlaintextHTTPRequest(req)` 相当の HTTP 許可設定が要る点を踏まえたか（§28）
- [ ] API（Bearer）エンドポイントは CSRF 免除になっているか（device token 経路。§28）
- [ ] HTMX フォームのバリデーション失敗テストで、サーバーが返すステータス（200+HTML か 422+部分 HTML）が設計と一致しているか
- [ ] 認証ガードのテストで、Web 未ログインは 302 `/login`、API 未認証は 401 を返すことを区別したか（`auth_test.go` / `device_auth_test.go`）
- [ ] NULL を取りうる nullable 列（`location` / `last_communicated_at` / `deleted_at` 等）のフィルタで NULL ケースを検証したか（§30）
- [ ] NULL 末尾ソートのテストで NULL 値が末尾に来ることを位置で検証したか（§31）

> 以下は spatie/RBAC・Tom Select・FiltersListData 等、本プロジェクトに**対応物が無い**Laravel 固有チェック項目。教訓だけ残す。
>
> 【Go では該当なし】spatie 権限テストの `givePermissionTo()` + `forgetCachedPermissions()` キャッシュ汚染 — RBAC 未導入。「権限テスト」は §27 の所有者認可（BOLA）チェックに集約。
>
> 【Go では該当なし】Tom Select / `js-tom-select` クラス、`FiltersListData::applyBranchFilter` の既定カラム名 — 採用ライブラリ・共通フィルタ基盤が無い。教訓「フィルタの既定対象列が業務的に正しいか確認する」だけ転用。

---

## 28. CSRF テストの作法（gorilla/csrf）

> Laravel: PHPUnit が CSRF トークンを**自動補完**するため、`X-CSRF-TOKEN` を空にしても 419 にならず、個別画面で CSRF を検証する意味が無かった。Go（gorilla/csrf + httptest）は**自動補完しない**ので、CSRF は実際にテスト可能であり、テストする価値がある。ここが Laravel と最も挙動が異なる点。

### 28.1 基本: GET でトークン取得 → POST 往復（ADAPT/PORT）

httptest はトークンを補完しないので、CSRF の正常系・異常系をそのまま検証できる。`internal/middleware/csrf_test.go` の作法に従う。

```go
func newCSRFRouter() *gin.Engine {
	gin.SetMode(gin.TestMode)
	cfg := &config.Config{AppEnv: "development", SessionSecret: "test-secret-for-csrf-key-derivation"}
	r := gin.New()
	grp := r.Group("/", CSRF(cfg))
	grp.GET("/form", func(c *gin.Context) { c.String(http.StatusOK, csrf.Token(c.Request)) })
	grp.POST("/submit", func(c *gin.Context) { c.String(http.StatusOK, "ok") })
	return r
}

func TestCSRF_トークン無しのPOSTは403(t *testing.T) {
	r := newCSRFRouter()
	w := httptest.NewRecorder()
	r.ServeHTTP(w, httptest.NewRequest(http.MethodPost, "/submit", nil))
	if w.Code != http.StatusForbidden {
		t.Fatalf("トークン無し POST: got %d, want 403", w.Code)
	}
}

func TestCSRF_GETでトークン取得しPOSTが通過(t *testing.T) {
	r := newCSRFRouter()
	// 1. GET でトークン + cookie を取得（GET は安全メソッドなので通過）
	gw := httptest.NewRecorder()
	r.ServeHTTP(gw, httptest.NewRequest(http.MethodGet, "/form", nil))
	token := gw.Body.String()
	cookies := gw.Result().Cookies()
	// 2. トークン（ヘッダ）+ cookie を付けて POST → 通過
	req := httptest.NewRequest(http.MethodPost, "/submit", nil)
	req.Header.Set("X-CSRF-Token", token)
	for _, ck := range cookies {
		req.AddCookie(ck)
	}
	w := httptest.NewRecorder()
	r.ServeHTTP(w, req)
	if w.Code != http.StatusOK {
		t.Fatalf("トークン付き POST: got %d, want 200 (body=%s)", w.Code, w.Body.String())
	}
}
```

### 28.2 開発環境（HTTP）の罠（PORT — Go 固有の重要ノウハウ）

gorilla/csrf は既定で **Secure cookie（HTTPS 前提）**を出すため、dev(HTTP) では cookie がセットされず往復が常に 403 になる。本プロジェクトの `CSRF(cfg)` ミドルウェアは `AppEnv == "development"` のとき `csrf.Secure(false)` 相当を有効にする実装で、テストでも `cfg.AppEnv = "development"` を渡す（`csrf_test.go` の `newCSRFRouter` と同じ）。

> Laravel 版の「フレームワークが自動処理するので個別テスト不要」という結論は Go では逆転する。Go では (1) GET → トークン取得 → POST 往復が通る、(2) トークン無し POST が 403、(3) dev(HTTP) でも Secure cookie の罠を踏まずに往復できる、の3点を**ミドルウェア単体テストで担保する**。個別画面ごとに 403 を再検証する必要は無い（ミドルウェアで一元化済みなら）点だけ Laravel と同じ。

### 28.3 通常フォームと HTMX と API の差（ADAPT）

| 経路 | CSRF の扱い | テスト観点 |
|------|------------|-----------|
| 通常フォーム（login/register/logout 等） | hidden `name="gorilla.csrf.Token"` を埋め込む（gorilla 既定。HTMX実装ガイド §8-A） | templ 出力に `name="gorilla.csrf.Token"` とトークン値が含まれるか（`page_test.go` / `layout_test.go` で検証済み） |
| HTMX フォーム | `<meta name="csrf-token">` + `htmx:configRequest` で `X-CSRF-Token` を全リクエストに自動付与（個別 hidden 不要） | App レイアウトに meta タグと `htmx:configRequest` が出力されるか（`layout_test.go` の `TestApp_共通要素を描画`） |
| API（Bearer / device token） | **CSRF 免除**。CSRF ミドルウェアを通さないルートグループに分離 | API グループに CSRF が掛かっていない（トークン無しでも 403 にならない）こと |

> HTMX 統合テストでは、まず通常 GET でページを取得して `extractCSRFToken(html)`（`integration_test.go` 参照）でトークンを抜き、続く HTMX POST に `X-CSRF-Token` ヘッダ + cookie を載せて往復させる。実際の運用フローと同じ経路をテストでなぞる。

---

## 29. E2E（ブラウザ自動化）【現状不使用】

> 📅 2026-04-03（原本）— Laravel Dusk は「HTMX + Selenium の合成クリック非互換」「DatabaseMigrations が本番 DB を破壊しうる」等の運用コストから不採用とされた。

> 【Go では該当なし】Laravel Dusk / Selenium standalone-chrome コンテナ / `php artisan dusk` / `.env.dusk.local` — 本プロジェクトに対応物は無い。E2E 自動化は将来 chromedp または Playwright を採用予定（現状不使用）。開発時の画面目視は **MCP chrome-devtools** で行う。

転用できる教訓（将来 chromedp/Playwright を入れる際の注意として残す）:

### 29.1 HTMX + 合成イベントの互換性（転用）

Selenium のネイティブクリックで HTMX の `hx-post` が発火しないことがあった（HTMX はイベント委譲/MutationObserver 依存で、合成クリックがバブリングしない場合がある）。

> 転用: 将来 chromedp/Playwright で HTMX 操作を自動化する場合も、ボタンの合成クリックがイベント委譲に届かないケースに備える。回避策として `page.evaluate`（chromedp なら `chromedp.Evaluate`）で `fetch` を直接叩き、`X-CSRF-Token`（`<meta name="csrf-token">` から取得）と本文を送ってレスポンスを DOM に差し込む手がある。ただし第一選択は HTMX の発火条件を満たす本物の操作で、`fetch` 直叩きは最終手段。

### 29.2 テスト DB の分離（転用 — 最重要）

Dusk が `migrate:fresh` で本番 DB を破壊する事故があった。Go の E2E でも、ブラウザ経由のリクエストはテストプロセスと**別トランザクション**で動くため tx ロールバックが効かない。

> 転用: E2E 用 DB は**接続文字列で本番/開発から必ず分離**し、`setUp` 相当でデータを INSERT、`tearDown` 相当で対象テーブルを TRUNCATE（または論理削除のクリーンアップ）する。`make` タスクで E2E 用 DB を goose migrate してから流す。Laravel の `forceDelete()` に相当するのは「物理 DELETE / TRUNCATE」。

### 29.3 動的 DOM（Alpine.js）初期化（転用）

HTMX で挿入したフラグメント内の Alpine.js `x-data` は通常自動初期化されるが、`innerHTML` 直接操作では初期化が走らないことがあった。

> 転用: 将来 E2E で動的挿入後の Alpine 状態を検証する場合、サーバーサイドで `<script type="application/json">`（純 JSON、HTMX実装ガイド §10-E）にデータを供給してフラグメント内で完結させるか、`Alpine.initTree(el)` を明示実行する。本プロジェクトの方針は「データ供給は属性連結でなく JSON script タグに集約」なので、E2E でもこの前提で書く。

---

## 30. NULL を取りうる nullable 列のフィルタテストパターン

> 📅 2026-04-03（原本）— Laravel では `is_unused tinyint(1) NULL` のフィルタで「null レコードが除外される」罠を踏んだ。本プロジェクトに `is_unused` 列は無い（`docs/database_snapshot/table_definitions.md` 参照）。教訓は **nullable 列を条件に使うと NULL が暗黙除外される**点であり、本プロジェクトの実 nullable 列（`devices.location` / `devices.last_communicated_at` / `device_tokens.expires_at` 等）に読み替えて適用する。

### 30.1 問題（ADAPT）

PostgreSQL でも `WHERE col = false` や `WHERE col <> 'x'` は **NULL を取りこぼす**（3値論理で `NULL = false` も `NULL <> 'x'` も `UNKNOWN` を返し、WHERE では偽扱い）。MariaDB と同じ罠で、PostgreSQL の方がむしろ厳格。

例えば「`last_communicated_at` が未設定（NULL = 一度も通信していない）デバイスも一覧に含めたい」のに、`WHERE last_communicated_at < $1` のような条件を素朴に書くと NULL のデバイスが消える。

### 30.2 実装パターン（ADAPT）

NULL を含めたい場合は `IS NULL` を OR で明示するか、`COALESCE` で既定値に寄せる。これは sqlc クエリ（`db/queries/*.sql`）側で表現する。

```sql
-- 例: 一度も通信していない(NULL) デバイスも「対象」に含めたい
-- name: ListDevicesNeedingAttention :many
SELECT * FROM devices
 WHERE user_id = $1
   AND deleted_at IS NULL
   AND (last_communicated_at < $2 OR last_communicated_at IS NULL)  -- ← IS NULL を明示
 ORDER BY last_communicated_at ASC NULLS FIRST;
```

### 30.3 テストパターン（3状態必須）（PORT）

NULL を取りうる列は **値あり / 別の値 / NULL の3状態**を必ずテストする。NULL ケースを忘れると本番で取りこぼす。Querier モックでも実 DB でも、3状態のレコードを用意して「表示される/されない」を確認する。

```go
func TestListDevices_NULLセーフフィルタ_3状態(t *testing.T) {
	// 実 DB テスト想定（goose migrate 済みテスト DB）。fixture builder で3状態を作る。
	cutoff := time.Date(2026, 6, 1, 0, 0, 0, 0, time.UTC)

	old := insertDevice(t, db, userID, "古い通信デバイス",
		withLastCommunicated(cutoff.Add(-time.Hour)))    // 値あり: 対象 → 表示
	recent := insertDevice(t, db, userID, "最近通信デバイス",
		withLastCommunicated(cutoff.Add(time.Hour)))     // 値あり: 対象外 → 非表示
	never := insertDevice(t, db, userID, "未通信デバイス",
		withLastCommunicated(nil))                       // NULL: 対象 → 表示（忘れがち）

	html := getPageHTML(t, app, "/devices/attention")

	assertContains(t, html, "古い通信デバイス")
	assertNotContains(t, html, "最近通信デバイス")
	assertContains(t, html, "未通信デバイス") // ← これを忘れると NULL を取りこぼす

	_ = old; _ = recent; _ = never
}
```

> `withLastCommunicated(nil)` のような fixture オプションは `pgtype.Timestamptz{Valid: false}` を渡す。NULL は Go では `pgtype.*{Valid:false}`（`pgconv` の round-trip テスト参照）で表現する点が、Laravel の `null` 直接代入との違い。**適用対象:** `location`・`last_communicated_at`・`expires_at` など nullable 列を WHERE/フィルタ条件に使う全クエリ。

---

## 31. NULL 末尾ソートのテストパターン

> 📅 2026-04-03（原本）— Laravel では `orderByRaw('COALESCE(\`order\`, ?)')` で NULL を末尾に寄せた。本プロジェクトに専用の `order` 列は無いが、教訓「**NULL を含む列でソートすると並び順が DB 既定（PostgreSQL は ASC で NULLS LAST、DESC で NULLS FIRST）に左右される**」はそのまま通用する。`last_communicated_at` 等で「未設定を末尾に」という要件が出たときに適用する。

### 31.1 実装パターン（ADAPT）

PostgreSQL は `ORDER BY ... NULLS LAST / NULLS FIRST` を直接書けるので、Laravel の `COALESCE(col, 999999)` ハックは不要。sqlc クエリで明示する。

```sql
-- NULL（未通信）を末尾に配置するソート（PostgreSQL ネイティブ）
-- name: ListDevicesByLastCommunicated :many
SELECT * FROM devices
 WHERE user_id = $1 AND deleted_at IS NULL
 ORDER BY last_communicated_at DESC NULLS LAST;  -- ← COALESCE 不要
```

> Laravel: `orderByRaw('COALESCE(`order`, 999999)')` → Go/PostgreSQL: `ORDER BY col DESC NULLS LAST`。COALESCE のマジックナンバー（999999）が不要になり、型不整合の事故も防げる。

### 31.2 テストパターン（PORT）

NULL を含む複数レコードを**挿入順とソート順が異なる**ように作成し、出力 HTML 内の出現位置（`strings.Index`）で順序を検証する。Laravel の `strpos` + `assertLessThan` を Go の `strings.Index` + 数値比較に置換する。

```go
func TestDevices_ソートはNULL末尾(t *testing.T) {
	// 意図的にソート順と逆の順序で作成（fixture builder, 実 DB テスト）
	t1 := time.Date(2026, 6, 3, 0, 0, 0, 0, time.UTC)
	t2 := time.Date(2026, 6, 2, 0, 0, 0, 0, time.UTC)
	t3 := time.Date(2026, 6, 1, 0, 0, 0, 0, time.UTC)

	insertDevice(t, db, userID, "デバイスB_T2", withLastCommunicated(t2))
	insertDevice(t, db, userID, "デバイスNULL_NEVER", withLastCommunicated(nil))
	insertDevice(t, db, userID, "デバイスA_T1", withLastCommunicated(t1))
	insertDevice(t, db, userID, "デバイスC_T3", withLastCommunicated(t3))

	html := getPageHTML(t, app, "/devices?sort=last_comm")

	posA := strings.Index(html, "デバイスA_T1")     // 最新 → 先頭
	posB := strings.Index(html, "デバイスB_T2")
	posC := strings.Index(html, "デバイスC_T3")
	posNull := strings.Index(html, "デバイスNULL_NEVER") // NULL → 末尾

	// 期待順: T1 → T2 → T3 → NULL（DESC NULLS LAST）
	if !(posA < posB && posB < posC && posC < posNull) {
		t.Errorf("ソート順が不正: A=%d B=%d C=%d NULL=%d", posA, posB, posC, posNull)
	}
}
```

**注意:** §31.2 の名前にサフィックス（`_T1` / `_NEVER` 等）を付けて HTML 内で**一意**にする（`strings.Index` は最初の出現位置を返すため、部分一致が衝突すると順序判定が狂う）。`strings.Index` は未出現で `-1` を返すので、`-1 < 何か` で偽の順序判定が成立しないよう、全要素が出現していること（全て `>= 0`）も併せて確認すると堅い。

---

## 32. 親子検索（子の条件で親を絞り込み + 該当する子のみ表示）のテストパターン

> 📅 2026-04-03（原本）— Laravel では `whereHas`（親の絞り込み）と `with` クロージャ（子のフィルタ）を両方使わないと、絞り込まれた親にぶら下がる子が全件表示される罠を踏んだ。本プロジェクトの相当は「デバイス（親）をその計測値/アラート履歴（子）の条件で絞り、かつ該当する子だけを表示する」ようなクエリ。`whereHas` + `with` は ORM 機能なので、sqlc では **JOIN で親を絞る + 子取得クエリ側にも同じ条件を入れる**2クエリ（または1 JOIN クエリ）に読み替える。

### 32.1 問題（ADAPT）

「条件に合う子を持つ親だけ表示し、かつ表示する子も条件に合うものだけ」を実現するには、**親の絞り込み条件**と**子の取得条件**の**両方**に同じフィルタを掛ける必要がある。片方（親の絞り込み）だけだと、親は正しく絞られるが、ぶら下げる子は全件出てしまう。

### 32.2 実装パターン（ADAPT）

sqlc には Eager Load が無いので、(A) 親一覧を子条件付き EXISTS で絞るクエリと、(B) 各親に対し子を条件付きで取るクエリ、の2本に分ける。両方に**同じ子フィルタ**を入れるのが肝。

```sql
-- (A) 親を絞る: 条件に合うアラート履歴を持つデバイスだけ
-- name: ListDevicesHavingAlert :many
SELECT d.* FROM devices d
 WHERE d.user_id = $1 AND d.deleted_at IS NULL
   AND EXISTS (
     SELECT 1 FROM alert_histories ah
      WHERE ah.device_id = d.id
        AND ah.deleted_at IS NULL
        AND ah.triggered_at >= $2   -- ← 子フィルタ
   )
 ORDER BY d.name ASC;

-- (B) 子を取る: 同じ子フィルタを入れる（入れ忘れると全件出る）
-- name: ListAlertsForDeviceSince :many
SELECT * FROM alert_histories
 WHERE device_id = $1
   AND deleted_at IS NULL
   AND triggered_at >= $2          -- ← (A) と同じ条件を必ず再掲
 ORDER BY triggered_at DESC;
```

> Laravel: `whereHas('children', fn)` + `with(['children' => fn])` の二重指定 → Go/sqlc: 親を絞る EXISTS クエリ + 子を取るクエリの**両方に同条件**。子クエリの条件を入れ忘れるのが Laravel の `whereHas` だけ書いた場合と同じバグ。

### 32.3 テストパターン（!strings.Contains が重要）（PORT）

同じ親にぶら下がる「ヒットする子」と「ヒットしない子」を用意し、ヒットしない子が**表示されない**ことを `!strings.Contains` で検証する。これが Laravel の `assertDontSee` 相当で、子フィルタの入れ忘れを検出する決定的なテスト。

```go
func TestDeviceAlerts_該当子は表示し非該当子は隠す(t *testing.T) {
	since := time.Date(2026, 6, 1, 0, 0, 0, 0, time.UTC)
	dev := insertDevice(t, db, userID, "親デバイスA")

	// ヒットする子（since 以降）
	insertAlertHistory(t, db, dev.ID, "該当アラート", withTriggeredAt(since.Add(time.Hour)))
	// ヒットしない子（since より前・同じ親）
	insertAlertHistory(t, db, dev.ID, "古いアラート", withTriggeredAt(since.Add(-time.Hour)))

	html := getPageHTML(t, app, "/devices?has_alert_since=2026-06-01")

	assertContains(t, html, "親デバイスA")      // 親は表示
	assertContains(t, html, "該当アラート")      // マッチした子は表示
	assertNotContains(t, html, "古いアラート")   // ← 子クエリの条件入れ忘れだと失敗する
}
```

**AND 結合テスト（複数条件）:**

```go
func TestDeviceAlerts_複数条件のAND結合(t *testing.T) {
	// デバイス「ハウスA」に temperature アラートあり
	// デバイス「ハウスB」に humidity アラートあり
	html := getPageHTML(t, app, "/devices?name=ハウスA&metric=temperature")

	assertContains(t, html, "ハウスA")
	assertContains(t, html, "temperature")
	assertNotContains(t, html, "ハウスB")  // デバイス名不一致で除外
	assertNotContains(t, html, "humidity") // metric 不一致で除外
}
```

> `assertNotContains` ヘルパは `assertContains` の対。`metric` は `alert_rules` / 判定対象の許容リスト（`temperature` / `humidity`）に整合させる。**適用対象:** デバイス × 計測値/アラート履歴の親子検索、将来の選択モーダルで親子フィルタを実装する場合。

---

## 33. ファイルエクスポート（CSV / XLSX）のテストパターン

> 📅 2026-04-04（原本）— Laravel では一覧 CSV を `StreamedResponse` + `streamedContent()` で、Excel を `BinaryFileResponse` + `ZipArchive` で検証した。本プロジェクトの相当は **Gin の `c.Data` / `io.Copy` + `encoding/csv`（CSV）/ `xuri/excelize`（XLSX）**。テスト側は `httptest.ResponseRecorder` の `w.Body`（バイト列）を直接読めるので、Laravel のような stream/binary の区別なく `w.Body.Bytes()` で本文を取得できる（ここが Laravel より単純）。ファイルダウンロードは HTMX 非対応のため通常の `<a href>` / フォーム送信で行う（HTMX実装ガイドの該当表）。

### 33.1 基本パターン（CSV）（ADAPT）

ステータス・Content-Type・Content-Disposition・BOM・ヘッダー行・データ行を検証する。`httptest.NewRecorder` は stream でも `w.Body` に全量バッファされるので `streamedContent()` 相当は不要。

```go
func TestExportCSV_センサーデータをCSVで返す(t *testing.T) {
	// 実 DB テスト（goose migrate 済みテスト DB）。fixture でデータを用意。
	dev := insertDevice(t, db, userID, "CSV出力テストデバイス")
	insertSensorReading(t, db, dev.ID, 28.50, 65.30,
		time.Date(2026, 6, 1, 9, 0, 0, 0, time.UTC))

	w := getWithCookies(app, "/devices/"+strconv.FormatInt(dev.ID, 10)+"/readings.csv", loginCookies)

	// 1. ステータス + Content-Type
	if w.Code != http.StatusOK {
		t.Fatalf("status = %d, want 200", w.Code)
	}
	if ct := w.Header().Get("Content-Type"); ct != "text/csv; charset=utf-8" {
		t.Errorf("Content-Type = %q", ct)
	}

	// 2. Content-Disposition（ファイル名にタイムスタンプ等を含む場合は部分一致）
	cd := w.Header().Get("Content-Disposition")
	if !strings.Contains(cd, "attachment") || !strings.Contains(cd, ".csv") {
		t.Errorf("Content-Disposition = %q, want attachment + .csv", cd)
	}

	body := w.Body.Bytes()
	// 3. BOM（Excel での文字化け回避。UTF-8 BOM を付ける運用なら）
	if !bytes.HasPrefix(body, []byte{0xEF, 0xBB, 0xBF}) {
		t.Error("UTF-8 BOM が先頭に無い")
	}
	// 4. 日本語ヘッダー行 + データ行
	text := string(body)
	assertContains(t, text, "計測日時")            // ヘッダー行
	assertContains(t, text, "28.50")             // データ行（温度）
	assertContains(t, text, "65.30")             // データ行（湿度）
}
```

> Laravel: `$response->assertHeader('Content-Type', 'text/csv; charset=UTF-8')` + `$response->streamedContent()` → Go: `w.Header().Get("Content-Type")` + `w.Body.Bytes()`。CSV 本文は `encoding/csv` で生成するが、テスト側は文字列として `strings.Contains` で十分（行の厳密検証が要るなら `csv.NewReader(bytes.NewReader(body))` でパースして比較）。

### 33.2 検索条件適用の検証（PORT）

エクスポートは現在の検索条件を引き継ぐ。マッチするデータとしないデータを作り、GET パラメータが反映されることを検証する。

```go
func TestExportCSV_検索条件を反映する(t *testing.T) {
	dev := insertDevice(t, db, userID, "デバイスX")
	insertSensorReading(t, db, dev.ID, 30.00, 50.00,
		time.Date(2026, 6, 1, 0, 0, 0, 0, time.UTC)) // 期間内 → 含む
	insertSensorReading(t, db, dev.ID, 99.00, 10.00,
		time.Date(2026, 5, 1, 0, 0, 0, 0, time.UTC)) // 期間外 → 除外

	w := getWithCookies(app,
		"/devices/"+strconv.FormatInt(dev.ID, 10)+"/readings.csv?from=2026-05-15&to=2026-06-30",
		loginCookies)
	text := w.Body.String()

	assertContains(t, text, "30.00")    // 期間内データ
	assertNotContains(t, text, "99.00") // 期間外データは出力されない
}
```

### 33.3 注意: ページネーション対象外（PORT）

エクスポートは検索条件マッチ**全件**で、画面のページングの影響を受けない。`page` / `per_page` を付けても件数が変わらないことを検証する（`db/queries` のエクスポート用クエリは LIMIT/OFFSET を持たない別クエリにする）。

```go
func TestExportCSV_ページングを無視する(t *testing.T) {
	dev := insertDevice(t, db, userID, "大量データデバイス")
	for i := 0; i < 50; i++ {
		insertSensorReading(t, db, dev.ID, 20.00, 50.00,
			time.Date(2026, 6, 1, 0, i, 0, 0, time.UTC))
	}

	w := getWithCookies(app,
		"/devices/"+strconv.FormatInt(dev.ID, 10)+"/readings.csv?page=99&per_page=10",
		loginCookies)

	// ヘッダー 1 行 + データ 50 行 = 51 行（CSV 改行数で確認）
	rows, err := csv.NewReader(bytes.NewReader(stripBOM(w.Body.Bytes()))).ReadAll()
	if err != nil {
		t.Fatalf("CSV パース失敗: %v", err)
	}
	if len(rows) != 51 {
		t.Errorf("行数 = %d, want 51 (header 1 + data 50)", len(rows))
	}
}
```

> 一覧画面のページング用クエリ（`ListSensorReadingsPaginated`、LIMIT/OFFSET 付き）と、エクスポート用の全件クエリは**別クエリ**にする（`db/queries/sensor_readings.sql` の paginated 版を流用しない）。`stripBOM` は先頭 3 バイトを落とす小ヘルパ。

### 33.4 XLSX 出力のテスト（ADAPT）

> Laravel では `SimpleXlsxExporter::download()` が `BinaryFileResponse` を返すため `streamedContent()` が使えず `getFile()` 経由でバイナリを取得した。Go で `xuri/excelize` を使う場合、生成した `*excelize.File` を `f.Write(w)`（または一時ファイル → `c.File`）でレスポンスに書き出す。**httptest では CSV と同じく `w.Body.Bytes()` で XLSX バイト列を取得できる**ので、Laravel のような Stream/Binary の場合分けは不要。

#### 33.4.1 基本パターン（ADAPT）

```go
func TestExportXLSX_xlsxを返す(t *testing.T) {
	dev := insertDevice(t, db, userID, "Excel出力デバイス")
	insertSensorReading(t, db, dev.ID, 28.50, 65.30,
		time.Date(2026, 6, 1, 9, 0, 0, 0, time.UTC))

	w := getWithCookies(app,
		"/devices/"+strconv.FormatInt(dev.ID, 10)+"/readings.xlsx", loginCookies)

	if w.Code != http.StatusOK {
		t.Fatalf("status = %d, want 200", w.Code)
	}
	const xlsxCT = "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
	if ct := w.Header().Get("Content-Type"); ct != xlsxCT {
		t.Errorf("Content-Type = %q, want %q", ct, xlsxCT)
	}
	cd := w.Header().Get("Content-Disposition")
	if !strings.Contains(cd, "attachment") || !strings.Contains(cd, ".xlsx") {
		t.Errorf("Content-Disposition = %q", cd)
	}
	if w.Body.Len() == 0 {
		t.Error("XLSX 本文が空")
	}
}
```

#### 33.4.2 シート内容の検証（excelize で再オープン）（ADAPT）

> Laravel は `ZipArchive` で `xl/worksheets/sheet1.xml` を直接読んだ。Go では `excelize.OpenReader(bytes.NewReader(w.Body.Bytes()))` でレスポンス本文を**再パース**してセル値を読めるので、XML 文字列の生検索より堅牢。

```go
func TestExportXLSX_想定の行を含む(t *testing.T) {
	dev := insertDevice(t, db, userID, "デバイスEXP")
	insertSensorReading(t, db, dev.ID, 28.50, 65.30,
		time.Date(2026, 6, 1, 9, 0, 0, 0, time.UTC))

	w := getWithCookies(app,
		"/devices/"+strconv.FormatInt(dev.ID, 10)+"/readings.xlsx", loginCookies)

	f, err := excelize.OpenReader(bytes.NewReader(w.Body.Bytes()))
	if err != nil {
		t.Fatalf("XLSX 再オープン失敗: %v", err)
	}
	defer f.Close()

	sheet := f.GetSheetName(0)
	rows, err := f.GetRows(sheet)
	if err != nil {
		t.Fatalf("GetRows: %v", err)
	}
	// ヘッダー行 + データ行を結合して部分一致で確認（列位置に依存しない簡易検証）
	flat := flattenRows(rows)
	assertContains(t, flat, "計測日時")     // ヘッダー
	assertContains(t, flat, "デバイスEXP") // データ
	assertContains(t, flat, "28.50")
}
```

> Laravel の「`inlineStr` 形式 + `htmlspecialchars` でエスケープ済み文字列に注意」という観点は、excelize で**再パースしてセル値を取得する方式なら不要**（excelize が XML エスケープを復元するため）。XML 文字列を生検索する方式を取った場合のみ、`&amp;` / `&lt;` のエスケープに注意する。

#### 33.4.3 一時ファイルのクリーンアップ（ADAPT）

> Laravel の `deleteFileAfterSend(true)` は「HTTP 実送信完了後に削除、テストでは残る」挙動だった。Go で excelize を `f.Write(w)` で直接ストリームすれば**一時ファイル自体が生じない**（最も簡潔）。一時ファイル経由（`f.SaveAs(tmp)` → `c.File(tmp)`）にする場合は、Handler 側で `os.CreateTemp` + `defer os.Remove(tmp)` で確実に消す。

```go
// 推奨: 一時ファイルを作らず excelize から直接書き出す（クリーンアップ不要）
func (h *ExportHandler) ReadingsXLSX(c *gin.Context) {
	f := excelize.NewFile()
	defer f.Close()
	// ... f にデータを書く ...
	c.Header("Content-Type", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
	c.Header("Content-Disposition", `attachment; filename="readings.xlsx"`)
	if _, err := f.WriteTo(c.Writer); err != nil {
		c.AbortWithStatus(http.StatusInternalServerError)
	}
}
```

#### 33.4.4 ページネーション対象外（XLSX）（PORT）

CSV（§33.3）と同じく全件。excelize で再オープンして行数を検証する。

```go
func TestExportXLSX_ページングを無視する(t *testing.T) {
	dev := insertDevice(t, db, userID, "大量XLSXデバイス")
	for i := 0; i < 50; i++ {
		insertSensorReading(t, db, dev.ID, 20.00, 50.00,
			time.Date(2026, 6, 1, 0, i, 0, 0, time.UTC))
	}

	w := getWithCookies(app,
		"/devices/"+strconv.FormatInt(dev.ID, 10)+"/readings.xlsx?page=99&per_page=10",
		loginCookies)

	f, err := excelize.OpenReader(bytes.NewReader(w.Body.Bytes()))
	if err != nil {
		t.Fatalf("XLSX 再オープン失敗: %v", err)
	}
	defer f.Close()
	rows, _ := f.GetRows(f.GetSheetName(0))
	if len(rows) != 51 { // ヘッダー 1 + データ 50
		t.Errorf("行数 = %d, want 51", len(rows))
	}
}
```

### 33.5 数値・日時の出力フォーマット検証（Go 固有の補足）

CSV/XLSX に numeric(5,2) 値（temperature/humidity）を出すときは、DB の `pgtype.Numeric` を `pgconv.NumericToFloat` で float に戻し、`strconv.FormatFloat(v, 'f', 2, 64)` 等で**桁を固定**してから書く。`recorded_at`（timestamptz）は `pgconv.TimestamptzToTime` で `time.Time` に戻し、JST 等のロケールに変換して整形する。テストでは出力文字列（`"28.50"`、`"2026-06-01 09:00"`）を直接アサートすることで、丸め・タイムゾーン変換の事故を捕まえられる（`pgconv_test.go` の round-trip テストと整合）。

### 33.6 エクスポート値の XSS / インジェクション安全性（Go 固有の補足）

CSV にユーザー入力（デバイス名・location 等）を出す場合、`=`・`+`・`-`・`@` で始まるセルは **CSV インジェクション（数式埋め込み）**のリスクがある。`encoding/csv` はクォートはするがこの無害化はしないので、必要なら先頭にシングルクォートを付ける等の対策を入れ、テストで検証する。HTML 出力ではないため templ の自動エスケープは効かない点に注意。

```go
func TestExportCSV_数式インジェクションを無害化(t *testing.T) {
	dev := insertDevice(t, db, userID, "=cmd|'/c calc'!A1") // 悪意あるデバイス名
	insertSensorReading(t, db, dev.ID, 20.00, 50.00, time.Now().UTC())

	w := getWithCookies(app,
		"/devices/"+strconv.FormatInt(dev.ID, 10)+"/readings.csv", loginCookies)
	text := w.Body.String()

	// セル先頭の "=" が無害化（先頭にプレフィックス付与等）されていること
	assertNotContains(t, text, "\n=cmd")
	assertNotContains(t, text, ",=cmd")
}
```

> Laravel の `Js::from()` による Unicode エスケープ（HTML/JS 埋め込みの XSS 対策）は、エクスポートでは文脈が違う（HTML ではなく表計算ソフトが開くファイル）。HTML/JS 埋め込みの XSS 検証は §10-C / §10-E（`templ.JSONString` / `<script type="application/json">`）側で扱い、エクスポートでは CSV インジェクションを別途検証する、と整理する。**適用場面:** センサーデータ履歴の CSV/XLSX 出力、将来のエクスポート系機能全般。

---

## 34. HX-Trigger レスポンスヘッダーの複数イベント検証

> 📅 2026-04-04 — 削除系 HTMX レスポンスで確立した知見を Gin 版へ移植。§7.3 の発展パターン。

### 34.1 背景

§7.3 では「`HX-Trigger` ヘッダが存在し、特定文字列を含むこと」を `strings.Contains` で検証するパターンを記載した。だが `HX-Trigger` が**複数イベントを JSON オブジェクトで返す**ケースでは、文字列部分一致ではキーの綴り順や空白に依存して脆くなる。`json.Unmarshal` して構造として検証する方が堅牢。

Gin では `c.Header("HX-Trigger", ...)` でセットし、テストでは `w.Header().Get("HX-Trigger")` で取り出す。Laravel の `$response->headers->get('HX-Trigger')` がそのまま `w.Header().Get(...)` に対応する。

### 34.2 文字列検証（§7.3 の既存パターン）

単一イベントなら部分一致で十分。

```go
func TestDeleteDevice_単一イベントのHXTrigger(t *testing.T) {
	w := doDelete(t, ...) // 204 を返すハンドラ
	trig := w.Header().Get("HX-Trigger")
	if trig == "" {
		t.Fatal("HX-Trigger ヘッダが無い")
	}
	if !strings.Contains(trig, "modal-close") {
		t.Errorf("HX-Trigger に modal-close が無い: %q", trig)
	}
}
```

### 34.3 JSON 構造検証（複数イベント時の推奨パターン）

ハンドラが `c.Header("HX-Trigger", \`{"modal-close":"","device-list-refresh":""}\`)` のように JSON を返す場合、`map[string]any` へ Unmarshal してキー存在を検証する。

```go
func TestDeleteDevice_複数イベントのHXTrigger(t *testing.T) {
	w := doDelete(t, ...)

	if got, want := w.Code, http.StatusNoContent; got != want {
		t.Fatalf("status: got %d, want %d", got, want)
	}

	raw := w.Header().Get("HX-Trigger")
	if raw == "" {
		t.Fatal("HX-Trigger ヘッダが無い")
	}

	var trigger map[string]any
	if err := json.Unmarshal([]byte(raw), &trigger); err != nil {
		t.Fatalf("HX-Trigger が JSON ではない: %q (%v)", raw, err)
	}
	for _, key := range []string{"modal-close", "device-list-refresh"} {
		if _, ok := trigger[key]; !ok {
			t.Errorf("HX-Trigger に %q が含まれていない: %v", key, trigger)
		}
	}
}
```

> Laravel の `json_decode($header, true)` + `assertArrayHasKey` は、Go では `json.Unmarshal` して `map` のキー存在チェック（`_, ok := m[key]`）に 1:1 で読み替える。

**使い分け:**
- 単一イベント（生文字列）: §7.3 の `strings.Contains` で十分。
- 複数イベント（JSON オブジェクト）: `json.Unmarshal` → キー存在チェックを推奨。綴り順・空白・値の有無に影響されない。

**ペイロード付きイベント**（`{"alert-fired":{"deviceId":10}}`）まで検証したい場合は、`map[string]json.RawMessage` で受けて該当キーだけ二段目の構造体へ Unmarshal する。

**適用対象:** デバイス/アラートルール削除レスポンス、保存後にトースト + 一覧リフレッシュを同時発火させる全ハンドラ。

---

## 35. 多段リレーションのテストデータ構築ヘルパパターン

> 📅 2026-04-04 — 複数テーブルにまたがる集計テストで確立。「ヘルパに構築を集約する」教訓を Go 版へ移植。

### 35.1 背景

本プロジェクトの中核チェーンは `users → devices → alert_rules → alert_histories`（および `devices → sensor_readings`）。たとえば「あるユーザーのデバイスで発火したアラート履歴一覧」を検証するには、この 4 テーブルを横断するデータが要る。各テストで毎回手作業に組み立てると冗長で可読性が落ちる。

> Laravel では `Branch::factory()->create([...])` をチェーンさせて Factory で構築した。Go には Factory/Eloquent が無いので、**(a) DB 非依存テストなら Querier モックの戻り値を組み立てるヘルパ**、**(b) 実 DB テストなら fixture builder（INSERT ヘルパ）** に読み替える。原則は (a)。実 DB が必須の検証だけ (b)。

### 35.2 (a) Querier モック向け: 行構造体ビルダ（原則・DB 不要）

`overrides` 相当は「ベース構造体を関数オプションで上書き」で表現する。sqlc 生成の行構造体をそのまま組み立て、モックの戻り値に詰める。

```go
// alert_rules_test.go 内のテストヘルパ。
// 戻り値の型は sqlc 生成構造体そのものなので IDE 補完が効く (PHPDoc @return 相当は不要)。

func newDevice(opts ...func(*repository.Device)) repository.Device {
	d := repository.Device{ID: 10, UserID: 7, Name: "ハウスA", IsActive: true}
	for _, o := range opts {
		o(&d)
	}
	return d
}

func newAlertRule(opts ...func(*repository.AlertRule)) repository.AlertRule {
	r := repository.AlertRule{
		ID:        100,
		DeviceID:  10,
		Metric:    "temperature",       // CHECK: temperature | humidity
		Operator:  ">",                  // CHECK: > | < | >= | <=
		Threshold: pgconv.Numeric(35.00), // numeric(5,2)
		IsEnabled: true,
	}
	for _, o := range opts {
		o(&r)
	}
	return r
}
```

### 35.3 テストでの使用例

`overrides` は可変長の関数オプションで渡す。テストごとに異なる値（閾値・指標・有効フラグ）を局所的に差し替えられる。

```go
func TestAlertRuleList_有効ルールのみ表示(t *testing.T) {
	rules := []repository.AlertRule{
		newAlertRule(func(r *repository.AlertRule) { r.ID = 100; r.IsEnabled = true }),
		newAlertRule(func(r *repository.AlertRule) {
			r.ID = 101
			r.Metric = "humidity"
			r.Operator = "<"
			r.Threshold = pgconv.Numeric(30.00)
		}),
	}
	q := &fakeAlertRepo{
		device: newDevice(), // user_id=7 所有
		rules:  rules,
	}
	// ... ハンドラを叩いて HTML / JSON を検証
}
```

### 35.4 (b) 実 DB 向け: fixture builder（実 DB が必須なときだけ）

CHECK 制約・部分 UNIQUE 索引・`deleted_at IS NULL` 条件付き索引など、**DB 側の挙動そのものを検証したい場合**は goose migrate 済みのテスト用 DB へ実際に INSQL する。チェーンを 1 関数に集約する。

```go
// 実 DB テスト専用ヘルパ。q は本物の *repository.Queries (pgx 接続)。
func seedAlertChain(t *testing.T, ctx context.Context, q *repository.Queries, ownerID int64) repository.AlertHistory {
	t.Helper()

	dev, err := q.CreateDevice(ctx, repository.CreateDeviceParams{
		UserID:     ownerID,
		Name:       "ハウスA",
		MacAddress: uniqueMAC(t), // §38 参照: アクティブ部分 UNIQUE を避けるため一意化
	})
	if err != nil {
		t.Fatalf("CreateDevice: %v", err)
	}

	rule, err := q.CreateAlertRule(ctx, repository.CreateAlertRuleParams{
		DeviceID:  dev.ID,
		Metric:    "temperature",
		Operator:  ">",
		Threshold: pgconv.Numeric(35.00),
	})
	if err != nil {
		t.Fatalf("CreateAlertRule: %v", err)
	}

	hist, err := q.CreateAlertHistory(ctx, repository.CreateAlertHistoryParams{
		AlertRuleID: rule.ID,
		Metric:      rule.Metric,                 // 発火時点を非正規化保持
		Operator:    rule.Operator,
		Threshold:   rule.Threshold,
		ActualValue: pgconv.Numeric(36.50),
		TriggeredAt: pgconv.Timestamptz(time.Now()),
	})
	if err != nil {
		t.Fatalf("CreateAlertHistory: %v", err)
	}
	return hist
}
```

### 35.5 設計ポイント

- **関数オプション (`opts ...func(*T)`)**: Laravel の `array $overrides` 相当。テストごとに金額・閾値・日付を柔軟に差し替えられる。
- **ベース値は CHECK 制約を満たす既定にする**: `Metric`/`Operator` は許容リスト内、`Threshold`/`Temperature` は `numeric(5,2)` の範囲・桁内（`devices.mac_address` は正規表現 `^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$`）を最初から満たしておく。テストが値の意図と無関係な制約違反で落ちない。
- **マスター相当の重複回避**: Laravel の `firstOrCreate` 相当が要るのは UNIQUE 制約のあるテーブル（`users.email`、`devices.mac_address` のアクティブ部分 UNIQUE）。実 DB テストでは一意値生成ヘルパ（§38）で衝突を防ぐ。
- **戻り値は sqlc 生成構造体**: 専用の戻り型を発明せず生成物をそのまま返すことで、後段アサートで実カラムにそのままアクセスでき、スキーマ変更にも追従する。

**適用対象:** アラート履歴一覧、デバイス別計測グラフなど、3 段以上のリレーションを持つ全機能のテスト。

---

## 36. 条件付きソート無効化テスト — `sort-icon` と hidden input の罠

> 📅 2026-04-07 — 一覧画面のタブ別ソート切替で発覚した教訓を templ 版へ移植。

### 36.1 問題

一覧画面で「タブ A ではソート有効、タブ B ではソート無効」のように条件付きでソート UI を出し分ける場合、`!strings.Contains(html, "sort=recorded_at")` で「ソートリンクが無いこと」を検証したくなる。だが**検索フォームの hidden input にも現在のソート状態が `sort` パラメータとして埋め込まれる**ことが多く（検索フォームがクエリパラメータを保持して GET 送信するため）、ページ全体に対する `!strings.Contains(html, "sort=")` は hidden input にマッチして誤検知する。

> Laravel の `assertDontSee('sort=...', false)` がページ全体走査だったのと同じ問題。Go の `strings.Contains(body, "sort=")` も body 全体が対象なので同様に当たってしまう。

### 36.2 テストパターン

ソート UI の有無は **ソートアイコン用クラス（例 `sort-icon`）の有無**で判別する。このクラスはソート可能テーブルコンポーネントでのみ出力されるため、hidden input とは衝突しない。

```go
func TestReadingsList_ソートUIのタブ別出し分け(t *testing.T) {
	// 計測順タブ: ソートアイコンを表示 (ソート可能テーブル)
	htmlSortable := render(t, ReadingsListPage(ReadingsView{Tab: "by-time"}))
	assertContains(t, htmlSortable, "sort-icon")

	// グルーピングタブ: ソートアイコン非表示 (別コンポーネント, ソート不可)
	htmlGrouped := render(t, ReadingsListPage(ReadingsView{Tab: "grouped"}))
	assertNotContains(t, htmlGrouped, "sort-icon")
}

func assertNotContains(t *testing.T, html, substr string) {
	t.Helper()
	if strings.Contains(html, substr) {
		t.Errorf("出力に %q が含まれてはならない:\n%s", substr, html)
	}
}
```

### 36.3 なぜ `sort=` で検証できないか

```html
<!-- 検索フォーム内の hidden input (タブに関わらず常に存在) -->
<input type="hidden" name="sort" value="recorded_at">

<!-- ソート可能テーブルのヘッダ内リンク (計測順タブのみ) -->
<a href="/devices/10/readings?sort=recorded_at&amp;order=desc">計測日時<span class="sort-icon">⇕</span></a>
```

`!strings.Contains(html, "sort=")` は body 全体を対象にするため、hidden input の `name="sort"`（および `value` 内の文字）にマッチしてしまう。`sort-icon` クラスはソート可能テーブルコンポーネントでのみ Render されるので、ソート UI の有無を正確に判別できる。

> §31（独自クラス禁止）の観点でも、判別に使うクラス名はモック `mocks/html/style.css` に実在する正本クラス（`sort-icon` 等）であること。templ 側で独自命名したクラスを判別基準に使うと、CSS 単一ソース運用（§40-B）から外れる。

**適用場面:** タブ切替でテーブルコンポーネント自体が切り替わる画面（ソート可能一覧 ⇄ グルーピング表示など）。

---

## 37. テスト用 DB と開発/本番 DB の取り違えによるデータ破壊問題

> 📅 2026-04-07 — テスト実行のたびに開発 DB のデータが消える事故として発覚した教訓を Go/goose 版へ移植。

### 37.1 問題（機構読替）

Laravel では `docker-compose.yml` の `DB_DATABASE=laravel_local`（開発 DB）が `phpunit.xml` の `DB_DATABASE=laravel_test`（テスト DB）を環境変数優先順位で上書きし、`RefreshDatabase` が**開発 DB に対して `migrate:fresh`（全テーブル DROP→再作成）を実行**してシードデータを消した。

Go/本プロジェクトには `RefreshDatabase` も `phpunit.xml` も無いが、**「テスト DB と開発/本番 DB を接続文字列で分離しないと、テストの TRUNCATE/goose reset が開発 DB を破壊する」という本質は完全に通用する。**実 DB テスト（§35-b）を導入した瞬間に同じ事故が起き得る。

> Laravel: env 優先順位で `DB_DATABASE` が上書きされる罠。→ Go: `os.Getenv("DATABASE_URL")` が開発用接続文字列のまま実 DB テストへ流れ込む罠。仕組みは違うが「テストが意図せず開発 DB を掴む」結果は同じ。

### 37.2 原因（Go 版）

1. 実 DB テストの接続元は `os.Getenv("DATABASE_URL")` や `config.Config.DatabaseURL` 一本。
2. ローカルでは shell / direnv / docker compose の env でこれが**開発 DB**を指している。
3. テストヘルパが TRUNCATE や `goose reset` を無防備に呼ぶと、その開発 DB を破壊する。

### 37.3 対策

**(1) テスト専用の接続文字列を別変数で持ち、未設定ならスキップする**（開発 DB を絶対に掴ませない）。

```go
// 実 DB テスト共通ヘルパ (例: internal/repository/testdb_test.go)
func openTestDB(t *testing.T) *pgxpool.Pool {
	t.Helper()
	dsn := os.Getenv("TEST_DATABASE_URL") // ★ 本番/開発の DATABASE_URL とは別変数
	if dsn == "" {
		t.Skip("TEST_DATABASE_URL 未設定のため実 DB テストをスキップ")
	}
	// 多重防御: DB 名にテスト用の印が無ければ破壊操作を拒否する
	if !strings.Contains(dsn, "go_iot_test") {
		t.Fatalf("TEST_DATABASE_URL が *_test を指していない: 破壊操作を中止 (%s)", maskDSN(dsn))
	}
	pool, err := pgxpool.New(context.Background(), dsn)
	if err != nil {
		t.Fatalf("テスト DB 接続失敗: %v", err)
	}
	t.Cleanup(pool.Close)
	return pool
}
```

**(2) クリーンアップは DROP/fresh ではなく TRUNCATE か tx ロールバックにする**（スキーマ再生成は goose に任せ、テストはデータだけ消す）。

```go
func truncateAll(t *testing.T, ctx context.Context, pool *pgxpool.Pool) {
	t.Helper()
	// goose migrate 済み前提。スキーマは DROP せずデータのみ削除する。
	_, err := pool.Exec(ctx,
		`TRUNCATE alert_histories, alert_rules, sensor_readings, device_tokens, devices, users RESTART IDENTITY CASCADE`)
	if err != nil {
		t.Fatalf("TRUNCATE 失敗: %v", err)
	}
}
```

tx ロールバック方式（テストごとに `BeginTx`→末尾で `Rollback`）なら他テストへ漏れず物理削除も不要。ただし sqlc の `*Queries` を tx 上で動かす（`q.WithTx(tx)`）必要がある。

### 37.4 検証方法

実 DB テストを流した後、開発 DB のデータが残っていることを確認する。

```bash
# テスト DB だけを対象に実行 (開発 DB の DATABASE_URL は渡さない)
TEST_DATABASE_URL='postgres://app:app@localhost:5432/go_iot_test?sslmode=disable' \
  go test ./internal/repository/ -run TestRealDB -v

# 開発 DB のシードが無事か確認 (psql で件数を見る)
psql "$DATABASE_URL" -c "SELECT count(*) FROM users;"
# → seed 投入済みの件数が返れば OK。0 なら破壊されている。
```

### 37.5 テスト DB のマイグレーション

テスト DB のスキーマが古い場合は goose で最新へ上げてから流す（Makefile のタスクに DSN を渡す）。

```bash
# goose をテスト DB に対して適用
GOOSE_DBSTRING="$TEST_DATABASE_URL" make db-migrate
# または直接
goose -dir db/migrations postgres "$TEST_DATABASE_URL" up
```

**適用場面:** §35-b の fixture builder を使う全実 DB テスト。Docker compose / direnv で env を共有しつつ別 DB をテストに使う構成全般。

---

## 38. `devices.mac_address` のアクティブ部分 UNIQUE 制約衝突問題

> 📅 2026-04-08 — マスター系 Factory の UNIQUE 衝突として発覚した教訓を、本プロジェクトの実スキーマへ整合させて移植。

### 38.1 問題

本プロジェクトで UNIQUE を持つのは主に次の 2 つ（`docs/database_snapshot/table_definitions.md` 準拠）:

- `users_email_unique`: `email` の通常 UNIQUE。
- `devices_mac_address_unique_active`: `CREATE UNIQUE INDEX ... ON devices (mac_address) WHERE (deleted_at IS NULL)` — **論理削除されていない行に限った部分 UNIQUE**。
- `device_tokens_token_hash_unique`: `token_hash` の UNIQUE。

実 DB テスト（§35-b）で `seedAlertChain` 等が固定 MAC で `CreateDevice` を繰り返すと、2 件目で部分 UNIQUE 違反になる。

> Laravel: `branches.code` の通常 UNIQUE に Faker のランダム値が衝突した。→ Go: `devices.mac_address` の**アクティブ部分 UNIQUE**に固定値が衝突する。さらに「論理削除した行の MAC は再利用可能（`deleted_at IS NULL` 条件のため）」という Postgres 部分 UNIQUE 固有の挙動も検証観点になる。

```
ERROR: duplicate key value violates unique constraint "devices_mac_address_unique_active" (SQLSTATE 23505)
```

### 38.2 対策

テストデータ生成側で `mac_address` を毎回一意にする。`devices_mac_address_format` CHECK（`^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$`）も同時に満たす形式で生成すること。

```go
// ❌ NG: 固定 MAC を複数回 INSERT するとアクティブ部分 UNIQUE 違反
d1, _ := q.CreateDevice(ctx, repository.CreateDeviceParams{UserID: 7, Name: "A", MacAddress: "AA:BB:CC:DD:EE:FF"})
d2, _ := q.CreateDevice(ctx, repository.CreateDeviceParams{UserID: 7, Name: "B", MacAddress: "AA:BB:CC:DD:EE:FF"}) // 23505

// ✅ OK: 連番から CHECK を満たす一意 MAC を生成
var macSeq atomic.Uint32

func uniqueMAC(t *testing.T) string {
	t.Helper()
	n := macSeq.Add(1) // テスト並行実行でも衝突しない
	// 02:00:00:XX:XX:XX (ローカル管理アドレス, 6 オクテット, CHECK 正規表現を満たす)
	return fmt.Sprintf("02:00:00:%02X:%02X:%02X", (n>>16)&0xFF, (n>>8)&0xFF, n&0xFF)
}
```

### 38.3 Querier モックなら制約は飛ばない（原則の DB 非依存テスト）

DB 非依存テスト（§35-a）では UNIQUE 制約はそもそも評価されないため、戻り値の `MacAddress` は重複しても問題ない。**衝突を気にするのは実 DB テストのときだけ**。原則テストを Querier モックに寄せておけば、この罠の影響範囲は最小化される。

### 38.4 部分 UNIQUE 固有の検証も加える

「論理削除済みの MAC は再利用できる」ことは仕様なので、実 DB テストとして固定したい。

```go
func TestDevices_論理削除後は同一MACを再登録できる(t *testing.T) {
	// (実 DB テスト) 1 台作成 → SoftDelete → 同じ MAC で再作成が成功すること
	mac := uniqueMAC(t)
	d, _ := q.CreateDevice(ctx, repository.CreateDeviceParams{UserID: 7, Name: "旧", MacAddress: mac})
	if err := q.SoftDeleteDevice(ctx, d.ID); err != nil {
		t.Fatalf("SoftDeleteDevice: %v", err)
	}
	if _, err := q.CreateDevice(ctx, repository.CreateDeviceParams{UserID: 7, Name: "新", MacAddress: mac}); err != nil {
		t.Errorf("論理削除後の同一 MAC 再登録が失敗: %v (部分 UNIQUE が WHERE deleted_at IS NULL で効いていない可能性)", err)
	}
}
```

**適用場面:** UNIQUE / 部分 UNIQUE を持つ全テーブル（`users.email`、`devices.mac_address`、`device_tokens.token_hash`）の実 DB テストデータ生成。

---

## 39. 論理削除（deleted_at）の検証 — 「行が消える」のではなく「`deleted_at` が入る」

> 📅 2026-04-09 — 中間テーブルの論理削除検証で発覚した教訓を、本プロジェクトの論理削除方式（`deleted_at`）へ移植。

### 39.1 問題（機構読替）

本プロジェクトは外部キーを張らず、削除は `deleted_at = NOW()` を立てる**論理削除**（`db/queries/devices.sql` の `SoftDeleteDevice`、`alert_rules.sql` の `SoftDeleteAlertRule` 等）。よって「削除後に行が存在しない」ことを検証しようとすると、行は `deleted_at` 付きで物理的には残っているため失敗する。

> Laravel: `SoftDeletes` のため `assertDatabaseMissing` ではなく `assertSoftDeleted` を使う、という教訓。→ Go: 専用アサートは無いので、**「該当 ID の `deleted_at` が NOT NULL になっていること」を直接 SELECT して確認する**（実 DB テスト）。原則テストでは「論理削除クエリの WHERE が `deleted_at IS NULL` を含み、二重削除しても `RowsAffected==0` になる」ことを Querier モック越しに固定する。

### 39.2 実 DB での論理削除アサート

物理存在ではなく `deleted_at` の状態を見る。enum 相当（`metric`/`operator`）は **DB では文字列**なので、比較値も文字列（`"temperature"`, `">"`）で渡す（§21 と同じ「Go の domain 型値の比較」と「DB の文字列の比較」を区別する原則）。

```go
func TestSoftDeleteAlertRule_行は残りdeleted_atが入る(t *testing.T) {
	// (実 DB テスト)
	rule, _ := q.CreateAlertRule(ctx, repository.CreateAlertRuleParams{
		DeviceID: dev.ID, Metric: "temperature", Operator: ">", Threshold: pgconv.Numeric(35.00),
	})

	if err := q.SoftDeleteAlertRule(ctx, rule.ID); err != nil {
		t.Fatalf("SoftDeleteAlertRule: %v", err)
	}

	// ❌ NG: 「行が無い」ことの確認は論理削除では成り立たない
	//   GetAlertRule は WHERE deleted_at IS NULL なので ErrNoRows を返すが、
	//   それは「物理削除された」証拠にはならない。

	// ✅ OK: 行は残り deleted_at が NOT NULL であることを生 SELECT で確認
	var deletedAt *time.Time
	var metric, operator string
	err := pool.QueryRow(ctx,
		`SELECT deleted_at, metric, operator FROM alert_rules WHERE id = $1`, rule.ID).
		Scan(&deletedAt, &metric, &operator)
	if err != nil {
		t.Fatalf("論理削除後に行が物理消失している (想定外): %v", err)
	}
	if deletedAt == nil {
		t.Error("deleted_at が NULL のまま: 論理削除が効いていない")
	}
	if metric != "temperature" || operator != ">" { // DB 上は文字列で比較
		t.Errorf("非正規化カラムが変質: metric=%q operator=%q", metric, operator)
	}
}
```

### 39.3 §21（Go 型値 vs DB 文字列）との関係

| 検証したいこと | 方法 | 渡す/見る値 |
|---|---|---|
| DB レベルで論理削除されたか | 生 SELECT で `deleted_at IS NOT NULL` を確認 | `deleted_at`（`*time.Time` で Scan、nil 判定） |
| 通常取得クエリから消えたか | `GetXxx`（WHERE `deleted_at IS NULL`）が `pgx.ErrNoRows` | エラーを `errors.Is(err, pgx.ErrNoRows)` |
| Go のロジック上の比較 | domain 型値で比較 | `domain.Metric`, `domain.ComparisonOperator`（`OpGreaterThan` 等） |
| DB に書かれた文字列の比較 | SELECT した文字列で比較 | `"temperature"`, `">"`（CHECK 許容リスト内） |

### 39.4 二重削除は `RowsAffected==0`（Querier モックでも固定可）

論理削除クエリは `WHERE id = $1 AND deleted_at IS NULL` を持つ（実クエリ確認済み）。よって 2 回目の削除は対象 0 行になる。この「冪等でない＝二重削除を検出できる」性質は、楽観ロック（`CommandTag.RowsAffected()`）と同じく重要な検証観点。

```go
func TestSoftDeleteAlertRule_二重削除は影響行0(t *testing.T) {
	// (実 DB テスト) repo が RowsAffected を返す設計なら 0 を確認。
	// SoftDeleteAlertRule の生成シグネチャが error のみなら、service 層で
	// 2 回目を「既に削除済み」として 404 に写像することをハンドラテストで固定する。
}
```

> sqlc 生成の `SoftDeleteXxx` が `error` のみを返す（`RowsAffected` を返さない）場合、二重削除の検出には DELETE 系クエリを `:execrows` で定義して `int64` を返させるか、削除前に存在チェックする設計判断が要る。

### 39.5 適用場面

`deleted_at` を持つ全テーブル（`devices`, `alert_rules`, `alert_histories`, `sensor_readings`）の削除検証。とくに削除→再作成（§38 の MAC 再利用）や、削除済みが一覧から消えることの確認。

---

## 40. NOT NULL カラム漏れによる保存テストの 500 / 制約違反

> 📅 2026-04-09 — 保存ヘルパの NOT NULL カラム漏れで 500 になった教訓を、Gin/sqlc/pgx 版へ移植。

### 40.1 問題（機構読替）

Laravel では FormRequest を通過したのに、`update()` が DB の NOT NULL カラムに `null` を渡して `Integrity constraint violation (1048)` の 500 になった。Go/本プロジェクトでも同型の罠がある:

- **binding（go-playground/validator）は通過するが**、Params 構造体の必須カラムを埋め忘れて `CreateXxxParams` に**ゼロ値**を渡す。
- sqlc の Params が `pgtype.Numeric`/`pgtype.Timestamptz` で、`Valid=false`（未設定）のまま INSERT すると **`null` として送られ NOT NULL 違反**になる。これは Go コンパイラもバリデータも気づけず、pgx 実行時（実 DB テスト）に初めて `SQLSTATE 23502` / `23514`（CHECK）として出る。

> Laravel: `$request->input('contact_department')` の取りこぼし。→ Go: `CreateSensorReadingParams.RecordedAt` を `pgconv.Timestamptz(...)` で埋め忘れて `Valid:false` のまま渡す、等。バリデーション層では見えず DB 層で初めて落ちる構造は同じ。

本プロジェクトの NOT NULL かつアプリが値を供給するカラム例（`table_definitions.md` 準拠）:
- `sensor_readings`: `device_id`, `temperature`, `humidity`, `recorded_at`（すべて NOT NULL）。
- `alert_rules`: `device_id`, `metric`, `operator`, `threshold`（NOT NULL）。`is_enabled` は DEFAULT true。
- `alert_histories`: `metric`, `operator`, `threshold`, `actual_value`, `triggered_at`（NOT NULL）。
- `devices`: `user_id`, `name`, `mac_address`（NOT NULL）。`location` は NULL 可。

### 40.2 対策: 保存テストの「有効 Params」ヘルパ作成チェックリスト

`validXxxParams()` ヘルパを作るときに:

1. ハンドラ/service が `CreateXxxParams` / `UpdateXxxParams` に詰める**全フィールド**を洗い出す。
2. `docs/database_snapshot/table_definitions.md` で対象テーブルの **NOT NULL カラム**を確認する。
3. NOT NULL かつアプリが値を供給するカラムは、ヘルパで**必ず非ゼロの有効値**を埋める。`pgtype` 系は `Valid:true` になっていること（`pgconv.Numeric/Timestamptz` を通す）。
4. NULL 許容（`location` 等）でも、ハンドラがデフォルトなしでリクエスト値を使うなら明示的に入れる。
5. CHECK 制約（`metric`/`operator` の許容リスト、`temperature/humidity` の範囲、`mac_address` 形式）を満たす値にする（§35.5 参照）。

```go
// 保存系テスト用の有効 Params ヘルパ。NOT NULL & CHECK を満たす既定を返す。
func validSensorParams(opts ...func(*repository.CreateSensorReadingParams)) repository.CreateSensorReadingParams {
	p := repository.CreateSensorReadingParams{
		DeviceID:    10,                                  // NOT NULL
		Temperature: pgconv.Numeric(20.00),              // NOT NULL, CHECK -40〜125, numeric(5,2)
		Humidity:    pgconv.Numeric(50.00),              // NOT NULL, CHECK 0〜100
		RecordedAt:  pgconv.Timestamptz(time.Now()),     // NOT NULL ← 埋め忘れ厳禁 (Valid:false で 23502)
	}
	for _, o := range opts {
		o(&p)
	}
	return p
}
```

### 40.3 ハンドラ/変換側の防御策

NOT NULL カラムへ `pgtype` を詰める箇所は `pgconv` 経由に統一し、`Valid:true` を保証する。「リクエストに無ければゼロ値で通す」を避け、binding で `required` を立てて**バリデーション段で弾く**（DB の 500 ではなく 422 で返す）。

```go
// binding タグで required を立て、未充足は 422 (HTML 再描画 or JSON) で返す。
// DB の NOT NULL 違反 (500) に頼らず、バリデーション層で防ぐのが正しい層分け。
type CreateSensorRequest struct {
	DeviceID    int64     `json:"device_id"    binding:"required"`
	Temperature float64   `json:"temperature"  binding:"required,gte=-40,lte=125"`
	Humidity    float64   `json:"humidity"     binding:"required,gte=0,lte=100"`
	RecordedAt  time.Time `json:"recorded_at"  binding:"required"`
}
```

### 40.4 「実 DB を 1 本通す」テストで層の隙間を塞ぐ

binding 通過 + Querier モックだけのテストは、この罠を**構造的に見逃す**（モックは制約を評価しない）。`sensor_api_test.go` の Querier モック方式が原則だが、保存系は最低 1 本、実 DB へ実際に INSERT する統合テスト（§35-b / §37 のテスト DB）を置き、NOT NULL/CHECK の網を本物の Postgres に張らせる。

```go
func TestCreateSensorReading_実DBへ保存できる(t *testing.T) {
	// (実 DB テスト) validSensorParams() で本物の Queries.CreateSensorReading を呼ぶ。
	// RecordedAt を空にしたケースを別途流し、pgx が 23502 を返すことも固定すると
	// 「埋め忘れ即 500」の回帰を検出できる。
	pool := openTestDB(t) // §37: TEST_DATABASE_URL 経由・開発 DB を掴まない
	// ...
}
```

**適用場面:** `CreateXxxParams`/`UpdateXxxParams` を組み立てる全保存系ハンドラ。とくに `pgtype.Numeric`/`Timestamptz` を含むテーブル（`sensor_readings`, `alert_rules`, `alert_histories`）。

---

## 41. ネスト構造の条件付きバリデーションのテストパターン

> **📅 2026-04-09 — Laravel W002 の `WorkLogUpdateRequest`（`staff.{enum}.user_id` パターン）で発覚した教訓を Go 流に読み替え。**

> 【ADAPT】Laravel では「ネスト配列キーに Enum 値（`staff.61.user_id`）を使い、`is_partner` の真偽で動的に rules を生やす」という FormRequest の動的ルール生成を使っていた。Go には FormRequest も「キー名にドット記法で動的ルールを足す」機構も無い。本質的教訓は **「ある条件フラグが立っているときだけ別フィールド群が必須になる」=「クロスフィールドの条件付き必須」をどう検証するか** であり、これは Go では (a) `binding`/`validator` の `required_if`/`required_unless` タグ、または (b) ハンドラ内の明示的な分岐検証、のどちらかに落ちる。

### 41.1 パターン（本プロジェクトでの相当形）

本プロジェクトのドメインに引き直すと、たとえば「アラートルール登録で `metric` が `temperature` のときだけ、温度域に応じた追加項目を必須にする」といった条件付き必須が該当する。Go では構造体タグの `required_if` で宣言的に書く。

```go
// internal/handler/alert_rule.go（例）
type AlertRuleForm struct {
    Metric    string  `form:"metric"    binding:"required,oneof=temperature humidity"`
    Operator  string  `form:"operator"  binding:"required,oneof=> < >= <="`
    Threshold float64 `form:"threshold" binding:"required"`
    // metric=temperature のときだけ補正係数を必須にする、という条件付き必須の例
    TempOffset *float64 `form:"temp_offset" binding:"required_if=Metric temperature"`
}
```

宣言的タグで表現しきれない複雑な条件（複数フィールドの相関や、フラグに応じた「グループまるごと必須」）は、`binding` を通した後にハンドラ内で明示分岐し、エラーを `map[string]string` に積む。共有マジック（Laravel の `view()->share('errors')`）は無いので、エラーは view struct のフィールドで運ぶ（`auth_validation.go` と同じ流儀）。

```go
// 条件付き必須をハンドラ内で明示検証する場合
func (h *AlertRuleHandler) validate(f AlertRuleForm) map[string]string {
    errs := map[string]string{}
    if f.Metric == string(domain.MetricTemperature) && f.TempOffset == nil {
        errs["temp_offset"] = "温度指標の場合は補正係数を入力してください"
    }
    return errs
}
```

### 41.2 テストデータの構築（valid を起点に override する）

Laravel の `validSaveData(array $overrides)` ヘルパに相当するものを、Go では **「有効値を返すビルダ関数 + 上書き用クロージャ」** で表現する。table-driven の各ケースは「有効フォームをコピーして1か所だけ壊す」と読みやすい。

```go
// 有効なフォームを1個返すビルダ。各ケースはこれを起点に1フィールドだけ崩す。
func validAlertRuleForm() AlertRuleForm {
    offset := 0.0
    return AlertRuleForm{
        Metric:     "temperature",
        Operator:   ">=",
        Threshold:  35.0,
        TempOffset: &offset,
    }
}
```

### 41.3 テストすべき3パターン（table-driven + t.Run）

Laravel の「①フラグONで必須→エラー / ②フラグOFFで不要→通過 / ③正常データ→通過」の3分岐を、そのまま table-driven に移植する。`validator.New().Struct(s)` を直接呼べば HTTP を経由せず単体検証できる（`auth_validation.go` 系の単体検証と同じ）。

```go
func TestAlertRuleForm_条件付き必須(t *testing.T) {
    v := validator.New()

    tests := []struct {
        name    string
        mutate  func(f *AlertRuleForm)
        wantErr bool
    }{
        {
            name:    "温度指標で補正係数なし → エラー",
            mutate:  func(f *AlertRuleForm) { f.TempOffset = nil },
            wantErr: true,
        },
        {
            name:    "湿度指標なら補正係数なしでも通過",
            mutate:  func(f *AlertRuleForm) { f.Metric = "humidity"; f.TempOffset = nil },
            wantErr: false,
        },
        {
            name:    "正常データは通過",
            mutate:  func(f *AlertRuleForm) {},
            wantErr: false,
        },
    }
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            f := validAlertRuleForm()
            tt.mutate(&f)
            err := v.Struct(f)
            if (err != nil) != tt.wantErr {
                t.Errorf("Struct() err=%v, wantErr=%v", err, tt.wantErr)
            }
        })
    }
}
```

### 41.4 注意: フォーム値の型変換タイミング（`$this->boolean()` の罠の相当）

Laravel では `FormRequest::rules()` 内の `$this->boolean('is_partner')` が `'1'`（文字列）を `true`、`''`（空文字）を `false` に解釈する評価タイミングの罠があった。Go の `c.ShouldBind`（フォーム）/`c.ShouldBindJSON` での相当は **「ゼロ値と未送信の区別」** に現れる:

- `bool` フィールドは、未送信でも JSON `false` でも同じゼロ値 `false` になり区別できない。「送られなかった」ことを検出したいなら `*bool`（ポインタ）にして `nil` 判定する。
- `required_if=Metric temperature` のような条件タグは **バインド後の Go の値**で評価される。`oneof` で値域を固定し、テストでは「文字列の `"temperature"` を渡したら確かに条件が発火するか」を上記 table-driven で固定する。
- 数値フィールドはフォームの空文字 `""` がバインド時にエラーになるかゼロ値になるかがタグ依存。`*float64` にして `nil`（未入力）と `0.0`（明示ゼロ）を分けるのが安全。

---

## 42. sensor_readings / alert_rules の挿入で複合的な一意性をテストする

> 📅 2026-04-10 — Laravel W002-1 の `user_work_ledger`（複合PK `(user_id, work_ledger_id)`）集計テスト設計時の教訓を本スキーマへ読み替え。

> 【ADAPT】Laravel 側の問題は「複合主キーを持つ中間テーブルに、同一 `(user_id, work_ledger_id)` で担当種別違いの行を複数 INSERT しようとして PK 重複した」というもの。本プロジェクトのスキーマ（`docs/database_snapshot/table_definitions.md`）には複合PKの中間テーブルは存在せず、各テーブルは `id bigint` の単一サロゲートPK + 一部に UNIQUE 索引（例 `device_tokens_token_hash_unique`）という構成。したがって**複合PKそのものは該当なし**だが、本質的教訓「**集計の DISTINCT/COUNT を検証するテストデータは、一意性制約に矛盾しない形で組み立てねばならない**」と「**Enum 値は DB へは文字列で入れる（型を取り違えない）**」はそのまま通用する。

### 42.1 問題（本プロジェクトでの相当: 同一指標のルール重複）

`alert_rules` には `(device_id, metric)` の UNIQUE 制約は **張られていない**（索引は `(device_id, is_enabled)` の通常索引のみ）。したがって「同一デバイスに `temperature` のルールを2本」入れること自体は DB レベルでは可能。集計テストで「デバイスごとの有効ルール件数」のような COUNT/DISTINCT を検証するときは、**何をもって1件と数えるか**をテストデータが体現している必要がある。Laravel の「同一台帳に複数ロールで紐付いても work は1件」を、本プロジェクトでは「同一デバイスに複数ルールがあっても device は1台」と読み替える。

### 42.2 対策（DISTINCT カウントは別エンティティに紐付ける）

DISTINCT デバイス数を検証したいなら、ルールを**別デバイス**に紐付けてデータを組む。Querier モックの戻り値で組むなら DB 制約に縛られず自由だが、**実DBで COUNT クエリ自体を検証する場合**は制約と矛盾しない INSERT にする。

```go
// 実DB（goose migrate 済みテストDB）で COUNT 系クエリを検証する fixture builder の例。
// device を2台作り、それぞれにルールを紐付ける → DISTINCT device = 2 を固定する。
func seedAlertRulesAcrossDevices(t *testing.T, q *repository.Queries, userID int64) {
    t.Helper()
    ctx := context.Background()

    d1 := mustCreateDevice(t, q, userID)
    d2 := mustCreateDevice(t, q, userID)

    for _, dev := range []repository.Device{d1, d2} {
        _, err := q.CreateAlertRule(ctx, repository.CreateAlertRuleParams{
            DeviceID:  dev.ID,
            Metric:    string(domain.MetricTemperature), // ← Enum は String() で文字列化して渡す
            Operator:  string(domain.OpGreaterThanOrEqual),
            Threshold: pgconv.Numeric2(35.0),
        })
        if err != nil {
            t.Fatalf("CreateAlertRule: %v", err)
        }
    }
    // → COUNT(DISTINCT device_id) = 2 を期待
}
```

### 42.3 適用場面

- 「デバイスごと/ユーザーごとの有効ルール件数」のような集計クエリのテスト
- 担当（紐付け）の入れ替え（論理削除 `deleted_at` セット + 新規 INSERT）時の件数検証

### 42.4 Querier モック経由で「行の一意性に依存しない集計」をテストするパターン

> 教訓: Laravel の Factory `state`（`forTaskSupervisor()`）で「データの意図を命名で明示する」発想は Go でも有効。Querier モックの戻り値を**意図が読めるビルダ関数**で組み立てる。

DB 制約まで含めて検証する必要が無い集計ロジック（例: サービス層が COUNT 結果をどう扱うか）は、Querier をモックして戻り値だけ与えれば DB 不要で速い。Enum 値の扱いだけ注意する（**Go の型値**と **DB の文字列**を取り違えない）。

```go
// alert_rules 行を「意図が読めるビルダ」で作る。Laravel の forTaskSupervisor() state 相当。
func temperatureRule(deviceID int64, threshold float64) repository.AlertRule {
    return repository.AlertRule{
        ID:        0,
        DeviceID:  deviceID,
        Metric:    string(domain.MetricTemperature), // DBは文字列。比較も文字列。
        Operator:  string(domain.OpGreaterThanOrEqual),
        Threshold: pgconv.Numeric2(threshold),
        IsEnabled: true,
    }
}

// モック側はこのビルダで戻り値を組む
type fakeAlertRepo struct {
    rules []repository.AlertRule
}

func (f *fakeAlertRepo) ListAlertRulesByDevice(_ context.Context, _ int64) ([]repository.AlertRule, error) {
    return f.rules, nil
}
```

#### 42.4.1 チェックリスト

- [ ] DB 制約まで検証する必要があるか（→ 実DB fixture）／ロジックだけか（→ Querier モック）を最初に決める
- [ ] 実DB で集計を検証するなら、INSERT が UNIQUE 索引（例 `device_tokens_token_hash_unique`）と矛盾しないデータにする
- [ ] Enum 相当は `string(domain.MetricTemperature)` のように **String() で文字列化**して DB / Params へ渡す（`domain` 型インスタンスを生でカラムに入れない）
- [ ] `numeric(5,2)` カラムへは `pgconv.Numeric2(...)` で `pgtype.Numeric` に変換して渡す（直接 `float64` を渡さない）
- [ ] 集計の意図（何を1件と数えるか）がビルダ関数名から読めるようにする

---

## 43. 集計の AVG/SUM が「行ゼロのとき NULL」になる罠（pgtype.Numeric の Valid 判定）

> 📅 2026-04-10 — Laravel W002-1「集計値が PHP では `'0.00'`（文字列）になる」教訓を sqlc/pgx の NULL ハンドリングに読み替え。

> 【ADAPT】Laravel/MariaDB では `COALESCE(SUM(...), 0)` の戻り値が PHP 側で**文字列 `'0.00'`** として現れる「型のゆるさ」が問題だった（`number_format` が暗黙変換で救ってくれていた）。Go/sqlc/pgx には PHP のような暗黙文字列化は無い。代わりに本質的に同じ落とし穴が **NULL** として現れる: **対象行が1件も無いと `AVG()`/`MAX()`/`MIN()`/`SUM()` は SQL NULL を返し、sqlc はこれを `pgtype.Numeric{Valid: false}` にマップする**。`.Float64Value()` を無条件に使うとゼロ値が混ざり、検証が静かに誤る。`COUNT(*)` だけは行ゼロでも `0` を返し、sqlc では非 NULL の `int64`（本プロジェクトでは `::BIGINT` キャスト済み）になる点も対照的。

### 43.1 問題（行が無いと集計列が NULL になる）

本プロジェクトの `GetSensorReadingsSummary` は `AVG(temperature)::NUMERIC(5,2)` 等を返すが、期間内にデータが無いと `AvgTemperature` は `pgtype.Numeric{Valid: false}`（NULL）になる。`SampleCount` だけは `COUNT(*)::BIGINT` なので `0`（int64）。

```go
// 該当デバイスに当該期間のデータが無いケース
row, err := q.GetSensorReadingsSummary(ctx, repository.GetSensorReadingsSummaryParams{
    DeviceID: dev.ID,
    Recorded:   from, // BETWEEN $2 AND $3
    Recorded_2: to,
})
// row.AvgTemperature.Valid == false  ← NULL（"0.00" ではない！）
// row.SampleCount == 0               ← COUNT は非NULLで 0
```

### 43.2 対策（Valid を必ず見る / 表示側でゼロ埋め）

集計値を float に変換する前に **必ず `Valid` を確認**する。`pgconv` に NULL を 0 扱いするヘルパを用意して使い回すのが安全（テンプレ側でのゼロ判定の重複を避ける）。

```go
// NULL（Valid==false）なら 0 を返す表示用ヘルパ
func NumericOrZero(n pgtype.Numeric) float64 {
    if !n.Valid {
        return 0
    }
    return pgconv.NumericToFloat(n)
}
```

テンプレ側（templ）は数値を受け取って描画するだけにし、NULL 救済はハンドラ/変換層で済ませる。Laravel の `{{ $user->work_amount ?? 0 }}` のような Blade 側ゼロ埋めは、Go では **view struct を組む段階で確定**させる（templ にロジックを持ち込まない）。

### 43.3 テスト時の注意

- **「データなし → 集計 NULL」を明示的に1ケース固定する**。Querier モックなら `pgtype.Numeric{Valid: false}` を返させ、ハンドラ/変換が `0`（または「-」表示）に落とすことを検証する。

```go
func TestSummary_データなしはゼロ表示(t *testing.T) {
    row := repository.GetSensorReadingsSummaryRow{
        AvgTemperature: pgtype.Numeric{Valid: false}, // NULL
        SampleCount:    0,
    }
    if got := NumericOrZero(row.AvgTemperature); got != 0 {
        t.Errorf("NULL は 0 になるべき: got %v", got)
    }
}
```

- **`COUNT` 系（`SampleCount`/`Total`）は NULL にならない**ので `int64` のまま比較してよい。`pgtype` 経由ではない点を取り違えない。
- 出力 HTML を `strings.Contains` で検証する場合、`0` も `0.00` も曖昧にマッチさせず、view struct に確定した値（例: `"0.0 ℃"`）を作ってから検証する。PHP の `'0.00'` 文字列前方一致のような曖昧マッチ（`data-work-amount="0`）は Go では不要。
- `numeric(5,2)` の往復は `pgconv_test.go` の `TestNumeric2_RoundTrip`（第3位四捨五入: `28.505→28.51`）に従う。集計値の期待値は丸め後の値で書く。

---

## 44. 日付グルーピングは DB 側で行う（`DATE(recorded_at)` / `time.Time` の format）

> 📅 2026-04-16 — Laravel W004「`$casts` 未設定の `report_date` が文字列で返り `->format()` で落ちる」教訓を sqlc/pgx + Go の時刻型に読み替え。

> 【ADAPT】Laravel の罠は「Eloquent モデルの日付カラムに `$casts` を付け忘れると文字列のまま返り、`Collection::groupBy(fn => $r->report_date->format())` で `Error: Call to a member function format() on string` になる」。Go では**型の取り違えがそもそも起きにくい**: `timestamp with time zone` は sqlc が `pgtype.Timestamptz`（→ `.Time` で `time.Time`）として返すので、`time.Time.Format(...)` は常に呼べる。本プロジェクトはさらに**日別グルーピングを Go の Collection ではなく SQL 側（`GROUP BY DATE(recorded_at)`）でやる**設計（`ListDailySensorAggregates`）なので、Laravel の「アプリ層 groupBy + format」の罠は構造的に回避済み。残る教訓は **「日付の境界（タイムゾーン）と表示フォーマットを一貫させる」** こと。

### 44.1 問題と本プロジェクトの設計（SQL 側集計）

本プロジェクトは日別集計を SQL で行うため、Go 側で `groupBy + format` する必要が無い。

```sql
-- db/queries/sensor_readings.sql（既存）: 日別グルーピングは DB の DATE() で行う
SELECT
    DATE(recorded_at)               AS reading_date,
    AVG(temperature)::NUMERIC(5, 2) AS avg_temperature,
    ...
  FROM sensor_readings
 WHERE device_id = $1 AND recorded_at >= $2 AND deleted_at IS NULL
 GROUP BY DATE(recorded_at)
 ORDER BY DATE(recorded_at) ASC;
```

`reading_date` は sqlc では `pgtype.Date`（または `time.Time`）として返り、表示時に `time.Time.Format("2006/01/02")` でスラッシュ区切りにできる。Laravel の `Carbon::parse(...)->format('Y/m/d')` に相当するのは Go の `t.Format("2006/01/02")`（参照時刻レイアウト）。

```go
// ハンドラ/変換層で表示用文字列を確定させる
func formatReadingDate(d pgtype.Date) string {
    if !d.Valid {
        return ""
    }
    return d.Time.Format("2006/01/02") // "2026/04/15"
}
```

### 44.2 対策（タイムゾーン境界に注意）

- `DATE(recorded_at)` は **DB セッションのタイムゾーン**で日付に丸める。`recorded_at` は `timestamp with time zone` なので、サーバ/DB の TZ 設定によって「日付の境目」がずれうる。テストでは UTC 基準など TZ を固定して期待値を書く（`pgconv.Timestamptz` は `time.Now().UTC()` を前提にしているのと整合させる）。
- 日付境界をアプリ層で揃えたい場合は、SQL を `DATE(recorded_at AT TIME ZONE 'Asia/Tokyo')` のように明示する設計判断もある。どちらにせよ**テストの期待日付は TZ を明示した上で固定**する。

### 44.3 テスト時の注意

- グルーピング結果のキーは `time.Time` であって文字列ではない。表示文字列（`"2026/04/15"`）を検証するなら、`Format` を通した後の view struct の値を `strings.Contains` する。
- `time.Time.Format("2006/01/02")` は Go の参照時刻レイアウト（`Mon Jan 2 15:04:05 MST 2006`）。`"Y/m/d"` のような他言語フォーマット記号は使えない点に注意。
- 実DB テストで `recorded_at` を入れる際は `pgconv.Timestamptz(time.Date(2026,4,15,0,0,0,0,time.UTC))` のように **TZ 込みで明示**する（`now()` 任せにすると日付境界テストが不安定になる）。

### 44.4 影響範囲（本プロジェクト）

- 日別集計を返す: `ListDailySensorAggregates`（7日/30日グラフ用）
- 期間集計を返す: `GetSensorReadingsSummary`（履歴画面の集計ボックス）
- いずれも `timestamp with time zone` を扱うため、**型エラーの心配は無いが TZ 境界の検証は必須**。新クエリで日付グルーピングを追加するときも、Go 側で `groupBy` し直さず SQL の `DATE()`/`date_trunc()` に寄せる方針を踏襲する。

---

## 45. メール送信テストパターン（mailer interface のモック）

> 📅 2026-04-16 — Laravel W005「`Mail::fake()` + `Mail::assertSent`」の教訓。**本プロジェクトはメール機能未実装のため「将来こう書く」設計提示。**

> 【ADAPT】Laravel は `Mail::fake()` というファサードのスワップでグローバルにメール送信を差し替え、`Mail::assertSent(Mailable::class, fn($mail) => $mail->hasTo(...))` で検証する。Go にはファサードもグローバル swap も無い。本質的教訓「**送信処理を本物のSMTPから切り離し、宛先・件名・本文を構造的に検証する / バリデーション失敗時は送らないことも検証する**」は、Go では **mailer を interface 化して DI し、テストではモックに記録させる**形で実現する。

### 45.1 基本パターン（interface + モック記録）

```go
// internal/mailer/mailer.go（将来追加する想定）
type Mailer interface {
    Send(ctx context.Context, msg Message) error
}

type Message struct {
    To      string
    Subject string
    Body    string
}
```

```go
// テスト用モック: 送信内容を記録する（Laravel の Mail::fake() 相当）
type fakeMailer struct {
    sent []mailer.Message
    err  error
}

func (f *fakeMailer) Send(_ context.Context, msg mailer.Message) error {
    if f.err != nil {
        return f.err
    }
    f.sent = append(f.sent, msg)
    return nil
}
```

```go
func TestSendDailyReport_送信内容を検証(t *testing.T) {
    fm := &fakeMailer{}
    h := &ReportHandler{Mailer: fm} // DI でモックを注入（ファサードの swap は不要）

    r := gin.New()
    r.POST("/reports/send-mail", h.SendMail)

    body := `{"to":"supervisor@example.com","subject":"【日報】2026/05/24","body":"テスト本文"}`
    req := httptest.NewRequest(http.MethodPost, "/reports/send-mail", strings.NewReader(body))
    req.Header.Set("Content-Type", "application/json")
    w := httptest.NewRecorder()
    r.ServeHTTP(w, req)

    if w.Code != http.StatusOK {
        t.Fatalf("status: got %d, body=%s", w.Code, w.Body.String())
    }
    // Mail::assertSent のクロージャ相当: 記録された Message を構造的に検証
    if len(fm.sent) != 1 {
        t.Fatalf("送信件数: got %d, want 1", len(fm.sent))
    }
    got := fm.sent[0]
    if got.To != "supervisor@example.com" || got.Subject != "【日報】2026/05/24" {
        t.Errorf("送信内容が不一致: %+v", got)
    }
}
```

### 45.2 重要な注意点

- **DI が `Mail::fake()` の代わり**。ハンドラは `Mailer` interface を受け取り、本番は SMTP 実装、テストは `fakeMailer` を渡す。グローバルなフェイクは要らない（テスト並列実行でも安全）。
- **送信内容は記録した `Message` を構造体比較**する（Laravel の `hasTo()`/`hasSubject()` 相当）。内部のバイト列やヘッダを直接掘らない。
- **「送らないこと」も検証**する（Laravel の `Mail::assertNotSent`）。バリデーション失敗ケースで `len(fm.sent) == 0` を固定する。
- 送信失敗時の挙動（`fakeMailer.err` をセット）でハンドラが 500 を返すこと、部分的に送って失敗した場合のロールバック有無も table-driven で固定する。

### 45.3 「宛先の二重設定」相当の注意

Laravel では `Envelope(to:)` と `Mail::send()->to()` の両方で宛先を設定すると重複する罠があった。Go の interface 設計でも **宛先の出どころを1か所に固定**する（`Message.To` のみを正とし、SMTP 実装側で重ねて宛先を足さない）。テストで「`fm.sent[0].To` が期待どおり1件」を確認すれば二重設定を検出できる。

### 45.4 バリデーションテスト（API は JSON で 422）

メール送信エンドポイントを API（JSON）として実装するなら、バリデーション失敗は `c.JSON(http.StatusUnprocessableEntity, ...)` を返す（`sensor_api_test.go` の 422 検証と同じ流儀）。`postJson` + `assertJsonValidationErrors` 相当を Go で書く。

```go
func TestSendMail_必須項目バリデーション(t *testing.T) {
    fm := &fakeMailer{}
    h := &ReportHandler{Mailer: fm}
    r := gin.New()
    r.POST("/reports/send-mail", h.SendMail)

    cases := []struct {
        name string
        body string
    }{
        {"全項目欠落", `{}`},
        {"メール形式不正", `{"to":"not-an-email","subject":"x","body":"y"}`},
    }
    for _, tc := range cases {
        t.Run(tc.name, func(t *testing.T) {
            req := httptest.NewRequest(http.MethodPost, "/reports/send-mail", strings.NewReader(tc.body))
            req.Header.Set("Content-Type", "application/json")
            w := httptest.NewRecorder()
            r.ServeHTTP(w, req)

            if w.Code != http.StatusUnprocessableEntity {
                t.Errorf("status: got %d, want 422, body=%s", w.Code, w.Body.String())
            }
            if len(fm.sent) != 0 {
                t.Error("バリデーション失敗時にメールが送られた")
            }
        })
    }
}
```

---

## 46. 集計クエリの「重複加算回避」と「フィルタ同期」のテスト戦略

> 📅 2026-04-16 — Laravel W005「2段階 `fromSub` + `leftJoinSub`（時間集計と金額集計の分離）」の教訓を sqlc/pgx + CTE/サブクエリに読み替え。

> 【ADAPT】Laravel は `fromSub($hoursQuery)->leftJoinSub($amountQuery, ...)` というクエリビルダ API でサブクエリを合成し、バインディング順序を自動管理していた。Go/sqlc には実行時クエリビルダは無く、**SQL は `db/queries/*.sql` に手書きして sqlc がメソッド生成する**（バインディングは `$1,$2` の位置パラメータで明示、順序ミスは起きにくい）。本質的教訓は **(1) 親テーブルの値を子テーブル JOIN 越しに SUM すると行数分だけ重複加算される / (2) 複数サブクエリを合成するときは両方に同じ検索条件を効かせる必要がある** の2点で、これは設計とテストの観点としてそのまま通用する。

### 46.1 背景（重複加算の構造）

「親（例 `sensor_readings`）の値を、子テーブルと JOIN した状態で `SUM` する」と、子の行数だけ親の値が重複する。Laravel は時間集計と金額集計を別サブクエリに分けて回避していた。Go では **集計の粒度ごとにクエリを分け、サブクエリ/CTE で明示的に集計してから JOIN する** SQL を `db/queries/` に書く。本プロジェクトの現行クエリ（`ListDailySensorAggregates`）は単一テーブルの `GROUP BY DATE(...)` なので重複加算は起きないが、将来 `alert_histories` を絡めた「日別の計測集計 + 同日アラート件数」のような合成を書くときに該当する。

```sql
-- 例: 日別の計測集計（CTE）と 同日アラート件数（CTE）を分離合成する。
-- 親(reading)の AVG を子(alert)の JOIN 越しに取らないことで重複加算を防ぐ。
WITH daily_readings AS (
    SELECT DATE(recorded_at) AS d,
           AVG(temperature)::NUMERIC(5,2) AS avg_temp,
           COUNT(*)::BIGINT AS sample_count
      FROM sensor_readings
     WHERE device_id = $1 AND recorded_at >= $2 AND deleted_at IS NULL
     GROUP BY DATE(recorded_at)
),
daily_alerts AS (
    SELECT DATE(ah.triggered_at) AS d,
           COUNT(*)::BIGINT AS alert_count
      FROM alert_histories ah
      JOIN alert_rules ar ON ar.id = ah.alert_rule_id
     WHERE ar.device_id = $1 AND ah.triggered_at >= $2 AND ah.deleted_at IS NULL
     GROUP BY DATE(ah.triggered_at)
)
SELECT r.d AS reading_date, r.avg_temp, r.sample_count,
       COALESCE(a.alert_count, 0)::BIGINT AS alert_count
  FROM daily_readings r
  LEFT JOIN daily_alerts a ON a.d = r.d
 ORDER BY r.d ASC;
```

### 46.2 テスト戦略

合成集計クエリは **実DB（goose migrate 済みテストDB）で検証する**のが本筋（NULL/重複/フィルタは Querier モックでは再現できない）。検証ポイントは Laravel と同じ3点:

1. **集計の正確性** — 同一日に複数 reading があるとき平均/件数が正しいこと
2. **重複回避** — `LEFT JOIN` した側の行数で件数が膨らんでいないこと（reading 側が alert 件数分に増えていないか）
3. **フィルタ同期** — 両 CTE に同じ `device_id`/期間条件が効いていること（片方だけ効くと「行は出るが片側の集計だけ NULL/0」になる）

### 46.3 テスト例（同一日の集計正確性）

```go
func TestDailyCombined_同一日の集計が正しい(t *testing.T) {
    q := newTestQueries(t) // goose migrate 済みテストDB（接続文字列で本番/開発と分離）
    ctx := context.Background()
    dev := mustCreateDevice(t, q, mustCreateUser(t, q).ID)

    day := time.Date(2026, 5, 24, 0, 0, 0, 0, time.UTC)
    insertReading(t, q, dev.ID, 25.0, day.Add(1*time.Hour)) // 同日2件
    insertReading(t, q, dev.ID, 27.0, day.Add(2*time.Hour))

    rows, err := q.ListDailyCombined(ctx, repository.ListDailyCombinedParams{
        DeviceID: dev.ID,
        From:     pgconv.Timestamptz(day),
    })
    if err != nil {
        t.Fatalf("query: %v", err)
    }
    if len(rows) != 1 {
        t.Fatalf("日別行数: got %d, want 1", len(rows))
    }
    // 平均: (25.0 + 27.0)/2 = 26.0、件数: 2
    if got := pgconv.NumericToFloat(rows[0].AvgTemp); math.Abs(got-26.0) > 0.01 {
        t.Errorf("avg_temp: got %v, want 26.0", got)
    }
    if rows[0].SampleCount != 2 {
        t.Errorf("sample_count: got %d, want 2", rows[0].SampleCount)
    }
}
```

### 46.4 フィルタ同期テスト（片側だけ効くバグの検出）

Laravel の `assertSee`/`assertDontSee` による間接検証は、Go では **返ってきた行を直接アサート**する（HTML を経由しないぶん明確）。「`device_id` フィルタが片方の CTE にしか効かない」バグは、**他デバイスのデータが混入していないか**を行レベルで固定して検出する。

```go
func TestDailyCombined_デバイスフィルタが両CTEに効く(t *testing.T) {
    q := newTestQueries(t)
    ctx := context.Background()
    user := mustCreateUser(t, q)
    target := mustCreateDevice(t, q, user.ID)
    other := mustCreateDevice(t, q, user.ID)

    day := time.Date(2026, 5, 24, 0, 0, 0, 0, time.UTC)
    insertReading(t, q, target.ID, 25.0, day.Add(time.Hour))
    insertReading(t, q, other.ID, 99.0, day.Add(time.Hour)) // 別デバイス: 混入してはいけない

    rows, _ := q.ListDailyCombined(ctx, repository.ListDailyCombinedParams{
        DeviceID: target.ID,
        From:     pgconv.Timestamptz(day),
    })
    // 平均が 25.0 のまま（99.0 が混ざって (25+99)/2=62 になっていない）= フィルタが両側に効いている
    if got := pgconv.NumericToFloat(rows[0].AvgTemp); math.Abs(got-25.0) > 0.01 {
        t.Errorf("別デバイスのデータが混入: avg=%v", got)
    }
}
```

**ポイント:** フィルタが片側の CTE にしか効いていないと、「日付行は出るが反対側の集計だけ NULL/0」または「他デバイスの値が混入して平均がずれる」という形で**この行レベルアサートが落ちる**。Laravel の `assertSee`/`assertDontSee` による間接検証より、Go では失敗時の差分が読みやすい。

### 46.5 SQL は `db/queries/*.sql` に手書き（クエリビルダを使わない）

Laravel の `fromSub`（推奨）vs `DB::raw + mergeBindings`（バインディング順序ずれリスクあり）の対比は、Go では **「sqlc 手書きSQL（位置パラメータ `$1,$2`）」一択**に収斂する。動的にSQL文字列を組み立てる（`fmt.Sprintf` で WHERE を継ぎ足す等）のは SQL インジェクションとパラメータ順序ずれの温床なので避け、検索条件のバリエーションは**クエリを分けて持つ**（COUNT 用と本体用を別 `-- name:` で持つのと同じ方針）。

```go
// NG: 文字列連結で WHERE を動的生成（パラメータ順序ずれ・インジェクションの温床）
//   query := "SELECT ... WHERE device_id = " + fmt.Sprint(id)

// OK: sqlc 生成メソッド + 位置パラメータ。条件の有無はクエリを分けて持つ
rows, err := q.ListDailyCombined(ctx, repository.ListDailyCombinedParams{
    DeviceID: id, From: pgconv.Timestamptz(from),
})
```

---

## 47. templ `{ }` と HTML エンティティの二重エスケープ検出

> 📅 2026-04-17 — Laravel W005「Blade `{{ }}` で `&yen;` が `&amp;yen;` に二重エスケープ」の教訓を templ に読み替え。§4.2 の補足。

> 【ADAPT】Blade の `{{ $x }}` も templ の `{ x }` も**出力を常に自動 HTML エスケープ**する点は同じ。したがって罠も同型: Go 文字列リテラルに HTML エンティティ参照（`&yen;`, `&#165;`, `&deg;`）を含めると `&` 自体が `&amp;` に変換され、画面に「&yen;16,000」とリテラル表示される。詳細は [HTMX実装ガイド §59](HTMX実装ガイド(動的).md) を参照。

### 47.1 問題

モック HTML の `&#165;`（¥）や `&deg;C`（℃）を templ にそのまま文字列リテラルとして持ち込み `{ }` で出力すると、二重エスケープされる。

```go
// NG: HTML エンティティを Go 文字列に含めると &deg;C → &amp;deg;C にエスケープされる
templ Temperature(v string) {
    <span>{ v + "&deg;C" }</span> // 画面に「25&deg;C」とリテラル表示
}

// OK: UTF-8 文字を直接使う（domain.Metric.Unit() が "℃"/"%" を返す設計と整合）
templ Temperature(v string, unit string) {
    <span>{ v } { unit }</span> // unit = domain.MetricTemperature.Unit() == "℃"
}
```

本プロジェクトでは `domain.Metric.Unit()` が `"℃"`/`"%"` という **UTF-8 文字を直接返す**（`metric_test.go` の `TestMetric_Unit` で固定済み）。templ にはこの UTF-8 値をそのまま渡せば二重エスケープは起きない。

### 47.2 テストでの検出方法

templ テスト（`render(t, Comp(...))` → `strings.Contains`）は **生成された HTML 文字列そのもの**を見るので、Laravel の `assertSee`（textContent ベースで二重エスケープを見逃す）と違い、**二重エスケープを直接検出できる**。`&amp;` が出ていないことをアサートに含める。

```go
func TestTemperature_二重エスケープしない(t *testing.T) {
    html := render(t, Temperature("25", domain.MetricTemperature.Unit()))

    assertContains(t, html, "25 ℃")           // UTF-8 がそのまま出ている
    if strings.Contains(html, "&amp;") {       // 二重エスケープの痕跡が無いこと
        t.Errorf("二重エスケープ (&amp;) が出力に混入:\n%s", html)
    }
    if strings.Contains(html, "&deg;") || strings.Contains(html, "&yen;") {
        t.Errorf("HTMLエンティティがリテラル混入:\n%s", html)
    }
}
```

Laravel では「`assertSee('¥')` が通ってしまい、ブラウザ実機 + DevTools の `innerHTML` 確認でしか発見できない」罠だった。**templ + `strings.Contains` は出力 HTML を直接見るため、ユニットテストで二重エスケープを捕捉できる**（実機確認に頼らなくてよい）のが Go 側の利点。開発時の最終目視は MCP chrome-devtools で行うが、回帰防止はこのユニットテストで担保する。

### 47.3 予防策

- templ の `{ }` に渡す Go 文字列では、**HTML エンティティ（`&yen;`/`&#165;`/`&deg;`）を使わず UTF-8 文字（`¥`/`℃`）を直接使う**（HTMX実装ガイド §59 の対応表に従う）。
- 単位・記号は `domain.Metric.Unit()` のような **ドメイン型のメソッド経由**で取得し、UTF-8 文字を一元管理する（モックからのコピペで `&deg;` が紛れ込むのを防ぐ）。
- HTML エンティティを使ってよいのは **`{ }` を通さない静的 HTML 直書き部分のみ**。動的値の埋め込み（`{ }`）には絶対に混ぜない。
